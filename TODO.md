# TODO

| id | status | severity | effort | description |
|---|---|---|---|---|
| T370 | open | medium | large | Separate `host/src/input.rs` wire decoding/controller dispatch from session settings, coordinate mapping and Linux uinput/KWin/X11 adapters. `InputServer` directly owns concrete devices and `apply_tablet_resolution` loads persistent config. Introduce narrow input/settings interfaces and shared Rust/Android wire fixtures, preserving authentication, controller ownership, event ordering, device release and geometry tests. Reuse those fixtures when resolving T247/T276/T318; do not merge transport authentication or platform mapping policies merely because their loops resemble each other. |
| T376 | open | medium | large | Split Android `VideoReceiver.kt` socket framing, decoder ownership/watchdog and timing/statistics coordination behind narrow clock/codec/transport boundaries. Start/stop, Surface lifecycle, reader/output threads and callbacks currently share one concrete mutable object, and tests reach into private fields. Preserve every generation check and ownership handoff; retain restart, fragmented packet, Surface destruction, codec failure and stop/start coverage. T339 fallback semantics and T386 performance experiments require their own evidence, not incidental changes during extraction. |
| T377 | open | medium | medium | Separate Android `MainActivity.kt` session coordination/window policy from Compose rendering and settings sections. `onCreate,connectStreamCallbacks,applyStreamSettings` and UI functions directly coordinate Prefs, VideoReceiver and TouchCapture; SettingsSheet mixes unrelated control groups. Introduce observable session/settings state and explicit events with an activity-scoped coordinator; extract focused UI sections and window-policy adapter. Preserve app-only brightness/refresh defaults, user overrides, switching apps, orientation, reconnect and settings tests; do not restart streaming merely because Compose recomposes. |
| T380 | open | medium | large | Split `host/evdi/evdi_helper.c` conversion kernels/worker pool, frame exchange/FIFO writer and EVDI event/mode lifecycle into explicit contexts and ownership interfaces. Conversion jobs already carry their inputs, but pool/buffer/mode lifetime still depends on numerous shared globals; native tests include the entire production translation unit. Start with conversion/pool boundaries, preserve distinct scaled/native hot paths, and retain sanitizer-backed mode-change, dirty-row, worker-start failure and writer-shutdown coverage. Preserve callback/buffer lifetime and T226's FIFO quarantine/replacement and encoder-generation retirement guarantees; do not claim that moving globals alone removes races. |
| T382 | open | medium | medium | Extend the [single-tablet baseline](docs/benchmarks/2026-09-17-device-baseline.md) into reproducible end-to-end performance baselines and stage traces. Its preserved CPU, latency-window and USB net-charge samples complement the T384/T387 synthetic artifacts but do not measure complete capture-to-display latency, isolated app power or multi-device scaling. Before broader device reuse, replace the initial benchmark collector's verified-for-this-tablet 100 Hz/4096-byte Android constants with validated per-device unit discovery. Record commit/toolchain/hardware/settings, capture/convert/encode/send/receive/decode/render stages, input latency, p50/p95/p99, delivered FPS, CPU/PSS/allocations and power/thermal observations for 1/2/4 tablets. Include static text, scrolling, motion and pen workloads; preserve raw samples and distinguish clock domains, simulated ACKs and USB net battery charge. Profile actual Android serialization/GC as well as decoder work. Use T408 to prevent the fake client from distorting throughput. Follow the [performance research](docs/reviews/2026-09-17-performance-scalability.md) and [copy/loop review](docs/reviews/2026-09-17-copy-loop-power.md). |
| T383 | open | medium | medium | Benchmark aggregate C conversion CPU/memory budgets across tablets. `evdi_helper.c:conv_pool_init` independently requests CPUs minus two, clamped to 1..8, per helper; compare 1/2/4/8 workers, restricted CPUs and 1/2/4 sessions before choosing a host-wide budget. `mark_damage` repeats per-row bit updates across overlapping rectangles; `bgra_to_nv12` wakes every worker and both kernels inspect every assigned chroma row even for sparse damage. Compare merged dirty ranges/word masks, nonempty-job dispatch and density-aware partitioning, then portable vectorization/scaled-kernel specialization. Measure conversion time, context switches, bandwidth, lock time and tail latency for empty/sparse/overlapping/full damage and scales 1..4. Preserve exact color rounding, stride, all three buffers' dirty histories, mode change, worker failure and shutdown tests. Oversubscription and potential speedups remain hypotheses. See the [copy/loop review](docs/reviews/2026-09-17-copy-loop-power.md). |
| T386 | open | medium | large | Evaluate Android decoder scheduling/capability profiles in `VideoReceiver.kt:feedDecoder,startOutputThread,decoderFormat,releaseCodec`. Input waits up to ten 20 ms dequeue attempts while holding the receiver monitor; output polls at 10 ms with MAX_PRIORITY, and low-latency/2x operating-rate/vendor hints are applied broadly. Compare the current path with a single codec owner using callbacks and a bounded access-unit queue; capability-gate experiments with hint combinations, operating rate and normal thread priority. Measure wakeups, blocked time, allocation, latency, battery and sustained output, including sparse/idle streams. Coordinate direct input-buffer experiments with T403 and power policy with T388. Preserve CSD/IDR, reconnect/Surface generations, callback retirement and T339 fallback coverage. Use the [baseline](docs/benchmarks/2026-09-17-device-baseline.md) sparse-versus-motion timing difference to guide correlated queue/output/callback traces before choosing a cause or policy. No default/profile change without device measurements across supported API levels and hardware. |
| T388 | open | medium | medium | Investigate the reported Android battery drain and design a measured, user-selectable power-saving mechanism. Compare UScreen off, static display, scrolling/video, pen-only, waiting, background and reconnect on USB and Wi-Fi with fixed brightness, charging source and temperature. Record decoder identity/hardware acceleration, observed refresh, CPU wakeups, power rails when available, net battery trend and negotiated USB power; separate app cost from insufficient charging power. `StreamingService.kt` holds CPU/Wi-Fi locks independent of useful work/transport; `MainActivity.kt:UScreenMain` polls FPS/Mbps every second even with stats hidden; `VideoReceiver.kt` polls decoder output and requests performance hints. Evaluate state/transport-aware locks, stopping hidden telemetry polls, T386 decoder profiles, T399 idle/frame pacing and T400 codec choices. Specify an opt-in battery profile with hysteresis, recovery and clear user override before automatic FPS/scale/quality changes; preserve app-only 50% brightness/60 Hz defaults and normal settings in other apps. Require deterministic lifecycle/lock-release/profile tests and sustained physical-device A/B power plus latency/quality evidence; do not infer savings from loopback routing or Robolectric. The [4490763 device baseline](docs/benchmarks/2026-09-17-device-baseline.md) reproduced 189.81 mAh net loss over 35.5 minutes of battery samples, with hardware decoding and reported 5 V/500 mA USB power; /sys/class/typec is absent. The gauge moves in 9.99 mAh steps, limiting short-phase comparisons. Add an app-off control and longer matched power trials; compare a documented higher-power data port separately. Reported charging limits are not input-power measurements or an inherent USB 2.0 limit. See the [power research](docs/reviews/2026-09-17-copy-loop-power.md). |
| T389 | open | medium | large | Compare raw-frame copy/mmap/backend options with stock FFmpeg, using T380 ownership boundaries. `evdi_helper.c` swaps three NV12 pointers but writes whole frames to FIFO; optional `encoder.rs:run,encode,copy_plane` reads into a Vec then copies into writable stride-padded AVFrame planes. Benchmark a contiguous-stride copy fast path, direct filling of leased writable planes, and a memfd/MAP_SHARED ring with explicit FD handoff; mapping the existing FIFO is not a solution. Specify bounded slots, size/stride/format/sequence/generation, acquire/release synchronization, encoder retention, resize/shrink protection, backpressure and crash recovery. Compare aligned allocation/anonymous mmap/THP advice using actual page backing and page-fault measurements; current MADV_HUGEPAGE advice is not proof of huge-page use or copy elimination. Keep corrected FIFO/CLI fallback and T226 recovery tests. Separately assess framed FIFO and PipeWire VIRTUAL/DMA-BUF plus hardware frames; validate desktop support, modifiers, synchronization and device matching. Preserve optional VAAPI rejection until hardware-context integration is validated and retain T259 bitrate policy. No FFmpeg patches. See the [copy/loop review](docs/reviews/2026-09-17-copy-loop-power.md). |
| T390 | open | medium | medium | Test and improve multi-device discovery fairness in `host/src/main.rs:adb_monitor,unique_devices,app_devices_with,TabletMonitor`. Device identity/app checks and session setup are awaited sequentially, so one slow ADB device extends the polling cycle for others. Build deterministic fake-ADB slow/failing-device scenarios and measure time to connect/recover unaffected tablets and stop. Evaluate bounded per-device concurrency with explicit global ADB serialization where needed, stable USB/Wi-Fi identity, backoff and cancellation. Preserve T281/T278/T330 decisions and T328 descendant-lifetime limitations; never parallelize shared forwarding changes without ownership. |
| T391 | open | medium | medium | Establish multi-session/client resource and slow-consumer limits using `main.rs` broadcast queues and `stream.rs,input.rs` admission. Eight-packet video queues bound count, not payload bytes or stalled writes; cheap Bytes clones share storage. `ClientPlayback::drain_batch` allocates a Vec for each batch and front-drains at the newest IDR; benchmark bounded reusable scratch/in-place selection without extending payload retention. Test 1/2/4 sessions with fast/slow/unauthenticated clients, codec changes and reconnect churn; record allocations, peak retained bytes, tasks, FD count and tail latency. Define byte/time budgets and fair recovery from measurements, preserving generation/IDR resynchronization and T399 skip policy. Account for whole backing allocations retained by T402/T407 shared slices. Keep the four-tablet limit until aggregate budgets are validated. |
| T344 | blocked | low | small | Blocked on the maintainer choice: preserve a dormant pointer preference or clear it during an explicit setting change. Resolve pointer-preference handling while the pen is disabled. `gui/src/main.rs:setting_input_devices` says a disabled checkbox must not retain a value the daemon would act on, and clears input_pointer during every render. However, `host/src/input.rs:InjectDevices::create` already gates pointer creation on both pen and pointer, and `FileConfig::sanitize` preserves the dormant preference. An isolated egui probe rendering the production settings method without events changes pen=false/pointer=true to pointer=false and marks the configuration dirty; pen=true preserves it. Unblock by choosing one consistent policy: preserve the dormant pointer preference, or explicitly clear it during a deliberate setting change/shared sanitization, then align the comment and UI. Required permanent T344 coverage: opening the tab without input, disabling/re-enabling the pen, saving/reloading and discarding edits; no host input devices are needed. |
| T332 | blocked | medium | medium | Blocked on the user-visible policy for unsupported resolution/FPS combinations: reject the edit or negotiate a fallback. Resolve independently accepted resolution/FPS settings versus the generated EDID pixel-clock limit. `common/src/model.rs:FileConfig::sanitize`, `input.rs:negotiated_geometry,apply_tablet_config` and both FPS selectors accept 3840x2160@90, but `capture.rs:helper_command` passes that mode to `edid.rs:make_edid_sized`, which rejects its clock above 655.35 MHz before helper startup. An offline probe of the production config/EDID code accepts and generates 3840x2160@60, retains 3840x2160@90 unchanged during sanitization, then fails generation; 4095x4095@60 fails likewise. A live FPS change can therefore replace a working mode with repeated setup failures. Unblock by choosing a shared joint-mode validation/negotiation policy: reject incompatible changes before retiring the current stream, or explicitly negotiate and display a supported resolution/refresh fallback. Apply it to CLI, persisted settings, GUI and tablet updates; do not remove the generator's overflow guard. Required permanent T332 coverage: clock-boundary combinations, valid 4K60→invalid 4K90 update with current-session preservation, automatic/manual geometry and accurate effective settings. Distinct from T321's low-clock consumer warning. Both generators use custom fixed-porch timings; the T325 comment correction did not change that timing contract. No display was attached. |
| T328 | open | medium | medium | Resolve command-timeout scope in `common/src/commands.rs` versus delegated operations such as `gui/src/main.rs:run_system_setup` (`pkexec sh -c ...`). Sync/async timeout paths kill only the direct child, so reporting timeout does not stop descendants from continuing the operation. An isolated probe of production SyncCommandExt with a disposable shell, a 100 ms deadline and a worker that writes after 300 ms reports timeout while the worker remains alive and subsequently writes; the probe reaps its own descendant and touches only a temporary directory. Resolve by defining and implementing cancellation of delegated work, including privileged descendants that the unprivileged caller cannot signal, or explicitly narrowing the operation/deadline contract and exposing continued work accurately. Required permanent T328 coverage: sync timeout, async timeout/cancellation, descendant retirement and no writes after completed cancellation, successful-command behavior and isolated privileged delegation if retained. Preserve T094's direct-child reaping assertions; no host setup or privileged process was exercised. |
| T321 | blocked | low | medium | Blocked on the supported low-clock timing contract and isolated compositor acceptance evidence. Resolve accepted low-resolution/low-refresh settings versus EDID validator compatibility. `common/src/model.rs:FileConfig::sanitize` accepts 640x480@10, `host/src/capture.rs:helper_command` uses that refresh for the display, and both EDID generators accept it with a 4.14 MHz DTD clock. Offline edid-decode --check rejects that generated descriptor with Pixelclock < 10 MHz, assuming invalid data; the [primary parser](https://android.googlesource.com/platform/external/edid-decode/+/refs/heads/android15-qpr1-s5-release/parse-base-block.cpp) contains that threshold. This proves a validator incompatibility, not a reproduced kernel/display failure. Unblock by selecting and documenting the accepted timing/consumer contract: generate suitable timings while retaining the requested mode, or restrict incompatible combinations consistently across config, negotiation and both generators. Required permanent T321 coverage: smallest supported geometry at minimum FPS, cases around the selected pixel-clock boundary, generator parity and chosen rejection/fallback behavior; any display acceptance claim needs an isolated compositor test. No host display was attached. |
| T308 | blocked | low | small | Resolve fork package-maintainer metadata: `README.md:8-9` and `docs/index.html:44,431` identify geraldo-netto as the fork maintainer and majmichu1 as the original author, while `packaging/deb/control:8` and `packaging/arch/PKGBUILD:1` still identify majmichu1 as the package maintainer. `packaging/build-packages.sh` copies that Debian field into fork releases without replacement. Unblock with the intended fork package-maintainer name and public contact address, then align Debian and Arch metadata while preserving upstream authorship and historical attribution. No email address should be inferred from a username or local Git identity. Metadata-only correction needs no artificial regression test. |
| T288 | blocked | medium | small | Blocked on the user-visible dependency policy when graphics-tablet mode is selected with the pen disabled. Resolve the GUI's selectable graphics-tablet mode with the pen device disabled: `gui/src/main.rs:setting_mode,setting_input_devices` permits and saves pen_only=true/input_pen=false while displaying that nothing will be streamed, but `host/src/main.rs:initial_pen_only` replaces that combination with display mode. Reproduce by selecting graphics-tablet mode, disabling Pen tablet, saving, then restarting; the saved GUI selection and effective daemon mode differ. Unblock by choosing a coherent dependency policy (reject/disable the incompatible selection, or explicitly enable the required pen) and exposing the effective result before restart. Required permanent T288 GUI/config/CLI fixtures: both toggle orders, an existing incompatible file, save/reload, and unchanged valid display-only and pen-only combinations; no real daemon or input-device startup. |
| T281 | open | high | medium | Resolve attachment identity versus geometry readiness: `host/src/main.rs:TabletMonitor::update_primary` publishes detach and attach through a boolean watch channel, while `spawn_display_gate` clears geometry_ready only when it observes false. Tokio watch can coalesce those transitions; a current-thread probe using the production gate sends false then true before yielding and retains active=true, geometry_ready=true from the previous tablet. This conflicts with T223's fresh-geometry-before-attachment requirement in `host/src/capture.rs`. Resolve by representing attachment identity/generation or invalidating readiness at the producer, defining physical-tablet replacement versus same-tablet transport migration, and associating negotiated metadata with the current authenticated session. Required permanent T281 regression: delayed gate consumer, immediate replacement with different native/physical dimensions, and USB/Wi-Fi migration; no helper attach with stale geometry, and no invalidation of newly accepted current-session metadata. Use fake helpers only. |
| T276 | blocked | low | small | Blocked on the protocol decision: reply dimensions describe the virtual display or the scaled encoded stream, including the pre-negotiation state. Define consistent dimensions in control replies: `host/src/input.rs:InputConfig::response` reads live FPS/codec from EncoderSettings but always emits virtual_width/virtual_height captured at startup in `host/src/main.rs`; negotiated geometry changes the actual display afterward. A probe using the production response function with startup 2960x1848 and live 1280x800@30 returns width=2960, height=1848, fps=30. `docs/architecture.md:Input/control WebSocket` now warns that these dimensions are stale; the reply fields still lack a consistent live-geometry contract. Unblock by specifying whether width/height describe the current virtual display or scaled encoded stream, implementing that meaning for initial/repeated replies, and documenting fallback behavior before geometry is ready. Required permanent T276 coverage: negotiated resolution changes, reconnect, non-unit stream_scale, and matching serialized host/client fixtures. The current Android decoder uses its native-size hint rather than these fields, so this finding concerns protocol consistency and other consumers. |
| T259 | blocked | medium | medium | Blocked on the bounded-versus-explicitly-uncapped VAAPI policy and validation on a supported driver. Resolve the VAAPI bitrate-ceiling contradiction: the configuration/UI offer a bitrate limit, but both `host/src/capture.rs:encoder_quality_args` and `host/src/encoder.rs:low_latency_options` force VAAPI CQP. [FFmpeg 6.1 rate-control code](https://github.com/FFmpeg/FFmpeg/blob/n6.1/libavcodec/vaapi_encode.c#L1618-L1624) marks CQP as using neither bitrate nor maxrate and bypasses bitrate control parameters for it. The configured ceiling therefore does not constrain this path; common/src/lib.rs and docs/development.md now document that limitation, but the UI/policy remains unresolved. Unblock by selecting a driver-supported bounded rate-control policy (with explicit handling of unsupported modes), or documenting and exposing an intentionally uncapped mode. Required permanent T259 coverage: CLI/in-process option parity and capability fallback fixtures, plus real VAAPI high-detail output measurement against the chosen cap. The [Navi 23 device baseline](docs/benchmarks/2026-09-17-device-baseline.md) measured median motion output of 28.85–29.22 Mb/s with a configured 20 Mb/s ceiling, confirming the existing CQP limitation on this driver; it does not select or validate a replacement policy. |
| T250 | blocked | medium | small | Resolve the fork release-signing contract: `android/app/build.gradle.kts` accepts any supplied release keystore, but an official fork signing identity and migration from upstream have not been established. SECURITY.md and docs/development.md describe this as pending and no longer assert the upstream fingerprint as the fork identity. A developer-generated key alone does not establish an official update contract. Unblock with the maintainer's chosen fork certificate and update/migration policy, then align documentation and, if an official certificate is required, verify its public fingerprint before publishing. Documentation-only changes need no artificial test; a publication gate needs a permanent wrong-certificate regression. |
| T222 | blocked | high | large | Diagnose and prevent the Xorg crash on EVDI attachment under Cinnamon. On 2026-09-17 at 01:52:40 CEST, Xorg faulted at address `0x20` through `ProcRRSetCrtcGamma`, immediately after `host/evdi/evdi_helper.c:1361` attached the virtual display; the later helper restart did not precede the fault. Blocked on identifying the failing Xorg state with a symbolized core or isolated reproducer and validating a mitigation. Required permanent coverage: isolated Xorg/EVDI attach, gamma update and detach without server/session loss; current fake-helper tests cannot exercise the system display server. See [incident evidence](docs/reviews/2026-09-17-cinnamon-restart.md). |
| T395 | open | high | medium | Investigate Android cold-start foreground-service timeout. On the RugKing Pad 2 Pro running Android 16, the freshly installed debug APK from 29b8068 crashed at 2026-09-17 12:25:50 CEST with ForegroundServiceDidNotStartInTimeException for StreamingService (PID 9152), after MainActivity and host token delivery launched together. The next process (9276) connected successfully and reported isForeground=true. MainActivity.onStart requests startForegroundService, onStop stops it, and StreamingService promotes only in onStartCommand; the exact failing lifecycle sequence is not yet established. Reproduce with controlled cold start, token delivery and rotation/background transitions; preserve timestamped lifecycle evidence. Add a permanent failing lifecycle regression before fixing the demonstrated cause, covering prompt foreground promotion, cancellation/re-entry and lock release. Do not treat a successful retry as resolution. |
| T399 | open | medium | medium | Research whether additional frame skipping reduces latency, host work and Android battery drain. `evdi_helper.c:claim_writer_frame,writer_frame_due` already coalesces to the latest raw frame and suppresses unchanged frames between 200 ms keepalives; `stream.rs:ClientPlayback` already skips backlog to an IDR. Compare no additional skipping, pre-conversion/pre-encode admission, longer idle intervals and decode-all/render-latest behavior under motion, static and slow-link loads. Arbitrarily dropping encoded reference frames can corrupt dependent pictures; preserve CSD/keyframe recovery and never discard part of a raw FIFO frame. Specify timestamps, ACK/loss accounting, join/reconnect, watchdog/read deadlines and idle-to-motion latency, including CLI wall-clock versus in-process frame-count GOP behavior. Require automated decoding/lifecycle fixtures and sustained CPU/bandwidth/battery/quality measurements before selecting a policy. The [device baseline](docs/benchmarks/2026-09-17-device-baseline.md) showed static-phase median encoded rates of 5.0–12.95 FPS on a shared Cinnamon desktop; isolate unrelated damage/capture activity before treating static-phase differences as optimization gains. Coordinate T388/T391; [research](docs/reviews/2026-09-17-copy-loop-power.md). |
| T400 | open | medium | medium | Research useful codec alternatives: compare current H.264/HEVC against AV1 and VP9, with intra-only/MJPEG only if latency or recovery evidence warrants it. `common/src/encoding.rs` registers five H.264/HEVC encoders; `media.rs,annex_b.rs`, Android `CodecReportReceiver.kt` and `ControlSession.kt` assume those codecs. Inventory actual stock-FFmpeg encoders and physical Android hardware decoders, profiles, levels, bit depths, dimensions/FPS and simultaneous-session limits; platform format support alone does not prove hardware acceleration. Measure equivalent desktop/text/pen quality, encode/decode latency, USB/Wi-Fi bandwidth, sustained power and thermal behavior. Specify codec-specific access-unit framing/CSD/random access, capability negotiation, old-client fallback and permanent shared protocol/decode tests before adding a codec. Retain distro portability and unmodified FFmpeg; [research](docs/reviews/2026-09-17-copy-loop-power.md). |
| T401 | open | medium | large | Evaluate further architecture improvements that support T389/T399/T400/T388 without parallel rewrites: explicit raw-frame leases and encoded-packet ownership, separate capture timing/encoder pacing/presentation policy, versioned capability negotiation, and bounded per-session resource/telemetry interfaces. `EncoderSettings::helper_geometry` includes FPS so ordinary FPS edits rebuild the helper; rapid battery adaptation through that path would repeatedly recreate the display. Produce a staged decision record comparing current process isolation/CLI fallback with optional in-process/shared-memory backends and a common session lifecycle. Reuse T368/T370/T376/T377/T380 boundaries and the Windows plan; keep Linux adapters separate from portable protocol/policy. Define generation-tagged cancellation, fault recovery, compatibility and observability with fake-backend contract tests before implementation. Hardware evidence gates backend promotion; [research](docs/reviews/2026-09-17-copy-loop-power.md). |
| T402 | open | medium | medium | Evaluate retaining encoded AVPacket storage instead of copying every packet in optional `host/src/encoder.rs:Encoder::drain` (`Bytes::copy_from_slice`). Compare an owning immutable packet wrapper/Bytes::from_owner using stock libavcodec reference or move APIs with the current copy. Prove stable payload range, reference-counted fallback, thread transfer/read safety and final release across broadcast clones, slow clients and encoder retirement; never borrow data from the reused local packet. Measure allocation/copy savings versus retained codec memory. Add permanent delayed-consumer, subsequent-encode, cancellation and exactly-once-release tests plus real decode equivalence before changing ownership. Distinct from raw-plane transport T389; [API research](docs/reviews/2026-09-17-copy-loop-power.md). |
| T403 | open | medium | large | Evaluate avoiding Android compressed-input staging copy in `VideoReceiver.kt:receivePackets,feedDecoder`: packets enter a reused heap ByteArray, then ByteBuffer.put copies the payload into MediaCodec input. After T376/T386 ownership work, compare bounded SocketChannel reads into codec-owned input buffers with the current path; optionally evaluate API 30+ LinearBlock copy-free compatibility with a supported-codec check and API 27 fallback. Direct buffers do not guarantee device/kernel zero-copy. Preserve validated lengths, fragmented headers/payloads, EOF, CSD/frame metadata, sequence timestamps, buffer capacity, socket cancellation and codec/Surface generations; a blocked socket must not monopolize codec shutdown. Require permanent transport/codec contract tests and physical-device allocation/CPU/latency/power measurements before enabling; [API research](docs/reviews/2026-09-17-copy-loop-power.md). |
| T404 | open | low | medium | Profile diagnostic lookup/allocation cost before changing it: Android `VideoReceiver.kt:noteReleased,decodeMicrosFor` linearly scan a 64-slot ring with modulo per iteration; Rust `latency.rs:on_rendered` searches a bounded deque and `maybe_report` discards sample-vector capacity after each report. Compare sequence-tagged direct slots, safe sample-buffer reuse and reduced lock contention while preserving exact timing/ACK semantics. These are small bounded structures, so require measured benefit. Define cross-thread publication and generation identity, reset reused release timestamps, and test sequence wrap, missing/delayed/duplicate/out-of-order callbacks, reconnect and concurrently reported samples; do not change ACK frequency as an incidental optimization. Coordinate T382/T376; [review](docs/reviews/2026-09-17-copy-loop-power.md). |
| T405 | open | medium | medium | Measure host polling wakeups and replace avoidable timed polling with readiness/cancellation events. Optional `encoder_io.rs:read_frame` sleeps 2 ms on EAGAIN/5 ms on EOF; `stream.rs` and `input.rs` poll running flags every 100 ms, and initial codec config polls at 100 ms. C `capture_poll_timeout` caps active waits at 4 ms and `wait_for_writer_frame` wakes each frame period even for idle keepalives. Compare poll/epoll plus a stop event, watch-based config/cancellation and exact work/keepalive deadlines; do not turn FIFO HUP/no-writer into a readiness busy loop. Retain T226 inode quarantine, partial-frame discard, prompt stop, mode change, startup/no-reader and lost-wakeup regressions with fake clocks/FIFOs/helpers. Measure idle and active wakeups plus p99 latency across sessions before changing timers; [review](docs/reviews/2026-09-17-copy-loop-power.md). |
| T406 | open | medium | medium | Benchmark batching Linux uinput writes in `host/src/input.rs:UInputDevice::emit,syn,inject_pen` and `InjectDevices::inject_touch`. Each event currently calls write_all; a single motion sample emits several event writes and possibly multiple intentional SYN_REPORT frames. Use a bounded native-event buffer flushed at each existing synchronization boundary; preserve every event and pen/button/proximity ordering without waiting for future samples. Verify native layout/padding and partial-write/error semantics, release_all and controller replacement. Require byte/frame-equivalent permanent pen/touch fixtures (including T318) and a counting writer benchmark before adoption; no real desktop input needed. Coordinate adapter extraction T370; [kernel/API research](docs/reviews/2026-09-17-copy-loop-power.md). |
| T407 | open | low | medium | Benchmark remaining CLI packetizer copies after T384 in `host/src/capture/cli_encoder.rs:read_loop` and `annex_b.rs:push,process_complete_nals,append_picture_slice`. Stdout fills a scratch buffer, push copies into retained input, completed NALs copy into access-unit assembly, and prefix drains move retained tails. Compare reading into owned spare capacity, offset-based compaction and BytesMut/owned segments with at most one required coalescing copy. Bound backing-buffer retention for slow clients and avoid repeated vector/scatter-gather work on dense NAL streams. Use the committed T384 replay/counters plus real H.264/HEVC decode cases; preserve byte-identical prefixes, CSD revisions, partial headers, EOF and encoder generation retirement. Measure against 1e7b045, not the pre-T384 baseline; [review](docs/reviews/2026-09-17-copy-loop-power.md). |
| T408 | open | low | small | Keep the synthetic tablet from becoming a copy/loop bottleneck in scalability measurements. `scripts/fake-tablet.py:read_exact` repeatedly concatenates immutable bytes on fragmented reads, and receive_video allocates/retains the whole encoded body just to inspect type/sequence. Compare reusable bytearray/memoryview recv_into and bounded draining after header validation, including BufferedSocket upgrade leftovers. Measure 1/2/4 fake clients with large frames and tiny fragments; add permanent exact-byte, EOF, fragmentation and ACK-sequence tests. Preserve smoke-test behavior and explicitly label ACKs as receive acknowledgements with synthetic decode_us, not actual decode/render latency. Feed results into T382; [review](docs/reviews/2026-09-17-copy-loop-power.md). |
| T409 | open | low | medium | Measure and reduce desktop status polling work in `gui/src/main.rs:poll_status,App::new` and `common/src/linux/processes.rs:same_user_processes`. Every two-second cycle repeats ADB/autostart/program/device probes and reads process exe/cmdline/cwd/start identity before filtering ownership; the GUI also requests a repaint each second. Compare cheaper ownership/name prefilters, a validated PID status fast path with fallback and invalidation, cached static capabilities, and lifecycle-bound status notifications from the daemon. Preserve full process discovery for destructive actions, PID-reuse/UID/native-path checks, stale PID recovery, external setup changes and bounded command failures. Use fake /proc/ADB/status sources to measure scan/syscall volume and refresh latency; retain UI correctness and cancel polling when its owner ends. Coordinate T390 and T401; [review](docs/reviews/2026-09-17-copy-loop-power.md). |
