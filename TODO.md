# TODO

| id | status | severity | effort | description |
|---|---|---|---|---|
| T382 | blocked | medium | medium | Single-tablet work can proceed with the connected RugKing Pad 2 Pro; complete scaling validation still requires additional tablets and a larger/NUMA host for sustained 128-participant throughput. Extend the [single-tablet baseline](docs/benchmarks/2026-09-17-device-baseline.md) into reproducible end-to-end performance baselines and stage traces. Its preserved CPU, latency-window and USB net-charge samples complement the T384/T387 synthetic artifacts but do not measure complete capture-to-display latency, isolated app power or multi-device scaling. Record commit/toolchain/hardware/settings, capture/convert/encode/send/receive/decode/render stages, input latency, p50/p95/p99, delivered FPS, CPU/PSS/allocations and power/thermal observations for 1/2/4 tablets. Include static text, scrolling, motion and pen workloads; preserve raw samples and distinguish clock domains, simulated ACKs and USB net battery charge. Profile actual Android serialization/GC as well as decoder work. Physically replay T420 token recovery with another Android app foreground, then resume UScreen; deterministic host/API 27/34 tests do not substitute for that device observation. Use T408 to prevent the fake client from distorting throughput. Validate T383's work-sensitive conversion budgets on large/NUMA hosts, including sustained 128-participant scaling and cgroup CPU-time quotas; the current 32-logical-CPU replay proves functional capacity, not 128-core throughput. Follow the [performance research](docs/reviews/2026-09-17-performance-scalability.md) and [copy/loop review](docs/reviews/2026-09-17-copy-loop-power.md). |
| T388 | blocked | medium | medium | Complete sustained power validation in an uninterrupted unlocked Linux session with the tablet available for the full balanced normal/profile static/motion matrix and streaming-off control. The [guarded attempt](docs/benchmarks/2026-09-18-power-validation.md) completed three five-minute phases, then correctly rejected normal-motion after keyboard-focus loss; repeated pairs and the sustained control did not run. Original preferences were restored. Physical lock ownership matches policy, but coarse 9.99 mAh charge steps and incomplete pairs establish no battery-saving gain. Retain waiting/pen/background/reconnect observations, available network transport and any available higher-power data-source comparison. Only USB ADB was available; physical network/pen validation needs a prepared isolated route/setup that avoids the unresolved T222 desktop-attachment failure. Preserve preferences, conservative unknown-route behavior, raw observations and clock/measurement boundaries. The [implementation report](docs/benchmarks/2026-09-18-power-policy.md) retains permanent regressions and prior lifecycle checks; T395 remains independently blocked on a reproducible cause. |
| T344 | blocked | low | small | Blocked on the maintainer choice: preserve a dormant pointer preference or clear it during an explicit setting change. Resolve pointer-preference handling while the pen is disabled. `gui/src/main.rs:setting_input_devices` says a disabled checkbox must not retain a value the daemon would act on, and clears input_pointer during every render. However, `host/src/input.rs:InjectDevices::create` already gates pointer creation on both pen and pointer, and `FileConfig::sanitize` preserves the dormant preference. An isolated egui probe rendering the production settings method without events changes pen=false/pointer=true to pointer=false and marks the configuration dirty; pen=true preserves it. Unblock by choosing one consistent policy: preserve the dormant pointer preference, or explicitly clear it during a deliberate setting change/shared sanitization, then align the comment and UI. Required permanent T344 coverage: opening the tab without input, disabling/re-enabling the pen, saving/reloading and discarding edits; no host input devices are needed. |
| T332 | blocked | medium | medium | Blocked on the user-visible policy for unsupported resolution/FPS combinations: reject the edit or negotiate a fallback. Resolve independently accepted resolution/FPS settings versus the generated EDID pixel-clock limit. `common/src/model.rs:FileConfig::sanitize`, `input.rs:negotiated_geometry,apply_tablet_config` and both FPS selectors accept 3840x2160@90, but `capture.rs:helper_command` passes that mode to `edid.rs:make_edid_sized`, which rejects its clock above 655.35 MHz before helper startup. An offline probe of the production config/EDID code accepts and generates 3840x2160@60, retains 3840x2160@90 unchanged during sanitization, then fails generation; 4095x4095@60 fails likewise. A live FPS change can therefore replace a working mode with repeated setup failures. Unblock by choosing a shared joint-mode validation/negotiation policy: reject incompatible changes before retiring the current stream, or explicitly negotiate and display a supported resolution/refresh fallback. Apply it to CLI, persisted settings, GUI and tablet updates; do not remove the generator's overflow guard. Required permanent T332 coverage: clock-boundary combinations, valid 4K60→invalid 4K90 update with current-session preservation, automatic/manual geometry and accurate effective settings. Distinct from T321's low-clock consumer warning. Both generators use custom fixed-porch timings; the T325 comment correction did not change that timing contract. No display was attached. |
| T321 | blocked | low | medium | Blocked on the supported low-clock timing contract and isolated compositor acceptance evidence. Resolve accepted low-resolution/low-refresh settings versus EDID validator compatibility. `common/src/model.rs:FileConfig::sanitize` accepts 640x480@10, `host/src/capture.rs:helper_command` uses that refresh for the display, and both EDID generators accept it with a 4.14 MHz DTD clock. Offline edid-decode --check rejects that generated descriptor with Pixelclock < 10 MHz, assuming invalid data; the [primary parser](https://android.googlesource.com/platform/external/edid-decode/+/refs/heads/android15-qpr1-s5-release/parse-base-block.cpp) contains that threshold. This proves a validator incompatibility, not a reproduced kernel/display failure. Unblock by selecting and documenting the accepted timing/consumer contract: generate suitable timings while retaining the requested mode, or restrict incompatible combinations consistently across config, negotiation and both generators. Required permanent T321 coverage: smallest supported geometry at minimum FPS, cases around the selected pixel-clock boundary, generator parity and chosen rejection/fallback behavior; any display acceptance claim needs an isolated compositor test. No host display was attached. |
| T308 | blocked | low | small | Resolve fork package-maintainer metadata: `README.md:8-9` and `docs/index.html:44,431` identify geraldo-netto as the fork maintainer and majmichu1 as the original author, while `packaging/deb/control:8` and `packaging/arch/PKGBUILD:1` still identify majmichu1 as the package maintainer. `packaging/build-packages.sh` copies that Debian field into fork releases without replacement. Unblock with the intended fork package-maintainer name and public contact address, then align Debian and Arch metadata while preserving upstream authorship and historical attribution. No email address should be inferred from a username or local Git identity. Metadata-only correction needs no artificial regression test. |
| T288 | blocked | medium | small | Blocked on the user-visible dependency policy when graphics-tablet mode is selected with the pen disabled. Resolve the GUI's selectable graphics-tablet mode with the pen device disabled: `gui/src/main.rs:setting_mode,setting_input_devices` permits and saves pen_only=true/input_pen=false while displaying that nothing will be streamed, but `host/src/main.rs:initial_pen_only` replaces that combination with display mode. Reproduce by selecting graphics-tablet mode, disabling Pen tablet, saving, then restarting; the saved GUI selection and effective daemon mode differ. Unblock by choosing a coherent dependency policy (reject/disable the incompatible selection, or explicitly enable the required pen) and exposing the effective result before restart. Required permanent T288 GUI/config/CLI fixtures: both toggle orders, an existing incompatible file, save/reload, and unchanged valid display-only and pen-only combinations; no real daemon or input-device startup. |
| T276 | blocked | low | small | Blocked on the protocol decision: reply dimensions describe the virtual display or the scaled encoded stream, including the pre-negotiation state. Define consistent dimensions in control replies: `host/src/input.rs:InputConfig::response` reads live FPS/codec from EncoderSettings but always emits virtual_width/virtual_height captured at startup in `host/src/main.rs`; negotiated geometry changes the actual display afterward. A probe using the production response function with startup 2960x1848 and live 1280x800@30 returns width=2960, height=1848, fps=30. `docs/architecture.md:Input/control WebSocket` now warns that these dimensions are stale; the reply fields still lack a consistent live-geometry contract. Unblock by specifying whether width/height describe the current virtual display or scaled encoded stream, implementing that meaning for initial/repeated replies, and documenting fallback behavior before geometry is ready. Required permanent T276 coverage: negotiated resolution changes, reconnect, non-unit stream_scale, and matching serialized host/client fixtures. The current Android decoder uses its native-size hint rather than these fields, so this finding concerns protocol consistency and other consumers. |
| T259 | blocked | medium | medium | Blocked on the bounded-versus-explicitly-uncapped VAAPI policy and validation on a supported driver. Resolve the VAAPI bitrate-ceiling contradiction: the configuration/UI offer a bitrate limit, but both `host/src/capture.rs:encoder_quality_args` and `host/src/encoder.rs:low_latency_options` force VAAPI CQP. [FFmpeg 6.1 rate-control code](https://github.com/FFmpeg/FFmpeg/blob/n6.1/libavcodec/vaapi_encode.c#L1618-L1624) marks CQP as using neither bitrate nor maxrate and bypasses bitrate control parameters for it. The configured ceiling therefore does not constrain this path; common/src/lib.rs and docs/development.md now document that limitation, but the UI/policy remains unresolved. Unblock by selecting a driver-supported bounded rate-control policy (with explicit handling of unsupported modes), or documenting and exposing an intentionally uncapped mode. Required permanent T259 coverage: CLI/in-process option parity and capability fallback fixtures, plus real VAAPI high-detail output measurement against the chosen cap. The [Navi 23 device baseline](docs/benchmarks/2026-09-17-device-baseline.md) measured median motion output of 28.85–29.22 Mb/s with a configured 20 Mb/s ceiling, confirming the existing CQP limitation on this driver; it does not select or validate a replacement policy. |
| T250 | blocked | medium | small | Resolve the fork release-signing contract: `android/app/build.gradle.kts` accepts any supplied release keystore, but an official fork signing identity and migration from upstream have not been established. SECURITY.md and docs/development.md describe this as pending and no longer assert the upstream fingerprint as the fork identity. A developer-generated key alone does not establish an official update contract. Unblock with the maintainer's chosen fork certificate and update/migration policy, then align documentation and, if an official certificate is required, verify its public fingerprint before publishing. Documentation-only changes need no artificial test; a publication gate needs a permanent wrong-certificate regression. |
| T222 | blocked | high | large | Diagnose and prevent the Xorg crash on EVDI attachment under Cinnamon. On 2026-09-17 at 01:52:40 CEST, Xorg faulted at address `0x20` through `ProcRRSetCrtcGamma`, immediately after `host/evdi/evdi_helper.c:1361` attached the virtual display; the later helper restart did not precede the fault. Blocked on identifying the failing Xorg state with a symbolized core or isolated reproducer and validating a mitigation. Required permanent coverage: isolated Xorg/EVDI attach, gamma update and detach without server/session loss; current fake-helper tests cannot exercise the system display server. See [incident evidence](docs/reviews/2026-09-17-cinnamon-restart.md). |
| T395 | blocked | high | medium | Establish a reproducible failing startup sequence or timestamped lifecycle/main-thread trace for the original Android 16 foreground-service timeout. The affected tablet is connected, but [30 device trials](docs/reviews/2026-09-18-foreground-start.md)—12 historical-source isolated starts, 12 current-source isolated starts and 6 installed-app live-host starts—did not reproduce it. Existing early promotion, rejection, cancellation/re-entry and lock-release regressions pass; successful starts do not establish the original cause. Preserve token-delivery order, process identity, service promotion and ApplicationExitInfo on recurrence; add a permanent failing regression before a demonstrated-cause fix. Historical source in a separate package does not recreate the original fresh-install/authenticated-stream state. |
| T414 | blocked | medium | large | Requires confirmation of the playback app/device, Bluetooth pairing/source and constant-offset versus growing-drift behavior, followed by the affected tablet for physical measurement. Investigate and improve audio/video synchronization: the user reports YouTube video appearing slightly behind audio on an older UScreen build. The user describes the audio setup as “bluetooth headset with a tablet”; verify the Bluetooth source/paired device and whether YouTube runs on Linux through UScreen or directly on Android. Record exact host/APK versions, stream settings and whether the offset is constant or grows over time; distinguish the reported older build from the subsequently deployed candidate. Reproduce with a timestamped flash/click clip and measure initial offset, sustained drift, pause/seek, reconnect, overload and recovery. Trace browser/compositor capture, raw FIFO age, encoder buffering, transport, Android decode and Surface presentation against the actual audio clock; larger pipes can hide stale video. `host/src/stream.rs` and `android/.../DecoderSession.kt` use sequence IDs as codec presentation timestamps for ACK identity, not a shared media clock, and the current protocol has no audio stream. T386 physical traces found the callback render-time argument equal to `sequence * 1000` rather than a comparable `System.nanoTime()` value; the boolean output-release API propagates input PTS to Surface timestamps. Preserve these raw traces and compare explicit monotonic output-release timestamps while retaining sequence-based ACK identity before interpreting render-time differences as latency. Separate latency reduction from any proposed audio-delay or synchronized-audio feature; retain sequence/ACK identity if real media timestamps are added. Coordinate T382/T386/T389/T399/T405. Before fixing a confirmed cause, add a permanent failing deterministic clock/queue regression, then prove it passes; require physical A/V offset/drift measurements for claimed playback improvements. |
| T417 | deferred | low | medium | Explicitly requested for later UI work; profile controls versus read-only diagnostics still needs a decision. Follow up on T386 by exposing decoder capabilities and the selected decoder profile in the Android app. Show the active codec identity, hardware-acceleration status, advertised standard low-latency support and supported operating rate for the current codec, dimensions and stream FPS. Distinguish advertised support, requested hints and confirmed active behavior; show unknown when Android cannot verify an effective setting, and identify watchdog fallback. Explain that advertised 2x rate headroom is not measured throughput or a latency guarantee, and that missing standard low-latency support does not by itself imply slow decoding. Use T386's repeated motion/sparse-stream measurements to explain any recommended profile. Before later UI implementation, confirm whether this should include profile-selection controls or read-only diagnostics; preserve current defaults until an explicit, measured policy is chosen. Include permanent tests for supported, unsupported and unknown capabilities, API 27 fallback, codec/stream changes and fallback reporting; if controls are added, also cover persistence and safe session transitions. Requested as a future follow-up, outside the current implementation batch. |
| T418 | deferred | medium | large | Production integration was left outside the requested implementation batch; retain this as the separate follow-up experiment. Follow up on T389's isolated memfd-ring measurements with a production raw-frame transport experiment. Convert directly into bounded shared slots and wrap read-only slot leases in stock libavcodec AVBufferRef/AVFrame ownership so encoder-retained references delay reuse; sending mapped pixels through another FIFO would retain the main copy boundary. Implement explicit descriptor handoff, validated version/format/dimensions/strides/size/sequence/generation, shrink/grow seals, acquire/release state transitions, cancellation and peer-crash/resize recovery. Preserve the stock-FFmpeg CLI/FIFO fallback and optional VAAPI rejection. Require permanent retained-frame, malformed-descriptor, stale-generation, full-ring, partial-startup, crash and mode-change tests before enabling, followed by actual conversion/encode/display latency at 60 FPS and 1/2/4 streams. The synthetic ring uses memset and sampled-byte verification, not capture, full encoder reads, USB or physical display; its throughput gains are not end-to-end gains. Keep this follow-up outside the current implementation batch. |
| T419 | deferred | medium | large | Explicitly requested as later compression research, separate from the implemented video-codec work. Research alternative frame compression for a future desktop-streaming mode, separately from T400's video-codec comparison. Compare full frames, changed rectangles/tiles and temporal deltas using fast lossless LZ4 or low-level Zstandard, with QOI/PNG or intra-image coding as controls, against hardware-decoded H.264/HEVC. Evaluate a hybrid only if static text/pen content benefits and switching to motion has bounded latency without quality flashes or stale pixels. Preserve original RGB/chroma detail when measuring lossless text fidelity; compressing NV12 cannot recover already discarded chroma. Separate host-only raw-frame IPC compression from wire-to-Android compression, using T389/T418 direct/shared-memory transport as the local baseline. Prioritize capture-to-display p50/p95/p99, motion recovery, bandwidth, Android decode/composition/upload cost, sustained battery/thermal behavior and 1/2/4-stream scaling; ample host CPU/RAM does not establish a latency or Android power gain. Measure any second compression of already encoded video as a control rather than assuming savings. Specify capability negotiation, tile/base-frame identity, atomic presentation, bounded decompressed sizes, loss/reconnect/full-refresh recovery and fallback; require permanent round-trip, corruption, stale-generation and lifecycle tests before enabling. Start from [LZ4](https://github.com/lz4/lz4) and [Zstandard](https://github.com/facebook/zstd) primary documentation; corpus benchmarks do not predict desktop-frame results. Coordinate T399/T400/T403/T414. Requested as later research, outside the current implementation batch. |
| T422 | blocked | medium | medium | Find a compatible stock encoder/configuration or validated driver/firmware correction for AMD VAAPI HEVC → Unisoc native error 14. [26 controlled device trials](docs/benchmarks/2026-09-18-hevc-interop.md) establish 15 failed VAAPI cases versus 11 successful x265 controls; alternate CSD delivery, disabled decoder hints, ten stock VAAPI variants and removal of optional AUD/SEI do not fix it. All 20 archived fixtures yield 240 software frames, but four VAAPI variants emit decode errors and are excluded as clean interoperability controls; the original failing VAAPI fixture is software-clean. T461 now rejects decoder errors even when FFmpeg returns zero. T458/T460 repaired the replay prerequisites; valid trials preserve source/APK hashes, parameter sets and native logs. Blocked on a working stock configuration or a trace isolating the native failure before a cause-specific change; do not patch FFmpeg or rewrite undocumented bitstream fields. Add a permanent failing regression for the demonstrated cause and verify the physical tablet before closing. Preserve H.264 fallback and distinguish advertised HEVC capability from render-verified success. |
| T429 | blocked | medium | medium | The affected tablet is connected; confirmation of the exact option originally selected is still required for the reported transition. The current live configuration uses standard h264_vaapi, not h264_vaapi_baseline. Compare it with the lower-delay profile measured in T400 before recommending a live migration. Encoder changes still use Apply & restart through `gui/src/settings.rs:apply_label` and `gui/src/main.rs:apply`, so one full reconnect is expected; pipe-only changes now apply live (T415). This does not establish the reported encoder-transition duration or resumed profile. Investigate the user's report that selecting the low-latency option makes UScreen keep searching for a while before streaming resumes. Confirm the exact option, measure the interruption and distinguish an expected encoder restart from repeated connection/decoder recovery; record host/APK versions, GPU/driver, codec, resolution/FPS and timestamped host/Android logs. Compare normal and explicit low-latency VAAPI H.264 selection transitions with otherwise identical settings, checking configuration persistence, encoder restart/failure, capture/display ownership, H.264 headers and Android decoder setup/recovery. Verify that the resumed stream actually uses the selected profile rather than silently reverting. T400's isolated Constrained Baseline benchmark does not prove the live transition works. Preserve a working default and avoid unrelated EVDI/Cinnamon restarts during diagnosis. Add a permanent failing regression for any demonstrated cause before fixing it, covering transition duration/retries, recovery, profile retention and switching back; verify live playback on the affected device. |
| T444 | blocked | medium | medium | Choose the attachment-admission contract, then retire stale routes before reusing a slot. Robust isolation needs attachment-specific credentials on both input and video; current per-run credentials and tokenless legacy clients cannot identify which tablet a late TCP connection came from. Unblock by choosing credential rotation with explicitly unisolated no-auth mode, or preserving shared-token compatibility with route/listener quarantine. T413's transient package-query repair prevents its erroneous handoff, but `monitor.rs:change_primary` still leaves old reverse mappings and a shared token after legitimate replacement; a newly accepted reconnect through an old route obtains the replacement epoch. Reproduce with two fake devices and real isolated control sockets, then enforce route retirement or attachment-specific authentication before admitting the replacement. Preserve accepted-socket generation checks, USB/Wi-Fi migration and explicit/manual launch behavior. |
| T445 | blocked | medium | medium | Choose the `auto_launch` policy after Android kills UScreen while another app is foreground. T420 removes token-retry Activity launches, but `host/src/main.rs:recover_app` deliberately launches a missing process when `auto_launch` is enabled; process absence cannot distinguish a streaming crash from background eviction. The request to allow use of another app conflicts with unconditional process-recovery launches in that case. Unblock with the desired distinction between launch on attachment, recovery of an active stream and background eviction, then add host/lifecycle regressions before changing the established auto-launch behavior. |
| T463 | open | medium | medium | Serialize persisted pipe settings and live publication across GUI processes. `gui/src/settings.rs:PendingSave::start_with_pipe` releases the ConfigStore transaction before calling `apply_pipe`; two saves can commit 2 then 4 MiB but publish 4 then 2 MiB, leaving the helper at the older request indefinitely. An isolated barrier-controlled probe of the production save worker reproduces persisted=4/live_request=2 without touching the live daemon. Use a shared transaction/revision-aware publisher or one command owner; process-local busy flags do not order separate GUIs or daemon startup publication. Add permanent deterministic reversed-publication, startup/save interleaving and failed-publication regressions before fixing; preserve saved preferences and avoid display restarts. |
| T464 | open | low | medium | Give Android release checks an explicit lifecycle owner and cancellation boundary. `SessionCoordinator.checkUpdate` launches an untracked Thread capturing `dispatchUi`/the Activity; `stop` never cancels it, and a completion still changes updateAvailable after stopping the Activity or disabling checkUpdates. `UpdateCheck.newerThan` owns its Call internally and sets connect/read timeouts but no whole-call deadline. Extract an injected cancellable release-check adapter from the session/media coordinator, reject obsolete completions and honor the current update preference; avoid introducing a generic event bus. Required permanent coverage: delayed response across stop/recreation, disabling checks in flight, cancellation and a bounded trickling response; use a local fake HTTP endpoint, never GitHub or the physical tablet. |
| T465 | open | medium | medium | Revisit automatic encoder selection after a verified stream fails. `host/src/selection/worker.rs:run` permanently marks a settings key completed after its trial; verification requires only three render ACKs, and later encoder/decoder health is not an input. Android video-only recovery leaves the control connection and selection key intact, so a candidate that renders three frames then repeatedly fails can be selected indefinitely without trying a working fallback. Introduce bounded, epoch-tagged health notifications owned by the session/selector, with retry/backoff and a fallback policy; a focused watch/Notify plus deadlines can also replace the 50 ms ACK polling, without a generic event bus. Required permanent coverage before fixing: successful trial followed by sustained failure, video-only reconnect, late events from retired encoders, exhausted candidates and recovery; distinguish background/sparse content from failure. This is a static recovery-path finding, not a reproduced failure on the connected tablet. |
| T466 | open | medium | medium | Configure H.264/HEVC decoders using the negotiated encoded dimensions. `ControlSession.requestDecoderCapabilities` checks the host's video_width/video_height, but only codec/FPS reach `SessionCoordinator`; nativeResolution instead supplies the panel dimensions to `VideoReceiver.ensureSurfaceCodec`. Legacy-codec setup must succeed before the video socket opens, so a scaled stream within decoder limits can be rejected because the larger panel-sized configuration is unsupported. Propagate one validated, generation-scoped stream format to capability reporting and decoder setup, with an explicit old-host fallback and safe format transitions. Required permanent regression: a fake decoder accepting the negotiated scaled format but rejecting native panel dimensions, plus resize/reconnect, stale greetings and old hosts. No physical failure is claimed; VP9/AV1 already obtain dimensions from their framed configuration. |
| T467 | open | medium | small | Align Arch build and package output directories. `packaging/arch/PKGBUILD:build` invokes Cargo without --target-dir, while package always copies target/release; CARGO_TARGET_DIR or a Cargo config override therefore makes a clean package fail or packages stale local binaries. An isolated invocation of the production build function with minimal real Cargo crates confirms new binaries land externally while the packaging source remains stale. The Makefile source-install route already pins its output; apply the same explicit contract to both Arch binaries. `scripts/ci/build-arch-package.sh` supplies prebuilt files and exercises only package, so it cannot detect this source-build defect. Required permanent regressions before fixing: external target directory and Cargo config override, with deliberately stale target/release binaries, proving the package contains the newly built daemon and GUI. |
| T468 | open | low | medium | Share compatible decoder selection between Android capability reporting and codec creation. `VideoCodec.kt:DecoderCapabilities.report` advertises hardware support if any decoder accepts the exact format, while `DecoderConfiguration.create` bypasses that selection for H.264/HEVC and calls createDecoderByType; only VP9/AV1 select the compatible, hardware-preferred decoder by name. Automatic host ranking can therefore rely on a different decoder from the one actually opened. After T466, extract one format/decoder-selection policy used by both paths, retain measured hint defaults and handle creation failure explicitly. Required permanent coverage: default decoder incompatible or software while another compatible hardware decoder exists, no compatible decoder, API 27 unknown hardware classification and codec retirement. This is a conditional compatibility/architecture gap, not evidence that the current tablet selected software. |
| T469 | open | medium | medium | Verify benchmark artifact contents instead of trusting copied hash metadata. `scripts/benchmarks/codec-host.py:trial` and `codec-latency.py:trial` label input with scene.sha256 without checking the raw file; `codec-match.py` compares metadata strings only, and `codec-fixtures.py` does not compare its encoded source against the selected result's sha256. An isolated real libx264 trial with an intentionally false corpus hash succeeds and passes cache validation. Introduce a shared content/size/provenance validator before encoding, cache reuse and fixture export; reject changed raw or encoded files and bind quality to the exact replayed bytes. Required permanent regressions before fixing: same-length modified corpus, stale metadata, substituted encoded output and matching artifacts; preserve the existing clean-decode/frame-count checks and historical evidence rather than silently relabeling it. |
| T470 | open | medium | medium | Make the full-device baseline invalidate interrupted tablet sessions and failed observers. `scripts/benchmarks/run-baseline.py` checks only an initial Android PID; its workload visibility guard checks Linux windows/lock state, while `observe.py:Sampler.run` can exit on an exception without notifying the workload. `summarize.py` accepts a completed visibility-guarded workload even with no samples/logs, and never verifies that UScreen stayed foreground or that the logged Android PID survived. Backgrounding/restarting the tablet app or losing collection can therefore leave apparently complete performance/battery output. Add a bounded read-only Android session/foreground health guard and owned observer error/completion reporting, then quarantine affected phases with explicit reasons and minimum evidence requirements. Required permanent fake-ADB/sampler regressions: background/obscuring keyguard, PID replacement, ADB failure, collector exception, missing/stale samples and healthy sparse streaming; never restore focus automatically. Do not invalidate historical measurements without checking their retained evidence. |
| T471 | blocked | low | small | Resolve the remaining historical latency-decomposition contradiction. `docs/benchmarks.md:How latency is measured` correctly says independent medians cannot isolate network stages, but its USB results and `README.md:Performance` still call 5–7 ms the remaining transport/queueing time; `docs/index.html` preserves wire ~6.2 ms with a caption that describes its contents without saying the subtraction is invalid. Historical paired raw samples are unavailable. Resolution requires removing/relabeling the derived component as an invalid legacy estimate, or supplying paired evidence that supports a replacement; preserve the separately measured historical numbers and label old log excerpts explicitly. This is documentation-only and needs no artificial regression test. |
| T472 | open | medium | medium | Make live settings mutations atomic against automatic selection. `host/src/input/settings.rs:apply_tablet_config` clones a watch value then sends the whole snapshot; apply_tablet_resolution similarly computes outside send_if_modified. The selector publishes concurrently, so either path can overwrite newer selection/capability state. A deterministic isolated probe of the production config function injects selection publication at its tracing boundary: h264_vaapi becomes libx264 again while the selection key stays unchanged. Because the worker has completed that key, selection can remain lost. Apply field-scoped commands to current state inside one watch transaction, or use one session settings owner; preserve controller/attachment guards. Fix before T465. Required permanent regressions: publication between read/write, duplicate or clamped configuration, repeated geometry and capability/selection updates, proving unrelated fields survive and no unnecessary encoder restart occurs. |
| T473 | blocked | low | small | Resolve the Android startup ownership documentation contradiction. `docs/architecture.md:DecoderSession` says codec creation/configuration occurs within the receiver monitor; `DecoderSession.setupCodec` now explicitly performs create/configure/start outside that monitor after reserveStartup, then revalidates and publishes under it, with a process-wide startup gate. The old statement misdescribes the fix and encourages reintroducing UI-blocking native work. Resolution requires updating the architecture description to the existing reserve/work/publish/retire contract, including cancellation of an unfinished startup and the separate bounded retirement wait. No behavior change or artificial regression test is required. |
