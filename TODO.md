# TODO

| id | status | severity | effort | short description |
|---|---|---|---|---|
| T078 | open | medium | medium | Fix workspace-built GUI accessibility startup (`gui/Cargo.toml`, `host/Cargo.toml`, `gui/src/main.rs::main`): `cargo build --release --workspace` unifies ksni's zbus Tokio feature into AccessKit; launching GUI panics with `there is no reactor running`. Standalone GUI build avoids it. Regression: retain a workspace-built GUI/AT-SPI startup smoke test that detects worker panics. |
| T082 | open | medium | medium | Reject or safely handle modes smaller than the downscale block (`host/evdi/evdi_helper.c::on_mode_changed`, `convert_strip_scaled`): minimum output 2×2 with a 2×2 source and scale 4 reads beyond the source allocation. ASan reproduced heap-buffer-overflow at line 220. Regression: exercise small explicit-EDID modes and all scales through the C harness with allocation guards/ASan. |
| T083 | open | medium | medium | Synchronize cross-thread helper state (`host/evdi/evdi_helper.c`, `g_writer_busy`, `g_mode_generation`, `g_running`): volatile reads/writes in writer, mode-change, and shutdown paths are not all protected by the same mutex or C atomics. Regression: retain a threaded mode-change/shutdown stress case under ThreadSanitizer; use defined ownership/publication for generation and writer state. |
| T087 | open | medium | medium | Allocate multitouch slots by active pointer identity (`android/app/src/main/java/com/uscreen/TouchCapture.kt::slotOf`): clamping Android IDs to 0–9 aliases valid IDs 10 and 11 to slot 9. Robolectric probes reproduced both packets using slot 9 on API 27 and 34. Regression: sparse/high pointer IDs get distinct slots, retained through index reshuffling and released on UP/CANCEL. |
| T088 | open | medium | medium | Isolate VideoReceiver sessions (`android/app/src/main/java/com/uscreen/VideoReceiver.kt::start`, `stop`, `connectAndReceive`): cancellation is not joined and the old coroutine's `finally` closes/nulls shared `socket` and `inputStream`. Repro interleaving: stop while old read exits, start a replacement, then resume old cleanup; replacement connection is closed. Regression: latch-controlled stop/start with late old cleanup must leave the new socket/decoder alive. |
| T089 | open | medium | medium | Release partially initialized decoders (`android/app/src/main/java/com/uscreen/VideoReceiver.kt::setupCodec`): exceptions from configure/start leave local `codec` unreleased; failures after callback-thread creation also retain that thread before retries overwrite it. Regression: inject failure at configure, listener registration, and start; every created codec/thread is released once before retry. |
| T090 | open | medium | medium | Make Android settings scroll at constrained heights (`android/app/src/main/java/com/uscreen/MainActivity.kt::SettingsSheet`): the landscape bottom sheet contains a long, non-scrollable Column, placing lower controls and Apply beyond the viewport on smaller tablets/large fonts. Regression: Compose layout test at a short landscape viewport and enlarged font scale can scroll to and activate Apply. |
| T091 | open | medium | medium | Make capture setup/retries cancellation-aware (`host/src/capture.rs::stream_frames`, `start_helper`, `wait_stream_size`): helper handshake and retry sleeps bypass shutdown/display changes; backoff grows to 30 s while daemon teardown allows only 5 s. Missing-helper probe ignored shutdown during backoff. Regression: stop/detach during handshake, mode wait, and maximum retry delay promptly reaps children and disconnects the output. |
| T095 | open | medium | small | Rebuild source installs based on all build inputs (`scripts/install.sh::build_if_needed`): existence of only `target/release/uscreen` skips building newer sources and missing GUI/helper. Isolated probe with a lone stale daemon returned success without invoking make. Regression: source updates and missing companion binaries cause a rebuild before installation; release tarballs still use their shipped binaries. |
| T096 | open | medium | small | Resolve the installed GUI explicitly in `make install` (`Makefile::install`, `scripts/uscreen.desktop`): unlike install.sh, Makefile copies bare `Exec=uscreen-gui`. A desktop PATH lacking ~/.local/bin launches an older /usr/bin GUI or nothing. Regression: install into a temporary prefix with a stale PATH binary and verify the launcher selects the newly installed sibling. |
| T097 | open | medium | small | Create setup parent directories in every entry point (`gui/src/main.rs::system_setup_script`, `Makefile::setup-system`): both write modprobe.d/modules-load.d files before ensuring those directories exist; install.sh handles this already. Repro: empty fake /etc causes setup to fail before installing uinput permissions. Regression: execute GUI and Makefile setup against a minimal temporary filesystem with mocked privileged commands. |
| T098 | open | medium | small | Do not certify HEVC support solely from host configuration (`host/src/doctor.rs::check_tablet`): `(false, false, true)` still prints HEVC OK, even when the tablet report contains no HEVC decoder. Repro: select hevc_nvenc with empty/failed codec output. Regression: mocked hardware/software/absent/unknown codec reports produce evidence-based results and no unsupported Main10 recommendation. |
| T099 | open | medium | medium | Diagnose the daemon's actual tablet/session (`host/src/doctor.rs::check_tablet`): doctor picks the first USB entry, while daemon can retain another device or prefer the one with com.uscreen; forwarding checks always use primary ports. Repro: charging phone precedes active tablet, or diagnose a second slot. Regression: multiple devices/transports and custom slot ports target the actual active session. |
| T100 | open | high | small | Bind published assets to the release commit (`scripts/publish-release.sh`): local tag existence is checked, but neither HEAD equality nor the remote tag is verified; a clean newer checkout can publish binaries under an older tag. Regression: an isolated repository with divergent HEAD/local/remote tags must fail before builds/network, and matching immutable release refs must pass. |
| T101 | open | medium | medium | Reject failed/stale package builds (`packaging/build-packages.sh`): unlike build-release.sh, no container-success sentinel exists; old dist deb/rpm files survive, and the inner shell also lacks pipefail around rpmbuild. Repro: failed container/package build with preexisting assets can reach final listing and downstream existence checks. Regression: mocked container/rpmbuild failures cannot reuse prior artifacts or report success. |
| T102 | open | medium | medium | Make local distribution artifacts complete (`Makefile::dist-local`): Android build/copy failures are swallowed with `\|\| true`, and the helper is copied without bundled libevdi or an origin runpath. Repro: failed assembleRelease still produces a successful tarball; a target without system libevdi cannot load its helper. Regression: fail missing APK and smoke-test helper loading from the isolated artifact tree. |
| T103 | open | low | small | Handle partial TCP I/O in fake-tablet (`scripts/fake-tablet.py::ws_connect`, `ws_send`, `ws_recv_text`): send/recv are assumed to transfer complete headers; upgrade over-read bytes are discarded. Repro: socket stub splits the 2-byte WS header or short-writes auth. Regression: fragmented upgrade/frame headers and short writes preserve complete packets without hangs or false failures. |

## Blocked / Deferred

| id | status | severity | effort | short description |
|---|---|---|---|---|
| T108 | blocked | medium | medium | Resolve free-card ownership contradiction: CaptureConfig/helper comments and docs/troubleshooting.md say helpers select a free/own card, but C discovery returns the lowest card without checking availability; extra sessions proceed with `card=None` when slots exceed available cards. Unblock with explicit free-card reservation, correct newly-added-card discovery, and rejection/deferment of unassigned slots. Regression: busy lowest card and insufficient-card cases never attach two helpers to one card. |
| T112 | blocked | medium | medium | Resolve stale-callback/lifecycle contradiction: TouchCapture.connect documents that replaced sockets cannot change state, yet onOpen/onMessage lack the stale-socket checks used by onClosed/onFailure; MainActivity callbacks can restart video after onStop. Robolectric probes on API 27/34 resurrected disconnected state and invoked mode callbacks. Unblock with generation checks for all callbacks plus foreground gating. Regression: replaced/background callbacks cannot reconnect, send config, or start video. |
| T113 | blocked | high | medium | Resolve bounded-FIFO-write contradiction: `evdi_helper.c::write_fifo_frame` and on_mode_changed promise bounded stalls, but try_open_fifo clears O_NONBLOCK and write can block after poll reports some space. A 2 MiB write remained blocked beyond its 1 s deadline in a pipe probe. Unblock with nonblocking writes, progress deadlines, and cancellation checks. Regression: a reader that stays open but stops draining cannot hang mode changes or shutdown. |
| T114 | blocked | medium | small | Resolve EDID physical-size contradiction: `scripts/gen-edid.py` claims Linux-compatible DTD encoding, but it and `host/src/edid.rs::make_edid_sized` swap height-low and shared-high bytes relative to [Linux's detailed_pixel_timing layout](https://github.com/torvalds/linux/blob/master/include/drm/drm_edid.h). A 310×194 mm input decodes as 3126×528 mm. Unblock by correcting both generators and invalidating cached EDIDs. Regression: independently decode physical dimensions, not only checksum/active resolution. |
| T115 | blocked | medium | medium | Resolve EDID-generator/range contradictions: Python accepts width 4096 then encodes 0, retains older chromaticity, and advertises only 55–65 Hz for a requested 90 Hz; Rust rejects overflow but permits 10 Hz while advertising minimum 23 Hz. Sources: both EDID generators and common MIN_FPS. Unblock with shared representability/range rules and equivalent output. Regression: boundary dimensions, color coordinates, and supported timing ranges agree in both generators. |
| T116 | blocked | medium | medium | Resolve keyframe-timing contradiction: docs/architecture.md advertises one keyframe per second plus requests on join, while default CLI capture cannot honor requests and counts `-g=fps` against a 5 fps idle floor (`evdi_helper.c::IDLE_KEEPALIVE_MS`). Idle joins can wait 12–18 s at 60–90 fps targets. Unblock with bounded wall-clock join recovery and accurate default/optional-path docs. Regression: join/recover on an unchanged screen receives a decodable frame within the promised bound. |
| T117 | blocked | high | medium | Resolve complete-release contradiction: publish-release.sh/Makefile promise a complete release, but create it public before uploading and accept HTTP-error JSON as upload success. Offline probe returned success after all six mocked uploads failed. Unblock by staging a draft, failing on HTTP/API errors, verifying uploaded assets/checksums, then publishing. Regression: fail any individual upload and keep release unpublished; only verified complete assets permit publication. |
| T118 | blocked | medium | small | Resolve credential-handling contradiction: publish-release.sh says GH_TOKEN is never passed on a command line, but curl receives it in `-H Authorization`. Offline dummy-token probe confirmed argv exposure. Unblock by supplying authenticated headers through stdin or a protected descriptor/config file. Regression: dummy credentials are absent from captured argv and logs while authenticated upload still works. |
| T119 | blocked | medium | small | Resolve in-process bitrate-parity contradiction: `encoder.rs::low_latency_options` claims CLI parity, but its libx264 branch omits VBV bufsize while setting max_bit_rate; capture.rs supplies both. Equivalent FFmpeg probe reports `VBV maxrate specified, but no bufsize, ignored`. Unblock by configuring the same effective rate cap or documenting/removing unsupported limits. Regression: optional libx264 encoder honors a configured ceiling under complex input. |
| T120 | blocked | low | small | Resolve decoder-rate contradiction: VideoReceiver setup says it follows the real stream FPS, but MainActivity sets streamFps only from initial local preferences; Apply and host greetings never update it. Unblock by propagating effective host FPS and rebuilding/updating decoder hints when needed. Regression: host-selected FPS and tablet Apply update hints across codec restarts instead of keeping the startup default. |
| T121 | blocked | low | small | Resolve live-file-settings contradiction: `main.rs` says editing config.toml live changes encoder settings, but no file watcher updates settings_tx; docs/architecture.md likewise implies file-based communication without its restart requirement. Unblock by implementing validated reload or documenting the required restart consistently. Regression if reload is implemented: edits update the active pipeline without overwriting CLI-only overrides. |
| T122 | blocked | low | small | Resolve explicit-helper precedence contradiction: `main.rs::find_helper` says an explicitly supplied --helper always wins, but a nonexistent explicit path silently falls back to an installed helper. Unblock by distinguishing an omitted default from an explicit override and reporting invalid explicit paths. Regression: missing explicit helper fails; omitted helper still discovers the appropriate install. |
| T123 | blocked | low | small | Resolve malformed-version contradiction: `host/src/update.rs::parse` promises malformed tags never look newer, but `999.bad` becomes 999.0.0; GUI and Android use similarly permissive parsing. Unblock with consistent validated version parsing. Regression: malformed/truncated/overflow and supported prerelease tags never produce false update banners. |
| T124 | blocked | low | small | Resolve X11 documentation contradiction: README FAQ/roadmap, docs/faq.md, docs/index.html, and docs/llms.txt describe manual X11 mapping; current input.rs and docs/compatibility.md/troubleshooting.md implement/document automatic xinput mapping. CHANGELOG Unreleased also names removed scripts/map-input-x11.sh. Unblock by updating current user-facing descriptions and prerequisites to match the daemon. Documentation-only; no artificial regression test. |
| T125 | blocked | low | small | Resolve transport/privacy documentation contradiction: README, docs/index.html FAQ, and docs/llms.txt claim screen/input never leave the cable, while Wi-Fi support and SECURITY.md explicitly allow local-network transport; docs/architecture.md also says no network traffic except updates. Unblock by consistently distinguishing USB-only use, optional ADB network transport, and cloud/update traffic. Documentation-only. |
| T126 | blocked | low | small | Resolve latency-scope contradiction: README/docs/index.html/docs/llms.txt call 18–22 ms end-to-end, while docs/benchmarks.md excludes capture; latency.rs says helper+encoder-output metrics cover the whole pipeline although encoding, packetizer buffering, and reverse acknowledgement time are not separated. Unblock by naming measured boundaries consistently and removing unsupported total-latency claims, or measuring the missing stages. Regression for instrumentation changes: controlled stage delays appear in the correct metric. |
| T127 | blocked | low | small | Resolve installation-start contradiction: docs/index.html says tarball installer performs `enable --now`, while install.sh deliberately only enables and README/docs/installation.md require a first manual start. Unblock by aligning website instructions with actual first-run behavior. Documentation-only. |
| T128 | blocked | low | small | Resolve source-build/release instructions: docs/development.md omits the libevdi link dependency required by Makefile and says bump both Cargo manifests although publish-release.sh checks three. Unblock with complete source prerequisites and version-file list. Documentation-only; validate instructions against a clean source-build environment. |
| T129 | blocked | medium | small | Include referenced license notices in distributed artifacts (`scripts/build-release.sh`, `Makefile::dist-local`, package recipes): bundled README links LICENSE and THIRD_PARTY_LICENSES.md, but tarball copies neither; deb/rpm also omit these notices despite bundling libevdi. Unblock by defining and shipping the license/notice file set consistently. Regression: inspect tar/deb/rpm contents for the declared notices and resolve README's local links. |
