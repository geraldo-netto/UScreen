# TODO

| id | status | severity | effort | description |
|---|---|---|---|---|
| T142 | blocked | high | medium | Resolve inconsistent tablet selection in `host/src/main.rs`: `pick_device_with` checks app presence only with multiple USB devices; extra slots accept every remaining ADB device; `setup_wifi` picks the first USB serial. A charging phone can take a display slot or receive the Wi-Fi command while the actual tablet uses another transport. Unblock by sharing app-aware selection across primary, extra, and Wi-Fi setup while retaining physical-device identity and explicit fake serials. Add permanent mocked-ADB regressions for phone plus Wi-Fi tablet, multiple USB devices, and extra-slot exclusion. |
| T143 | open | medium | medium | Recover a crashed app on extra tablets. `adb_monitor` checks `app_running` only for the primary every fifth poll; extra sessions only relaunch upon an input authentication notification, which a dead app cannot send. Add the same bounded, per-device recovery for every assigned tablet, respecting `auto_launch_app`; add a permanent mocked-ADB regression proving only dead extra apps relaunch, failures back off, and disabled auto-launch stays inactive. |
| T144 | open | high | medium | Retry failed ADB port forwarding before marking a tablet session ready. `on_tablet_connected` logs reverse failures and returns unit, but `adb_monitor` still records the serial and signals attachment, so subsequent polls skip setup indefinitely. Add permanent regressions for a failed first reverse followed by success on unchanged serial, covering primary and extra sessions; keep readiness false until both mappings succeed and bound retries. |
| T141 | blocked | medium | small | Resolve installer desktop-path inconsistency: `make install` quotes the GUI executable in its desktop entry, but `scripts/install.sh::install_files` emits an unquoted Exec path. Installing under a home directory containing spaces creates a broken app-menu launcher. Unblock by correctly encoding the executable path in both entry points; add a permanent sandboxed regression that launches the generated entry from a path with spaces and reserved desktop-entry characters. |
| T135 | blocked | medium | medium | Resolve GUI tablet-status contradiction: `gui/src/main.rs::poll_status` claims to select like the daemon but selects the first ready USB device, while the daemon retains actual app sessions and doctor reads their runtime ledger. A charging phone can appear as the streaming tablet. Unblock by deriving GUI active tablet status from validated daemon sessions. Add permanent regressions for an unrelated first USB device, an active Wi-Fi tablet, multiple sessions, and stale session metadata. |
