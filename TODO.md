# TODO

## Open items

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| T566 | open | low | small | Configuration diagnostics in `common/src/storage.rs::write_at` emit `Config written` before temporary-file write, sync and rename succeed. A failed filesystem commit can therefore claim a change that was never persisted. Discovered while fixing T559; retain for a separate review/fix as requested. Add a permanent failing persistence-error/logging regression, then emit successful-write diagnostics only after the atomic replacement succeeds. |
| T567 | open | low | small | Normalize pre-existing Rust formatting in `host/src/monitor/forwarding.rs`: `rustfmt --check --edition 2021 host/src/monitor/forwarding.rs` reports formatting differences in the T557 implementation and tests. The item-1 formatter exposed this unrelated drift; retain it for the requested later review. Apply formatting only and verify the normal formatter check; no artificial behavioral regression is required. |
| T568 | open | medium | small | Review pending-frame accounting in `host/src/latency.rs::EncoderEvidence::acknowledge`: `encoded_at_ack` receives the latest total encoded count rather than the ordinal of the frame acknowledged. An older ACK can therefore make `unrendered_output()` report zero while newer frames remain pending, potentially hiding a subsequent output/render stall from `selection::health`. Discovered during T492 design; retain for later review as requested. Add an ordered-output/delayed-ACK regression with multiple pending frames, verify watchdog behavior after output stops, and tie progress to the acknowledged frame if confirmed. |
| T492 | open | medium | medium | Finish native acceptance of the opt-in host adaptive-idle implementation ([design and test boundaries](docs/reviews/2026-09-21-reliability-batch.md#item-2--adaptive-idle-capture)). Portable policy and Linux CLI helper control now measure current named-decoder ACKs/timestamps, try 2 idle updates/s, preserve motion FPS, coordinate 0.9-second forced-IDR deadlines and fall back to the existing 5 idle updates/s. Synthetic timing and native helper regressions cannot certify physical Android watchdog/reconnect behavior, total raw-write-to-ACK latency or battery gains. Next: use an agreed normal-startup window, avoiding T222 EVDI reattachment, to validate complete capture/encode/USB/presentation, late joins, long pauses, slow clients, reconnect and sustained battery behavior for available named selections. Explicit encoders without named-decoder evidence and the optional in-process encoder retain compatibility cadence; add an independently validated adapter before enabling sparse in-process capture. Historical local sparse High-profile latency rose from ~224 to 1027 ms while Baseline stayed ~23–26 ms; do not substitute render progress, codec-name allowlists or the historical 60% isolated raw-write reduction for current full-pipeline evidence. |
| T561 | open | low | medium | Investigate the six video buffers queued but never latched among 1,727 interior frames in the T560 current-playback trace at 1280×800/30 FPS. [Evidence](docs/benchmarks/2026-09-20-live-playback/README.md) retains exact SurfaceFlinger frame numbers/times; all other interior frames had present fences. Correlate these events with capture/encode packet timing, codec release and compositor deadlines before assigning a cause or changing frame policy. Render-thread runnable waits were sub-millisecond at p95 and no application GC event was observed; these do not explain every omission. Preserve stream order and bounded ownership, and add a permanent failing regression before any demonstrated software fix. No deliberate EVDI reattachment or Android lock needed for the initial trace analysis. |
| T558 | open | low | medium | Mitigate libevdi 1.15.0 startup scanning after the completed [T558 investigation](docs/benchmarks/2026-09-20-evdi-startup/README.md). Read-only replay of `wait_for_master`, excluding the live helper descriptor, timed out after 11.741 s with 6.736 s CPU: up to 51 full `/proc` scans plus 50×100 ms sleeps, while root Xorg fd/maps are unreadable. This accounts for the earlier roughly 12-second pre-connection delay; no exact original function trace was captured. Implement a supported upstream fix or bounded Linux adapter only with permanent failing timeout/permission/ownership regressions and native validation of master acquisition/release. Do not bypass DRM ownership or validate by deliberately detaching the active display; stock dependency remains unchanged. |
| T414 | open | medium | large | Investigate recurring YouTube audio gaps separately from the earlier A/V offset report. The maintainer confirms Linux Chrome playback, video shown on the tablet and Bluetooth headphones paired to Linux; playback was smooth during the passive 2026-09-20 sample. PipeWire routes Chrome to BW01 A2DP/SBC and the short pw-top sample shows ERR=0; this does not establish the cause of intermittent gaps. The [retained passive sample](docs/benchmarks/2026-09-20-youtube-audio/README.md) includes process hashes, CPU, GPU and encoded transport observations. Correlate a future occurrence with PipeWire/Bluetooth errors, Chrome playback state and host scheduling without changing the live display or Android lock state. Preserve versions/settings and measurement boundaries; the UScreen protocol remains video-only, and ACK sequence identities are not an audio clock. If offset/drift also recurs, retain T386's requirement for monotonic presentation timing and measure a timestamped flash/click clip across initial/sustained playback, pause/seek, reconnect and recovery; manual audio delay or new audio transport remains separate scope. A demonstrated software defect needs a permanent failing regression before its fix, and synchronization claims need physical offset/drift evidence. |
| T553 | open | low | medium | Assess whether simultaneous display and camera traffic benefits from per-resource USB/network routing before implementing automatic balancing. Read-only checks on 2026-09-20 still show a 480 Mb/s USB link; T491 measured 238.89–244.34 Mb/s host-to-tablet payload and a historical 7.1 Mb/s display sample, while current camera preferences target 3 Mb/s in the opposite direction. These are not a combined-load measurement. Begin with code review and passive throughput, queue/latency and codec-load observations; distinguish link congestion from encode/decode work. `host/src/camera/bridge.rs` selects one ADB serial independently and display selection in `host/src/linux_main.rs` prefers USB, but neither implements link bonding. Only propose a portable, explicit per-resource route policy if measured benefit warrants it; any later split-route trial needs an identified paired network route and must preserve physical identity, permissions, mapping ownership and reconnect behavior. Do not reset ADB, lock Android, detach EVDI or reopen T382/T487's deferred broad campaigns for this narrow assessment. |
| T549 | open | medium | small | Identify which host/tablet lifecycle actions lock Android and require a fresh manual unlock, as reported by the maintainer during T543 validation. Begin with existing logs and code paths; do not deliberately repeat lock/sleep actions in the live session. Record the exact trigger and expected unlock/foreground behavior before any fix, with permanent regression coverage if a behavioral bug is confirmed. Further physical lock-screen validation requires an agreed window for manual unlock. |
| T523 | open | medium | large | Extract the remaining shared session, authentication, control and video-protocol code from Linux-only host modules so both platform entry points can reuse it. Extend the existing `host/src/input/backend.rs` InputBackend/InputSink boundary rather than duplicating it; isolate capture/display ownership and native socket/process operations in focused adapters. Preserve Android wire compatibility, Linux behavior and all regressions. This preparation can start without a tablet or a selected virtual-display driver; keep Windows runtime capabilities disabled pending T493 and their backend acceptance checks. See [Windows architecture](docs/windows-port.md#architecture-and-reuse). |
| T521 | open | medium | medium | Research the Windows virtual-display driver choice: compare integration of an existing [Virtual Display Driver](https://github.com/VirtualDrivers/Virtual-Display-Driver) with an owned IDD. Identify a candidate version, licensing/distribution and signing requirements, monitor-control interface, per-application ownership, capture compatibility and recovery behavior. Present the implications for separate driver installation versus maintaining our own driver. Research can start now; an existing-driver recommendation is not an approved selection, and T527 must wait for the decision and interface validation. |
| T533 | open | medium | medium | Extend the existing Windows preview diagnostics in `host/src/windows_main.rs` and GUI platform status to report executable discovery, dependency versions and the implemented backend capabilities as they become available. Distinguish missing, unsupported and unverified states; never infer a connected tablet or working stream from saved preferences or ADB/FFmpeg discovery alone. Add native-command failure, malformed-output and unavailable-backend regressions. Basic diagnostics can proceed now; wire driver/capture diagnostics after T527/T528 without prematurely enabling those capabilities. |
| T418 | open | medium | large | Integrate an experimental shared-memory raw-frame transport using T389’s memfd-ring measurements and [ownership design](docs/benchmarks/2026-09-18-raw-input.md#backend-design-and-limits). Convert directly into bounded shared slots and wrap read-only slot leases in stock libavcodec AVBufferRef/AVFrame ownership so encoder-retained references delay reuse; sending mapped pixels through another FIFO would retain the main copy boundary. Implement explicit descriptor handoff, validated version/format/dimensions/strides/size/sequence/generation, shrink/grow seals, acquire/release state transitions, cancellation and peer-crash/resize recovery. Preserve the stock-FFmpeg CLI/FIFO fallback and optional VAAPI rejection. Require permanent retained-frame, malformed-descriptor, stale-generation, full-ring, partial-startup, crash and mode-change tests before enabling, followed by actual conversion/encode/display latency on the existing tablet at 60 FPS; isolated host concurrency measurements may supplement this without requiring a multi-tablet or large-machine campaign. The synthetic prototype uses memset and sampled-byte verification, not capture, full encoder reads, USB or physical display; its throughput gains are not end-to-end gains. This supplies the uncompressed local-transport comparison for T419, whose independent compression research can start in parallel. |
| T537 | open | low | small | Investigate the historical tray shutdown panic in the `uscreen.service` journal at 2026-09-20 10:01:58 CEST: ksni 0.3.6 `src/service.rs:134` reported “all futures in select! were completed” while the daemon was stopping; the daemon then logged a clean stop. `Cargo.lock` still selects ksni 0.3.6, and `host/src/linux_main.rs` aborts the task running `host/src/tray.rs::run` during shutdown. Reproduce teardown under an isolated D-Bus watcher without attaching EVDI, establish whether the current AppImage is affected, and add a permanent failing lifecycle regression before any fix. The current AppImage registered an active tray icon after starting its previously inactive service; that missing icon was not evidence of this historical shutdown problem. |

## Other items

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| T565 | blocked | low | small | Verify a live 30 FPS Android display session after the installed T563 FFmpeg 6.1.6 AppImage is activated by a normal startup. Packaging, clean-container GUI/lifetime, native H.264/HEVC VAAPI, framing, idle and camera regressions passed; the current extracted session deliberately remains on 5.1.9. Blocked on a normal display-free startup window or resolution of T222: killing the live encoder terminates its EVDI helper, so it is not a safe hot-upgrade mechanism. After startup, record the executable/version, selected encoder/decoder, render progress, latency and reconnect behavior without changing the user's 30 FPS preference. See [T563 validation](docs/benchmarks/2026-09-21-ffmpeg6/README.md). |
| T564 | blocked | low | small | Validate the bundled FFmpeg 6.1.6 NVENC and VP9/AV1 VAAPI backends on compatible hardware before claiming native support for those paths. The current host cannot load libcuda.so.1 and offers no VP9/AV1 VAAPI encoding entrypoint; compile-time encoder inventory is verified, but is not runtime evidence. Blocked on a compatible GPU/driver test environment. Run bounded encode/decode and existing framing/recovery regressions with the bundled executable when that environment is available, retaining version/profile and failure/fallback results. This is missing platform coverage for T563, not a request to reopen T382's broad hardware campaign. See [native results](docs/benchmarks/2026-09-21-ffmpeg6/README.md). |
| T540 | blocked | low | small | Validate physical USB unplug/replug while sharing a tablet camera. Desktop-sidecar disconnect/restart and browser capture are verified, but physical USB removal would detach the active EVDI display through unresolved T222. Unblock after T222 is resolved or a display-free USB test window is available; require camera release, black inactive output and explicit selection after reconnect. See [camera validation](docs/reviews/2026-09-20-camera-implementation.md). |
| T538 | blocked | medium | small | Verify an identified hub/charger arrangement that preserves USB video and supplies adequate sustained power to the RugKing Pad 2 Pro. The tablet disappeared from Linux USB/ADB after the tablet-side hub was added at 17:40:09 CEST on 2026-09-20; USB video recovered at 17:44:34 without an application fix. A hub-role conflict remains unconfirmed. Subsequent user changes affected only the charging cable; exact hub/charger models and port wiring remain missing. [Retained observations](docs/benchmarks/2026-09-20-usb-hub-power/README.md): battery rose from 97% to 100% between uncontrolled checks; final-30-second/next-64-second samples averaged -3.93/-13.71 mA net battery current, with about 30 FPS video and unchanged Android-reported 5 V/500 mA maxima. These are near-neutral short observations, not proof of increased input power or sustained charging. Blocked on identifying the hardware/wiring; then verify a stable workload below full charge before claiming the drain resolved. Ulefone advertises 18 W USB-C/pogo-dock charging, but concurrent dock charging/USB data is unverified. Keep T487’s broader comparison deferred; any demonstrated software defect requires a permanent failing regression before a fix. |
| T493 | blocked | medium | large | Complete native Windows validation of the shared platform-service foundation before enabling Windows lifecycle/capture. Configuration paths, executable discovery, owned-handle identity and kill-on-close child jobs are implemented; permanent regressions reproduced relative config paths and escaped descendants before their fixes and now pass in isolated Wine. Native private runtime-directory ACL and single-instance tests remain mandatory: Wine 9 loses the protected-DACL flag and rejects the new directory, so it cannot validate the positive ACL/lease cases. Keep those tests intact in the normal Windows suite and keep capabilities disabled. Unblock by running the existing native MSVC job in `.github/workflows/windows.yml` or the equivalent normal suite on a native Windows runner/VM; retain the tested revision and logs, validate owner-only ACLs, reparse rejection, single-instance ownership, token privacy and process retirement, fix any demonstrated failure, and satisfy the requested per-function coverage/fuzz gate. Linux behavior and tests must remain intact. See [service validation](docs/reviews/2026-09-19-windows-services.md); T494/T495 may use the separately validated path/discovery services but must report unavailable backends accurately. |
| T497 | blocked | medium | large | Complete the per-function coverage/fuzz requirement on native Windows after T493’s runtime/ACL prerequisite is available. The Linux/Android/C/essential-script gates and retained out-of-scope tests are implemented; the full Rust report still exposes 58 unmeasured Windows-only functions, and shared functions also need Windows-native evidence. Run the normal Windows suite with native counters, add meaningful invalid-input/bounds and lifecycle cases where needed, enforce at least 80% executable-line coverage per function/method and retain all existing regressions. Cross-compilation and Wine do not satisfy this gate. See [coverage scope and evidence](docs/reviews/2026-09-19-function-coverage.md) and [repeatable commands](scripts/coverage/README.md). |
| T520 | blocked | low | small | Confirm the first Windows release target and encode that decision in the build/support policy. Windows 11 x64 is the recommendation in [the Windows plan](docs/windows-port.md#decisions-and-resources-needed), not an accepted requirement. Blocked on the maintainer choosing the initial OS/architecture and whether Windows 10 or ARM64 belongs in that first scope; portable preparation and existing Windows CI need not wait. |
| T522 | blocked | medium | medium | Arrange and execute functional acceptance on one Windows PC with the existing Android tablet. Record the Windows build, architecture, GPU/driver, USB connection and a test window; arrange administrator access only for the selected driver installation. Validate the implemented milestones incrementally: connection, pen input, extended display, mode changes, lock/unlock, sleep/resume, disconnect/reconnect and ownership-safe cleanup. Blocked on an available Windows machine/tester and tablet window; a native CI runner or VM can cover isolated contracts but does not establish this physical USB/GPU/display result. No additional tablet or large-machine campaign is required. |
| T524 | blocked | medium | large | Implement Windows daemon lifecycle and GUI start/stop/restart/status using the shared services, private runtime state, identity checks and owned child jobs. Run in the interactive user session with graceful shutdown and deterministic token/session cleanup; preserve explicit unsupported results for unfinished display/input backends. Blocked on T493 native ACL/lease validation, T520 target selection and the required T523 shared interfaces. Require permanent concurrent-start, stale-owner, cancellation, crash, Unicode-path and child-retirement regressions before enabling lifecycle capabilities. |
| T525 | blocked | medium | large | Implement Windows tablet discovery and authenticated ADB USB connection using the existing Android app and protocol. Port executable invocation, port forwarding, attachment/token delivery, reconnect and disconnect cleanup without relying on Unix shell behavior. Blocked on T523 shared transport interfaces and T524 lifecycle/private-state readiness. Add permanent failed-command, unauthorized/offline-device, malformed-inventory, token-rotation, reconnect and partial-startup regressions; complete the physical connection check under T522. |
| T526 | blocked | medium | large | Implement the Windows pen-only input backend for pen, touch and mouse through the existing InputBackend/InputSink contract. Preserve pressure, tilt, hover, eraser, buttons, contact ownership and enabled-device settings; map the selected monitor across negative desktop coordinates, rotation and mixed DPI. Release all contacts on ownership change, disconnect and shutdown, and report session/elevation limitations accurately. Blocked on T520 target selection and the T523/T524 backend/lifecycle boundaries; end-to-end acceptance also needs T525 and T522. This milestone does not require a virtual-display driver. Add bounds, mapping, privilege-failure and held-contact cleanup regressions. |
| T527 | blocked | medium | large | Implement the Windows virtual-monitor adapter after the T521 driver research and maintainer driver choice. Detect compatible installed versions, create or claim only UScreen-owned outputs, configure resolution/refresh/placement and retire only owned resources. Blocked on the driver decision and validated control/ownership interface, plus T524 lifecycle. If an owned driver is chosen, agree its maintenance, signing and distribution responsibilities before implementation. Add missing/incompatible-driver, ownership collision, attach/detach, resize and partial-startup cleanup regressions; verify actual extended-display behavior under T522. |
| T528 | blocked | medium | large | Implement the Windows frame-capture adapter, evaluating Desktop Duplication against the selected virtual driver before committing to that capture route. Preserve frame geometry, strides, cursor/rotation handling and resource ownership; handle access loss, desktop locking, resize, display removal and cancellation with bounded recovery. Blocked on T520, the T523 capture interface and T521/T527 driver-output compatibility. Add permanent malformed-frame, mode-change, access-loss and resource-retirement tests; native virtual-display capture acceptance belongs to T522. Direct access to driver swap-chain surfaces requires a supported interface and must not be assumed. |
| T529 | blocked | medium | large | Implement Windows raw-frame transfer and encoding with unmodified FFmpeg, preserving a working software-encoder fallback and existing Android framing/codec negotiation. Replace the Unix FIFO dependency with an explicit Windows transfer adapter; define frame boundaries, bounded buffering, cancellation, partial-write and encoder-child ownership before wiring capture. Blocked on T520 and the T523 frame/session contracts; complete stream integration after T525/T528. Add permanent partial-frame, slow-reader, encoder-exit, startup-failure, resize and shutdown regressions. In-process encoding is optional and must not inherit the current Unix-only FIFO assumption. |
| T530 | blocked | medium | large | Implement Windows encoder capability discovery and measured selection for available NVENC, AMF, QSV and CPU candidates, reusing tablet decoder compatibility and existing selection policy. Distinguish an advertised encoder from successful initialization and measured latency; preserve software fallback and explicit user choices. Blocked on the T529 encoder path and a T522 GPU test environment for enabling hardware candidates. Test unavailable devices, incompatible profiles, probe failures, stale selections and fallback. Evaluate GPU-texture transfer as a separate measured optimization; do not claim zero-copy or performance gains from compilation or advertised support. |
| T531 | blocked | low | medium | Implement Windows tray integration with accurate connection/status and lifecycle actions, using the shared state rather than polling a second independent model. Blocked on T524 lifecycle and status events from T525. Add permanent action-dispatch, stale-state, shutdown and tray-resource cleanup tests; verify ordinary-user behavior in the native interactive session. |
| T532 | blocked | low | medium | Implement optional per-user Windows autostart and connect it to the GUI preference. Keep preference/state reporting consistent, avoid duplicate launchers and preserve Unicode/spaced executable paths; ordinary application startup must not require driver-installation elevation. Blocked on T520 target selection and T524 lifecycle/single-instance readiness. Add enable/disable, stale-path, permission-failure and repeated-login regressions; ensure T534 upgrades/uninstall update or remove only UScreen-owned registration. |
| T534 | blocked | medium | large | Package the functioning Windows application with reproducible builds, dependency/version checks, distribution notices and install/upgrade/uninstall behavior that preserves settings and unrelated monitors/drivers. Blocked on a maintainer choice of installer format and whether ADB/FFmpeg are bundled or prerequisites; release also requires the accepted Windows milestones and T522 functional evidence. Keep any driver installation separate from ordinary per-user operation, confirm the selected application/driver signing route before distribution, and document tested versions and limitations. Add automated package-content, missing-dependency, upgrade-preservation and uninstall-ownership regressions. |
| T535 | blocked | medium | medium | Benchmark the implemented Windows stream on the T522 host/tablet setup before making performance claims or changing automatic preferences. Measure latency boundaries, frame pacing, host CPU/GPU/memory, tablet resource use and battery behavior with controlled settings and repeatable UI/video workloads; retain versions, raw observations and recovery results. Blocked on T522 hardware availability and working T525/T528/T529 streaming; compare T530 hardware candidates only when present and validated. Reuse the existing Android app and benchmark methodology, distinguish isolated measurements from end-to-end results, and require no multi-tablet campaign. |
| T222 | blocked | high | large | Analyze the existing Xorg crash core with matching debug symbols before considering an isolated reproducer; do not deliberately reproduce the desktop crash. The 2026-09-17 01:52:40 CEST fault at `0x20` through `ProcRRSetCrtcGamma` followed EVDI attachment and preceded the helper restart. Blocked on readable access to the PID 2392 core and matching Xorg debug information (build ID `2dc631fda11e081777c5d43ba3ad1d6ce8a5a6fd`): `coredumpctl` still reports inaccessible, noninteractive privileged access requires a password, and the prior debuginfod lookup returned 404. Use the evidence to identify the failing state and validate a mitigation. Required permanent fix coverage: isolated Xorg/EVDI attach, gamma update and detach without server/session loss; fake-helper tests cannot exercise the system display server. See [incident evidence](docs/reviews/2026-09-17-cinnamon-restart.md). |
| T388 | blocked | medium | medium | Complete the USB battery comparison before the separate network/pen/power-source experiments in T487. The [low-latency USB attempt](docs/benchmarks/2026-09-18-usb-power-comparison.md) completed one five-minute normal/static phase, then rejected saver/static after Linux keyboard-focus loss; remaining pairs and the sustained streaming-off control did not run. Preferences were restored. T488 fixed the Android foreground-query collector bug with a permanent regression; the remaining prerequisite is an undisturbed Linux host session or an explanation of unexpected focus changes, not merely an idle tablet. Use the deployed signed fork APK (`io.github.geraldo_netto.uscreen`), not the earlier debug package: current baseline is H.264 VAAPI Constrained Baseline/QP 18, 1280×800/60 FPS, 50% app brightness/60 Hz, with normal USB CPU/Wi-Fi locks held and Battery saver disabled. Compare against Battery saver enabled while keeping APK/host hashes and these settings fixed; record newer undeployed checkout changes separately. The accepted counter stayed flat over 270 seconds, but coarse 9.99 mAh steps and a missing saver pair establish no power saving. Retry the balanced matrix and control in a 60–90 minute window, extending only if useful and the devices remain available; preserve raw observations, clock boundaries and preferences. Retain USB waiting/background/reconnect observations and conservative unknown-route behavior, without reattaching EVDI through unresolved T222. See the [power-policy report](docs/benchmarks/2026-09-18-power-policy.md) for permanent coverage. |
| T419 | blocked | medium | large | Live lossless RGB integration requires either a repeatable tablet battery advantage over matched reduced-cadence hardware H.264, or a maintainer decision to pursue RGB fidelity/CPU independently of battery. The [presentation and sustained-power follow-up](docs/benchmarks/2026-09-19-presentation-power.md) preserves the completed research and conditional implementation order: RGB pen app CPU fell about 33%, but its battery advantage did not repeat; static one-update/s H.264 charged more strongly than RGB in both rounds. Retain the unresolved bounded wire-format/base/sequence/generation contract, integrity and decompression validation, atomic updates, reconnect/full refresh, renderer retirement and video fallback requirements. Require permanent round-trip, corruption, stale-base, overload and lifecycle regressions before production enablement, then compare actual capture/compression/USB/render latency and sustained CPU, memory, thermal and battery behavior across UI, natural video, transitions, resize and background/resume. Keep callback, compositor and optical boundaries distinct; queued-but-unlatched frames explain the reproduced presentation omissions, not every earlier untraced miss. Preserve RGB/chroma fidelity, account for full refresh and photo/scroll bandwidth, retain bounded reads over whole-fixture mmap, and compare host-only compression separately with T389/T418 shared memory. No FFmpeg patch or multi-tablet/large-machine campaign is required. |
| T544 | deferred | medium | large | Expose tablet microphone as a host audio input with host-only configuration and explicit Start/Stop. Requested future resource-sharing work, deferred until camera integration T543 is accepted; then define supported desktop audio backends, Android microphone permission/foreground lifecycle, latency and echo requirements, and permanent consent/disconnect/bounds regressions before implementation. |
| T545 | deferred | medium | large | Add tablet audio output sharing controlled from UScreen host. Requested future work after T543; clarify audio direction and OS device expectations before choosing the transport/backend, then cover routing, buffering, device changes and shutdown in permanent tests. |
| T546 | deferred | medium | large | Research tablet NFC resource sharing controlled from UScreen host. Requested future work after T543; decide target NFC operations (for example tag reading versus card emulation), then establish Android/hardware and desktop integration limits before promising a transparent OS device. Require explicit sessions and permanent permission, malformed-data and disconnect coverage. |
| T487 | deferred | medium | medium | Extend T388 power validation after the USB comparison, as separately prioritized experiments: physical network transport, pen interaction and any available higher-power data source. Prepare an isolated route/input/power setup and a suitable uninterrupted device window first; avoid the unresolved T222 desktop-attachment failure and do not require another physical tablet. Preserve conservative unknown-route behavior, preferences, raw observations and measurement boundaries; verify waiting/background/reconnect and actual lock ownership. Compare within each controlled configuration instead of mixing transport, input or supply changes into the USB profile comparison. Report measured charging/discharging separately from advertised USB current capability. Deferred to keep the current battery work focused on USB. |
| T417 | deferred | low | medium | Explicitly requested for later UI work; profile controls versus read-only diagnostics still needs a decision. Follow up on T386 by exposing decoder capabilities and the selected decoder profile in the Android app. Show the active codec identity, hardware-acceleration status, advertised standard low-latency support and supported operating rate for the current codec, dimensions and stream FPS. Distinguish advertised support, requested hints and confirmed active behavior; show unknown when Android cannot verify an effective setting, and identify watchdog fallback. Explain that advertised 2x rate headroom is not measured throughput or a latency guarantee, and that missing standard low-latency support does not by itself imply slow decoding. Use T386's repeated motion/sparse-stream measurements to explain any recommended profile. Before later UI implementation, confirm whether this should include profile-selection controls or read-only diagnostics; preserve current defaults until an explicit, measured policy is chosen. Include permanent tests for supported, unsupported and unknown capabilities, API 27 fallback, codec/stream changes and fallback reporting; if controls are added, also cover persistence and safe session transitions. Requested as a future follow-up, outside the current implementation batch. |
