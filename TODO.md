# TODO

| id | status | severity | effort | description |
|---|---|---|---|---|
| T382 | open | medium | medium | Extend the [single-tablet baseline](docs/benchmarks/2026-09-17-device-baseline.md) into reproducible end-to-end performance baselines and stage traces. Its preserved CPU, latency-window and USB net-charge samples complement the T384/T387 synthetic artifacts but do not measure complete capture-to-display latency, isolated app power or multi-device scaling. Before broader device reuse, replace the initial benchmark collector's verified-for-this-tablet 100 Hz/4096-byte Android constants with validated per-device unit discovery. Record commit/toolchain/hardware/settings, capture/convert/encode/send/receive/decode/render stages, input latency, p50/p95/p99, delivered FPS, CPU/PSS/allocations and power/thermal observations for 1/2/4 tablets. Include static text, scrolling, motion and pen workloads; preserve raw samples and distinguish clock domains, simulated ACKs and USB net battery charge. Profile actual Android serialization/GC as well as decoder work. Use T408 to prevent the fake client from distorting throughput. Validate T383's work-sensitive conversion budgets on large/NUMA hosts, including sustained 128-participant scaling and cgroup CPU-time quotas; the current 32-logical-CPU replay proves functional capacity, not 128-core throughput. Follow the [performance research](docs/reviews/2026-09-17-performance-scalability.md) and [copy/loop review](docs/reviews/2026-09-17-copy-loop-power.md). |
| T388 | blocked | medium | medium | Finish sustained power validation of the implemented opt-in Android battery profile before resolving this item. Requires the user to unlock the Linux session so the controlled workload is visible; the lock screen invalidates desktop baseline comparisons (T424). The [policy report](docs/benchmarks/2026-09-18-power-policy.md) records implementation, tests, deployment and lifecycle checks. Run matched normal/profile static/motion trials, an app-off control, waiting/pen/background/reconnect observations and available network transport; measure whole-device charge, thermal state and latency. Preserve display/stream preferences and conservative unknown-route behavior. Allow for the gauge's 9.99 mAh steps; a higher-power data-port comparison requires an available alternative port/source. T395 remains unresolved pending reproduction of its older foreground-start timeout. |
| T344 | blocked | low | small | Blocked on the maintainer choice: preserve a dormant pointer preference or clear it during an explicit setting change. Resolve pointer-preference handling while the pen is disabled. `gui/src/main.rs:setting_input_devices` says a disabled checkbox must not retain a value the daemon would act on, and clears input_pointer during every render. However, `host/src/input.rs:InjectDevices::create` already gates pointer creation on both pen and pointer, and `FileConfig::sanitize` preserves the dormant preference. An isolated egui probe rendering the production settings method without events changes pen=false/pointer=true to pointer=false and marks the configuration dirty; pen=true preserves it. Unblock by choosing one consistent policy: preserve the dormant pointer preference, or explicitly clear it during a deliberate setting change/shared sanitization, then align the comment and UI. Required permanent T344 coverage: opening the tab without input, disabling/re-enabling the pen, saving/reloading and discarding edits; no host input devices are needed. |
| T332 | blocked | medium | medium | Blocked on the user-visible policy for unsupported resolution/FPS combinations: reject the edit or negotiate a fallback. Resolve independently accepted resolution/FPS settings versus the generated EDID pixel-clock limit. `common/src/model.rs:FileConfig::sanitize`, `input.rs:negotiated_geometry,apply_tablet_config` and both FPS selectors accept 3840x2160@90, but `capture.rs:helper_command` passes that mode to `edid.rs:make_edid_sized`, which rejects its clock above 655.35 MHz before helper startup. An offline probe of the production config/EDID code accepts and generates 3840x2160@60, retains 3840x2160@90 unchanged during sanitization, then fails generation; 4095x4095@60 fails likewise. A live FPS change can therefore replace a working mode with repeated setup failures. Unblock by choosing a shared joint-mode validation/negotiation policy: reject incompatible changes before retiring the current stream, or explicitly negotiate and display a supported resolution/refresh fallback. Apply it to CLI, persisted settings, GUI and tablet updates; do not remove the generator's overflow guard. Required permanent T332 coverage: clock-boundary combinations, valid 4K60→invalid 4K90 update with current-session preservation, automatic/manual geometry and accurate effective settings. Distinct from T321's low-clock consumer warning. Both generators use custom fixed-porch timings; the T325 comment correction did not change that timing contract. No display was attached. |
| T321 | blocked | low | medium | Blocked on the supported low-clock timing contract and isolated compositor acceptance evidence. Resolve accepted low-resolution/low-refresh settings versus EDID validator compatibility. `common/src/model.rs:FileConfig::sanitize` accepts 640x480@10, `host/src/capture.rs:helper_command` uses that refresh for the display, and both EDID generators accept it with a 4.14 MHz DTD clock. Offline edid-decode --check rejects that generated descriptor with Pixelclock < 10 MHz, assuming invalid data; the [primary parser](https://android.googlesource.com/platform/external/edid-decode/+/refs/heads/android15-qpr1-s5-release/parse-base-block.cpp) contains that threshold. This proves a validator incompatibility, not a reproduced kernel/display failure. Unblock by selecting and documenting the accepted timing/consumer contract: generate suitable timings while retaining the requested mode, or restrict incompatible combinations consistently across config, negotiation and both generators. Required permanent T321 coverage: smallest supported geometry at minimum FPS, cases around the selected pixel-clock boundary, generator parity and chosen rejection/fallback behavior; any display acceptance claim needs an isolated compositor test. No host display was attached. |
| T308 | blocked | low | small | Resolve fork package-maintainer metadata: `README.md:8-9` and `docs/index.html:44,431` identify geraldo-netto as the fork maintainer and majmichu1 as the original author, while `packaging/deb/control:8` and `packaging/arch/PKGBUILD:1` still identify majmichu1 as the package maintainer. `packaging/build-packages.sh` copies that Debian field into fork releases without replacement. Unblock with the intended fork package-maintainer name and public contact address, then align Debian and Arch metadata while preserving upstream authorship and historical attribution. No email address should be inferred from a username or local Git identity. Metadata-only correction needs no artificial regression test. |
| T288 | blocked | medium | small | Blocked on the user-visible dependency policy when graphics-tablet mode is selected with the pen disabled. Resolve the GUI's selectable graphics-tablet mode with the pen device disabled: `gui/src/main.rs:setting_mode,setting_input_devices` permits and saves pen_only=true/input_pen=false while displaying that nothing will be streamed, but `host/src/main.rs:initial_pen_only` replaces that combination with display mode. Reproduce by selecting graphics-tablet mode, disabling Pen tablet, saving, then restarting; the saved GUI selection and effective daemon mode differ. Unblock by choosing a coherent dependency policy (reject/disable the incompatible selection, or explicitly enable the required pen) and exposing the effective result before restart. Required permanent T288 GUI/config/CLI fixtures: both toggle orders, an existing incompatible file, save/reload, and unchanged valid display-only and pen-only combinations; no real daemon or input-device startup. |
| T276 | blocked | low | small | Blocked on the protocol decision: reply dimensions describe the virtual display or the scaled encoded stream, including the pre-negotiation state. Define consistent dimensions in control replies: `host/src/input.rs:InputConfig::response` reads live FPS/codec from EncoderSettings but always emits virtual_width/virtual_height captured at startup in `host/src/main.rs`; negotiated geometry changes the actual display afterward. A probe using the production response function with startup 2960x1848 and live 1280x800@30 returns width=2960, height=1848, fps=30. `docs/architecture.md:Input/control WebSocket` now warns that these dimensions are stale; the reply fields still lack a consistent live-geometry contract. Unblock by specifying whether width/height describe the current virtual display or scaled encoded stream, implementing that meaning for initial/repeated replies, and documenting fallback behavior before geometry is ready. Required permanent T276 coverage: negotiated resolution changes, reconnect, non-unit stream_scale, and matching serialized host/client fixtures. The current Android decoder uses its native-size hint rather than these fields, so this finding concerns protocol consistency and other consumers. |
| T259 | blocked | medium | medium | Blocked on the bounded-versus-explicitly-uncapped VAAPI policy and validation on a supported driver. Resolve the VAAPI bitrate-ceiling contradiction: the configuration/UI offer a bitrate limit, but both `host/src/capture.rs:encoder_quality_args` and `host/src/encoder.rs:low_latency_options` force VAAPI CQP. [FFmpeg 6.1 rate-control code](https://github.com/FFmpeg/FFmpeg/blob/n6.1/libavcodec/vaapi_encode.c#L1618-L1624) marks CQP as using neither bitrate nor maxrate and bypasses bitrate control parameters for it. The configured ceiling therefore does not constrain this path; common/src/lib.rs and docs/development.md now document that limitation, but the UI/policy remains unresolved. Unblock by selecting a driver-supported bounded rate-control policy (with explicit handling of unsupported modes), or documenting and exposing an intentionally uncapped mode. Required permanent T259 coverage: CLI/in-process option parity and capability fallback fixtures, plus real VAAPI high-detail output measurement against the chosen cap. The [Navi 23 device baseline](docs/benchmarks/2026-09-17-device-baseline.md) measured median motion output of 28.85–29.22 Mb/s with a configured 20 Mb/s ceiling, confirming the existing CQP limitation on this driver; it does not select or validate a replacement policy. |
| T250 | blocked | medium | small | Resolve the fork release-signing contract: `android/app/build.gradle.kts` accepts any supplied release keystore, but an official fork signing identity and migration from upstream have not been established. SECURITY.md and docs/development.md describe this as pending and no longer assert the upstream fingerprint as the fork identity. A developer-generated key alone does not establish an official update contract. Unblock with the maintainer's chosen fork certificate and update/migration policy, then align documentation and, if an official certificate is required, verify its public fingerprint before publishing. Documentation-only changes need no artificial test; a publication gate needs a permanent wrong-certificate regression. |
| T222 | blocked | high | large | Diagnose and prevent the Xorg crash on EVDI attachment under Cinnamon. On 2026-09-17 at 01:52:40 CEST, Xorg faulted at address `0x20` through `ProcRRSetCrtcGamma`, immediately after `host/evdi/evdi_helper.c:1361` attached the virtual display; the later helper restart did not precede the fault. Blocked on identifying the failing Xorg state with a symbolized core or isolated reproducer and validating a mitigation. Required permanent coverage: isolated Xorg/EVDI attach, gamma update and detach without server/session loss; current fake-helper tests cannot exercise the system display server. See [incident evidence](docs/reviews/2026-09-17-cinnamon-restart.md). |
| T395 | open | high | medium | Investigate Android cold-start foreground-service timeout. On the RugKing Pad 2 Pro running Android 16, the freshly installed debug APK from 29b8068 crashed at 2026-09-17 12:25:50 CEST with ForegroundServiceDidNotStartInTimeException for StreamingService (PID 9152), after MainActivity and host token delivery launched together. The next process (9276) connected successfully and reported isForeground=true. That build promoted only in onStartCommand; current T388 code also promotes in onCreate and tests rejection/cancellation, but the original failing sequence remains unestablished. Reproduce with controlled cold start, token delivery and rotation/background transitions; preserve timestamped lifecycle evidence. Add a permanent failing lifecycle regression before fixing the demonstrated cause, covering prompt foreground promotion, cancellation/re-entry and lock release. Do not treat a successful retry as resolution. |
| T413 | open | medium | medium | Investigate control admission when a transient ADB package-query failure makes an attached tablet ineligible while its app and reverse mappings remain alive. `app_installed_with` maps command failures to false; primary reassignment retires accepted attachment leases but neither removes the old device's reverse routes nor changes the shared token. A reconnect from the previous tablet after the epoch changes may therefore be accepted into the replacement slot. This is a source-level race candidate, not a reproduced incident. Add a fake-ADB/two-client reproduction covering transient `pm path` failure, reassignment, late reconnect and geometry/input ownership; distinguish unknown probe results from confirmed package absence and define route/token retirement before fixing confirmed behavior. Preserve T281 accepted-connection generation checks. |
| T414 | open | medium | large | Investigate and improve audio/video synchronization: the user reports YouTube video appearing slightly behind audio on an older UScreen build. The user describes the audio setup as “bluetooth headset with a tablet”; verify the Bluetooth source/paired device and whether YouTube runs on Linux through UScreen or directly on Android. Record exact host/APK versions, stream settings and whether the offset is constant or grows over time; distinguish the reported older build from the subsequently deployed candidate. Reproduce with a timestamped flash/click clip and measure initial offset, sustained drift, pause/seek, reconnect, overload and recovery. Trace browser/compositor capture, raw FIFO age, encoder buffering, transport, Android decode and Surface presentation against the actual audio clock; larger pipes can hide stale video. `host/src/stream.rs` and `android/.../DecoderSession.kt` use sequence IDs as codec presentation timestamps for ACK identity, not a shared media clock, and the current protocol has no audio stream. T386 physical traces found the callback render-time argument equal to `sequence * 1000` rather than a comparable `System.nanoTime()` value; the boolean output-release API propagates input PTS to Surface timestamps. Preserve these raw traces and compare explicit monotonic output-release timestamps while retaining sequence-based ACK identity before interpreting render-time differences as latency. Separate latency reduction from any proposed audio-delay or synchronized-audio feature; retain sequence/ACK identity if real media timestamps are added. Coordinate T382/T386/T389/T399/T405. Before fixing a confirmed cause, add a permanent failing deterministic clock/queue regression, then prove it passes; require physical A/V offset/drift measurements for claimed playback improvements. |
| T415 | open | medium | medium | Implement the requested raw capture pipe-capacity dropdown with exactly 1, 2, 4 and 8 MiB choices; preserve the 1 MiB default. Confirm which settings surface(s) should expose it before UI-dependent work, then carry the validated selection through shared configuration and the helper/FIFO-open boundary. Report requested versus effective F_GETPIPE_SZ capacity and retain a working smaller pipe when enlargement is denied by kernel/per-user limits; do not require a sysctl change or new service capability. Define safe application of edits, including shrinking a nonempty pipe, reconnect and per-session settings, without recreating the desktop display or discarding part of a raw frame. Explain the measured CPU-versus-queued-frame-age tradeoff using the [capacity ramp](docs/benchmarks/2026-09-17-pipe-capacity.md). Add permanent configuration/UI persistence and real isolated FIFO regressions for all choices, invalid values, rounding/refusal fallback, active edits and T226 recovery; compare stock-FFmpeg CPU and actual display latency before changing the default. Coordinate T389/T405/T414. |
| T416 | open | low | medium | Measure and, if useful, cap CLI packet publication batches independently of read bytes. `annex_b.rs:process_complete_nals` returns a Vec of all completed access units from one bounded 512 KiB read; exceptionally dense tiny units can allocate many VideoPacket/MediaBytes metadata objects before T391 queue admission. This is bounded by input size, but its peak is outside the encoded backing budget. Compare incremental bounded batches or a publication callback against the T407 read replay, including minimum-sized units, 1/2/4 sessions, slow consumers, sequence/config ordering, EOF, cancellation and generation retirement; keep the existing framing and bounded NAL/AU/CSD limits. Record CPU, allocation count and peak retained metadata before adding a more complex drain interface. |
| T417 | open | low | medium | Follow up on T386 by exposing decoder capabilities and the selected decoder profile in the Android app. Show the active codec identity, hardware-acceleration status, advertised standard low-latency support and supported operating rate for the current codec, dimensions and stream FPS. Distinguish advertised support, requested hints and confirmed active behavior; show unknown when Android cannot verify an effective setting, and identify watchdog fallback. Explain that advertised 2x rate headroom is not measured throughput or a latency guarantee, and that missing standard low-latency support does not by itself imply slow decoding. Use T386's repeated motion/sparse-stream measurements to explain any recommended profile. Before later UI implementation, confirm whether this should include profile-selection controls or read-only diagnostics; preserve current defaults until an explicit, measured policy is chosen. Include permanent tests for supported, unsupported and unknown capabilities, API 27 fallback, codec/stream changes and fallback reporting; if controls are added, also cover persistence and safe session transitions. Requested as a future follow-up, outside the current implementation batch. |
| T418 | open | medium | large | Follow up on T389's isolated memfd-ring measurements with a production raw-frame transport experiment. Convert directly into bounded shared slots and wrap read-only slot leases in stock libavcodec AVBufferRef/AVFrame ownership so encoder-retained references delay reuse; sending mapped pixels through another FIFO would retain the main copy boundary. Implement explicit descriptor handoff, validated version/format/dimensions/strides/size/sequence/generation, shrink/grow seals, acquire/release state transitions, cancellation and peer-crash/resize recovery. Preserve the stock-FFmpeg CLI/FIFO fallback and optional VAAPI rejection. Require permanent retained-frame, malformed-descriptor, stale-generation, full-ring, partial-startup, crash and mode-change tests before enabling, followed by actual conversion/encode/display latency at 60 FPS and 1/2/4 streams. The synthetic ring uses memset and sampled-byte verification, not capture, full encoder reads, USB or physical display; its throughput gains are not end-to-end gains. Keep this follow-up outside the current implementation batch. |
| T419 | open | medium | large | Research alternative frame compression for a future desktop-streaming mode, separately from T400's video-codec comparison. Compare full frames, changed rectangles/tiles and temporal deltas using fast lossless LZ4 or low-level Zstandard, with QOI/PNG or intra-image coding as controls, against hardware-decoded H.264/HEVC. Evaluate a hybrid only if static text/pen content benefits and switching to motion has bounded latency without quality flashes or stale pixels. Preserve original RGB/chroma detail when measuring lossless text fidelity; compressing NV12 cannot recover already discarded chroma. Separate host-only raw-frame IPC compression from wire-to-Android compression, using T389/T418 direct/shared-memory transport as the local baseline. Prioritize capture-to-display p50/p95/p99, motion recovery, bandwidth, Android decode/composition/upload cost, sustained battery/thermal behavior and 1/2/4-stream scaling; ample host CPU/RAM does not establish a latency or Android power gain. Measure any second compression of already encoded video as a control rather than assuming savings. Specify capability negotiation, tile/base-frame identity, atomic presentation, bounded decompressed sizes, loss/reconnect/full-refresh recovery and fallback; require permanent round-trip, corruption, stale-generation and lifecycle tests before enabling. Start from [LZ4](https://github.com/lz4/lz4) and [Zstandard](https://github.com/facebook/zstd) primary documentation; corpus benchmarks do not predict desktop-frame results. Coordinate T399/T400/T403/T414. Requested as later research, outside the current implementation batch. |
| T420 | open | medium | medium | Prevent token-recovery launches from stealing foreground focus from another Android app. During the T399 cadence replay, the installed baseline daemon logged “Delivering the session token … next in 600s” and launched UScreen at 2026-09-18 00:33:28 UTC, interrupting the separate benchmark before it wrote a result. Review current `host/src/monitor.rs:extra_token`, `host/src/main.rs:launch_app_using` and `android/.../TokenActivity.kt` against that reproduction; the deployed baseline predates T390, so verify the current behavior before attributing it to the new monitor. Preserve explicit user launch/reconnect and secure token delivery while allowing users to inspect another app. Require a permanent fake-ADB/lifecycle regression with UScreen alive but another Activity foreground, then a physical replay; avoid restarting EVDI/Cinnamon to test this. Outside the selected implementation batch. |
| T421 | open | low | medium | Investigate repeated stock-FFmpeg “non monotonically increasing dts” warnings in the installed baseline during T399 measurements. `host/src/capture/cli_encoder.rs` combines rawvideo frame rate, wall-clock input timestamps and passthrough output; inspect writer catch-up bursts and timestamp quantization before assigning a cause. Reproduce on the current candidate and preserve complete raw frames, encoded reference order and periodic IDR behavior while obtaining valid timestamps; do not patch FFmpeg or silently duplicate/drop coded frames. Add a permanent CLI timing regression before any confirmed fix and distinguish log noise from measured playback/A/V effects. Coordinate T399/T414; outside the selected implementation batch. |
| T422 | open | medium | medium | Isolate the stock AMD VAAPI HEVC / Unisoc decoder interoperability failure found in T400. A 1280×800/60 Main-profile 8-bit sample encoded by stock FFmpeg 6.1.1/Mesa on RX 6600 XT decodes fully in software but `c2.unisoc.hevc.decoder` rejects it with native error 14 before measured frames; the same replay decodes libx265 HEVC successfully. Preserve fixture hashes, codec inventory, CSD, headers and failure logs in the T400 artifacts. Compare stock encoder options, stream parameter sets and Android configuration without patching FFmpeg or rewriting undocumented bitstream fields; distinguish a replay framing error from a driver/encoder compatibility issue before changing production. Add a permanent failing decode/negotiation regression for the demonstrated cause, verify the physical tablet and retain H.264 fallback. Advertised HEVC support alone must not be reported as successful decoding. |
| T442 | open | low | small | Make `scripts/benchmarks/plot-baseline.py` handle missing battery, CPU or latency observations and label only recorded settings. `series` indexes the first battery row and `plot` unpacks empty series, while its title unconditionally claims H.264 VAAPI 1280x800/60 for any dataset. Add permanent sparse-data and provenance-title regressions, retain available traces and identify missing observations explicitly. |
| T429 | open | medium | medium | Investigate the user's report that selecting the low-latency option makes UScreen keep searching for a while before streaming resumes. Confirm the exact option, measure the interruption and distinguish an expected encoder restart from repeated connection/decoder recovery; record host/APK versions, GPU/driver, codec, resolution/FPS and timestamped host/Android logs. Compare normal and explicit low-latency VAAPI H.264 selection transitions with otherwise identical settings, checking configuration persistence, encoder restart/failure, capture/display ownership, H.264 headers and Android decoder setup/recovery. Verify that the resumed stream actually uses the selected profile rather than silently reverting. T400's isolated Constrained Baseline benchmark does not prove the live transition works. Preserve a working default and avoid unrelated EVDI/Cinnamon restarts during diagnosis. Add a permanent failing regression for any demonstrated cause before fixing it, covering transition duration/retries, recovery, profile retention and switching back; verify live playback on the affected device. |
