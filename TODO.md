# TODO

| id | status | severity | effort | description |
|---|---|---|---|---|
| T133 | open | high | small | Restart active Android sessions when the token changes during control reconnect. `MainActivity.applyToken` gates restart on an already-connected control socket, while `VideoReceiver.start` captures the old token for every retry; reconnect then repeatedly authenticates video with stale credentials. Add a permanent regression that changes the token with control disconnected and video running, proves both active connection generations restart, and preserves background inactivity. |
| T134 | blocked | high | medium | Resolve decoder recovery contradiction in `VideoReceiver.feedDecoder/resetCodec/onSurfaceDestroyed`: watchdog comments require a new connection for codec headers and a keyframe, but input timeout, codec errors, and surface replacement reset the decoder while retaining its stream; generic feed errors silently discard frames. Unblock by making every decoder-invalidating path obtain fresh headers/keyframe before resuming. Add permanent regressions for timeout, feed failure, and surface replacement that verify transport retirement and recovery. |
| T135 | blocked | medium | medium | Resolve GUI tablet-status contradiction: `gui/src/main.rs::poll_status` claims to select like the daemon but selects the first ready USB device, while the daemon retains actual app sessions and doctor reads their runtime ledger. A charging phone can appear as the streaming tablet. Unblock by deriving GUI active tablet status from validated daemon sessions. Add permanent regressions for an unrelated first USB device, an active Wi-Fi tablet, multiple sessions, and stale session metadata. |
| T136 | blocked | medium | medium | Resolve configured tablet-capacity/setup contradiction: `needs_system_setup` accepts any positive EVDI count and `system_setup_script` hardcodes two boot devices, so `max_tablets=4` with two devices is reported ready despite insufficient capture slots; config documentation claims setup matches capacity. Unblock by checking and provisioning the configured device count and making setup documentation accurate. Add permanent regressions for existing two/configured four devices, missing devices, and persisted boot count. |
| T139 | open | medium | medium | Bound video-client admission and authenticate before capture subscription/keyframe requests. `StreamServer::run_with_listener` subscribes and sets `idr_wanted` before authentication, spawning unlimited pending clients; unauthenticated sockets therefore trigger capture work and consume unbounded tasks. Add permanent loopback regressions proving pending/invalid clients cannot subscribe or request IDRs, admission is bounded, and capacity returns after disconnect. |
