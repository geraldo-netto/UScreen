# TODO

| id | status | severity | effort | description |
|---|---|---|---|---|
| T368 | open | medium | large | Consolidate session construction and teardown in `host/src/main.rs:run_daemon,spawn_extra_session,ExtraSession::stop`. Both paths build settings/channels, capture, stream and input servers, but maintain separate ownership and shutdown wiring. Extract a session specification and owned runtime; keep primary persistence/CLI overrides, extra-slot ports/cards and global/per-session shutdown explicit. Preserve startup-failure cleanup, second-bind failure, settings parity and stop/reconnect coverage. Coordinate with T281/T330 without silently choosing their unresolved policies. See the [architecture review](docs/reviews/2026-09-17-architecture.md). |
| T369 | open | medium | large | Split `host/src/capture.rs` by responsibility: neutral media/settings types and Annex-B packetization, helper/FIFO/process adapter, desktop placement, encoder adapter and capture supervision. Stream/input/encoder currently import shared types from the concrete capture implementation. Reuse the existing encoder I/O and packet helpers; preserve generation invalidation, startup cancellation, partial access units and cleanup tests. Keep T224/T226/T245/T348 behavior decisions separate; introducing interfaces alone must not claim to fix them. |
| T370 | open | medium | large | Separate `host/src/input.rs` wire decoding/controller dispatch from session settings, coordinate mapping and Linux uinput/KWin/X11 adapters. `InputServer` directly owns concrete devices and `apply_tablet_resolution` loads persistent config. Introduce narrow input/settings interfaces and shared Rust/Android wire fixtures, preserving authentication, controller ownership, event ordering, device release and geometry tests. Reuse those fixtures when resolving T247/T276/T318; do not merge transport authentication or platform mapping policies merely because their loops resemble each other. |
| T375 | open | medium | medium | Split Android `TouchCapture.kt` control-session connection/authentication/retry ownership from MotionEvent translation, touch-slot state and wire serialization. Merge the repeated pen field construction in `emitPen,sendPenButton,sendPenProximityExit` through one typed serializer while keeping each action's semantics explicit. Preserve historical stylus samples, eraser/tilt, button/proximity order, touch cancellation and connection-generation tests. Shared wire fixtures should agree with Rust serialization; T247/T318 remain independent behavioral work. |
| T376 | open | medium | large | Split Android `VideoReceiver.kt` socket framing, decoder ownership/watchdog and timing/statistics coordination behind narrow clock/codec/transport boundaries. Start/stop, Surface lifecycle, reader/output threads and callbacks currently share one concrete mutable object, and tests reach into private fields. Preserve every generation check and ownership handoff; retain restart, fragmented packet, Surface destruction, codec failure and stop/start coverage. T339 fallback semantics and T386 performance experiments require their own evidence, not incidental changes during extraction. |
| T377 | open | medium | medium | Separate Android `MainActivity.kt` session coordination/window policy from Compose rendering and settings sections. `onCreate,connectStreamCallbacks,applyStreamSettings` and UI functions directly coordinate Prefs, VideoReceiver and TouchCapture; SettingsSheet mixes unrelated control groups. Introduce observable session/settings state and explicit events with an activity-scoped coordinator; extract focused UI sections and window-policy adapter. Preserve app-only brightness/refresh defaults, user overrides, switching apps, orientation, reconnect and settings tests; do not restart streaming merely because Compose recomposes. |
| T379 | open | low | medium | Reuse `scripts/stage-linux-bundle.sh` from `Makefile:dist-local` for the duplicated binary/library/symlink/script/icon/document layout. Parameterize the validated local libevdi input without losing portable-build provenance checks, APK requirements or local-versus-release distinctions. Compare complete staged trees, file permissions, library symlinks and document links in existing distribution fixtures; cover missing dependencies. Resolve alternate Cargo output selection through T336 rather than preserving the duplicate hardcoded paths in a new wrapper. |
| T380 | open | medium | large | Split `host/evdi/evdi_helper.c` conversion kernels/worker pool, frame exchange/FIFO writer and EVDI event/mode lifecycle into explicit contexts and ownership interfaces. Conversion jobs already carry their inputs, but pool/buffer/mode lifetime still depends on numerous shared globals; native tests include the entire production translation unit. Start with conversion/pool boundaries, preserve distinct scaled/native hot paths, and retain sanitizer-backed mode-change, dirty-row, worker-start failure and writer-shutdown coverage. Preserve callback/buffer lifetime and T226's unresolved framing condition; do not claim that moving globals alone removes races. |
| T382 | open | medium | medium | Establish reproducible current-fork performance baselines and stage traces; `docs/benchmarks.md` contains historical upstream results without raw samples, and send-to-ack begins after capture/encoding. Record commit/toolchain/hardware/settings, capture/convert/encode/send/receive/decode/render stages, input latency, p50/p95/p99, delivered FPS, CPU/PSS/allocations and power/thermal observations for 1/2/4 tablets. Keep clock domains and USB net battery charge explicit. Add a replayable synthetic workload and raw-result format; hardware runs remain necessary. Follow the [performance research](docs/reviews/2026-09-17-performance-scalability.md). |
| T383 | open | medium | medium | Benchmark aggregate conversion CPU/memory budgets across tablets. `host/evdi/evdi_helper.c:conv_pool_init` independently selects online CPUs minus two, clamped to 1..8, for every helper; four sessions can request 32 conversion execution slots plus encoders/writers. Compare 1/2/4/8 workers, restricted CPU availability and 1/2/4 sessions; measure conversion time, context switches, bandwidth and per-session tail latency. Evaluate a host-wide worker budget and portable vectorization only after profiling. Preserve exact conversion/dirty-row/scaling output and worker-failure/shutdown tests; oversubscription is a code-derived risk, not a measured current-host regression. |
| T384 | open | medium | medium | Benchmark and reduce avoidable Annex-B packetizer allocations/scans in `host/src/capture.rs:H264AnnexBPacketizer`. `process_complete_nals` scans the retained buffer twice per push, copies each NAL and drains its front; configuration bytes are copied for packets. Compare an incremental scan cursor, retained capacity and immutable cached config under fragmented H.264/HEVC and large access units. Measure allocations/bytes copied and throughput before changing code. Preserve all packetization/generation/IDR tests and add chunk-boundary equivalence coverage for any new algorithm; `stream.rs:write_frame` already uses vectored I/O. |
| T386 | open | medium | large | Evaluate Android decoder scheduling/capability profiles in `VideoReceiver.kt:feedDecoder,startOutputThread,decoderFormat,releaseCodec`. Input waits up to ten 20 ms dequeue attempts while holding the receiver monitor; output polls separately, and low-latency/2x operating-rate/vendor hints are applied broadly. Compare the current path with a single codec owner using callbacks and a bounded access-unit queue; check capabilities and measure CPU wakeups, allocation, latency, power and sustained output. Preserve CSD/IDR, reconnect/Surface generations, callback retirement and T339 fallback coverage. No default/profile change without device measurements across supported API levels and hardware. |
| T387 | open | medium | medium | Measure and define Android control-channel backpressure in `TouchCapture.kt:sendWhenConnected` and pending-setting delivery. JSON is built for each pen/history sample and frame acknowledgement; WebSocket.send's Boolean result is ignored, with no application queue-size telemetry. OkHttp 4.12 returns false on closed/overflowed queues and caps queued payload at 16 MiB. Add fake slow/refusing WebSocket coverage for failed sends, reconnect and pending settings; measure queued bytes, GC and event age. Preserve ordered down/up/button/cancel and stylus history; choose explicit overload recovery rather than silently dropping drawing samples. See T375 and the performance research. |
| T388 | open | medium | medium | Measure Android power/thermal cost of decoder hints, actual display refresh and service locks during USB/Wi-Fi streaming, pen-only, waiting and background states. `StreamingService.kt` acquires locks on service start independently of negotiated transport; ADB loopback alone cannot identify physical USB versus Wi-Fi. Use capability-aware traces and reliable host transport information before considering selective locks. Preserve configurable app-only 50% brightness/60 Hz defaults and normal other-app settings. Compare sustained runs with controlled charging/workload; record USB battery counters as net charge, not app consumption. Any automatic quality/brightness policy needs a separate explicit decision. |
| T389 | open | medium | large | Produce a measured raw-frame transport/backend feasibility comparison after T369/T380: framed FIFO versus leased shared-memory buffers, and optional PipeWire/DMA-BUF capture with hardware-frame encoding. Current CLI and in-process paths still receive packed NV12 through FIFO; the latter also copies into libavcodec planes. Resolve T226 before claiming frame-safe IPC. Specify sequence/size/format/generation, buffer lifetime, backpressure and process failure recovery; validate GPU format/modifier/synchronization and portable fallbacks. Probe portal VIRTUAL-source availability per desktop, preserving extended-display behavior and EVDI fallback. Preserve the optional build's explicit VAAPI restriction; require separate hardware-context integration and isolated encode/decode evidence before adding support, and keep T259 bitrate semantics unresolved. |
| T390 | open | medium | medium | Test and improve multi-device discovery fairness in `host/src/main.rs:adb_monitor,unique_devices,app_devices_with,TabletMonitor`. Device identity/app checks and session setup are awaited sequentially, so one slow ADB device extends the polling cycle for others. Build deterministic fake-ADB slow/failing-device scenarios and measure time to connect/recover unaffected tablets and stop. Evaluate bounded per-device concurrency with explicit global ADB serialization where needed, stable USB/Wi-Fi identity, backoff and cancellation. Preserve T281/T278/T330 decisions and T328 descendant-lifetime limitations; never parallelize shared forwarding changes without ownership. |
| T391 | open | medium | medium | Establish multi-session/client resource and slow-consumer limits using `host/src/main.rs` broadcast queues and `stream.rs,input.rs` client admission. Video queues hold eight packets per session, while payload sizes and per-client writes vary; cheap Bytes clones share storage and must not be counted as full copies. Test 1/2/4 sessions with fast/slow/unauthenticated clients, codec changes and reconnect churn; record peak retained bytes, tasks, FD count and per-session tail latency. Define byte/time budgets and fair recovery only from measurements, preserving generation/IDR resynchronization. Keep the four-tablet limit until card ownership and aggregate budgets are validated. |
| T344 | blocked | low | small | Blocked on the maintainer choice: preserve a dormant pointer preference or clear it during an explicit setting change. Resolve pointer-preference handling while the pen is disabled. `gui/src/main.rs:setting_input_devices` says a disabled checkbox must not retain a value the daemon would act on, and clears input_pointer during every render. However, `host/src/input.rs:InjectDevices::create` already gates pointer creation on both pen and pointer, and `FileConfig::sanitize` preserves the dormant preference. An isolated egui probe rendering the production settings method without events changes pen=false/pointer=true to pointer=false and marks the configuration dirty; pen=true preserves it. Unblock by choosing one consistent policy: preserve the dormant pointer preference, or explicitly clear it during a deliberate setting change/shared sanitization, then align the comment and UI. Required permanent T344 coverage: opening the tab without input, disabling/re-enabling the pen, saving/reloading and discarding edits; no host input devices are needed. |
| T339 | open | medium | medium | Resolve decoder fallback behavior for repeated one-frame stalls. `VideoReceiver.kt` describes a decoder that renders one frame then stops, and promises to disable low-latency hints after repeated stalls; however, `startOutputThread` resets `outputStalls` on every output frame, while `checkOutputProgress` requires two accumulated stalls. An isolated API 27/34 Robolectric probe runs the production output worker with a codec emitting one frame per restart, then triggers the watchdog after four queued frames and two seconds without output: all three restarts report stalls=1 and hints=true. Resolve by defining what sustained output counts as recovery and retaining the stall history until that condition, so the documented one-frame failure can reach the fallback. Required permanent T339 coverage: repeated zero-frame and one-frame stalls, sustained recovery resetting history, selected fallback format hints, stop/start and codec changes, with controlled clock/codec fixtures and no hardware stream. |
| T336 | open | medium | medium | Resolve source-build output selection across Cargo, Make and the installer. `Makefile:build` lets Cargo honor its selected target directory, but `scripts/install.sh:install_binaries` and Make run/status/stop/list/dist-local paths always use `target/release`; `scripts/ci/build-artifacts.sh` already selects CARGO_TARGET_DIR explicitly. [Cargo supports alternate output directories through environment/config/flags](https://doc.rust-lang.org/cargo/reference/build-cache.html). An isolated installer fixture with new binaries under CARGO_TARGET_DIR and stale binaries under the default target path returns success and installs the stale daemon/GUI. With no default outputs it instead fails after a successful custom-directory build. Resolve by defining one artifact-location contract for source workflows: resolve and consume the effective Cargo target output, or explicitly force a consistent project output directory for both build and consumers. Preserve prebuilt-tarball selection. Required permanent T336 regressions: absolute and relative target directories, spaces/metacharacters, Cargo configuration override, stale/missing default outputs and unchanged tarball installation; execute build/install/dist dispatch with disposable files/stub tools only. |
| T332 | blocked | medium | medium | Blocked on the user-visible policy for unsupported resolution/FPS combinations: reject the edit or negotiate a fallback. Resolve independently accepted resolution/FPS settings versus the generated EDID pixel-clock limit. `common/src/model.rs:FileConfig::sanitize`, `input.rs:negotiated_geometry,apply_tablet_config` and both FPS selectors accept 3840x2160@90, but `capture.rs:helper_command` passes that mode to `edid.rs:make_edid_sized`, which rejects its clock above 655.35 MHz before helper startup. An offline probe of the production config/EDID code accepts and generates 3840x2160@60, retains 3840x2160@90 unchanged during sanitization, then fails generation; 4095x4095@60 fails likewise. A live FPS change can therefore replace a working mode with repeated setup failures. Unblock by choosing a shared joint-mode validation/negotiation policy: reject incompatible changes before retiring the current stream, or explicitly negotiate and display a supported resolution/refresh fallback. Apply it to CLI, persisted settings, GUI and tablet updates; do not remove the generator's overflow guard. Required permanent T332 coverage: clock-boundary combinations, valid 4K60→invalid 4K90 update with current-session preservation, automatic/manual geometry and accurate effective settings. Distinct from T321's low-clock consumer warning. Both generators use custom fixed-porch timings; the T325 comment correction did not change that timing contract. No display was attached. |
| T330 | open | medium | medium | Resolve multi-tablet card assignment versus EVDI ownership: `host/src/main.rs:run_daemon,assign_slot_card,spawn_extra_session` pins slots to the lowest/all-card enumeration from `vdisplay::evdi_cards`, including cards occupied by other applications. The startup comment says helper discovery would choose the same card for every tablet, but `evdi_helper.c:open_available_device_in` already leases distinct free cards and refuses a pinned busy card. Existing isolated T108 Rust/C fixtures confirm positional assignment plus rejection of connected card 0 while card 2 is free; enabling multi-tablet mode can therefore strand the primary on an external application's card despite spare capacity. Resolve by defining stable per-session allocation from available cards, preserving other applications' outputs and preventing two slots from sharing a card across restarts/device additions/removals. Required permanent T330 coverage: busy lowest card with sufficient free cards, simultaneous slot startup, reconnect/restart, card-set changes and exhausted capacity, with production daemon allocation and fake helper leases in one fixture. Preserve T108's no-stealing and pinned-card guarantees; no real DRM operation was performed. |
| T328 | open | medium | medium | Resolve command-timeout scope in `common/src/commands.rs` versus delegated operations such as `gui/src/main.rs:run_system_setup` (`pkexec sh -c ...`). Sync/async timeout paths kill only the direct child, so reporting timeout does not stop descendants from continuing the operation. An isolated probe of production SyncCommandExt with a disposable shell, a 100 ms deadline and a worker that writes after 300 ms reports timeout while the worker remains alive and subsequently writes; the probe reaps its own descendant and touches only a temporary directory. Resolve by defining and implementing cancellation of delegated work, including privileged descendants that the unprivileged caller cannot signal, or explicitly narrowing the operation/deadline contract and exposing continued work accurately. Required permanent T328 coverage: sync timeout, async timeout/cancellation, descendant retirement and no writes after completed cancellation, successful-command behavior and isolated privileged delegation if retained. Preserve T094's direct-child reaping assertions; no host setup or privileged process was exercised. |
| T321 | blocked | low | medium | Blocked on the supported low-clock timing contract and isolated compositor acceptance evidence. Resolve accepted low-resolution/low-refresh settings versus EDID validator compatibility. `common/src/model.rs:FileConfig::sanitize` accepts 640x480@10, `host/src/capture.rs:helper_command` uses that refresh for the display, and both EDID generators accept it with a 4.14 MHz DTD clock. Offline edid-decode --check rejects that generated descriptor with Pixelclock < 10 MHz, assuming invalid data; the [primary parser](https://android.googlesource.com/platform/external/edid-decode/+/refs/heads/android15-qpr1-s5-release/parse-base-block.cpp) contains that threshold. This proves a validator incompatibility, not a reproduced kernel/display failure. Unblock by selecting and documenting the accepted timing/consumer contract: generate suitable timings while retaining the requested mode, or restrict incompatible combinations consistently across config, negotiation and both generators. Required permanent T321 coverage: smallest supported geometry at minimum FPS, cases around the selected pixel-clock boundary, generator parity and chosen rejection/fallback behavior; any display acceptance claim needs an isolated compositor test. No host display was attached. |
| T318 | open | medium | medium | Resolve Android hover lifecycle versus Linux pen proximity in `TouchCapture.kt:handleHoverEvent,setSurfaceView,sendPenProximityExit` and `host/src/input.rs:UInputDevice::inject_pen`. Android ACTION_HOVER_EXIT is forwarded as action=4 (leave proximity), but [AOSP dispatchHoverExit](https://android.googlesource.com/platform/frameworks/native/+/refs/heads/main/services/inputflinger/reader/mapper/TouchInputMapper.cpp) emits it when a hovering stylus touches down too, using the previous button state. With the side button held, the app sends hover, button-down, proximity-out, tip-down without reasserting the held button. [libinput tablet_flush](https://gitlab.freedesktop.org/libinput/libinput/-/blob/1.28.0/src/evdev-tablet.c) clears button state on proximity-out, so that hover-to-contact transition loses the button modifier. The host also emits proximity-out on ordinary tip-up. Resolve by defining consistent tip/hover/physical-proximity semantics and transporting current stylus-button state through transitions without latching buttons after a real exit. Required permanent T318 Android/host fixtures: held-button hover→down→up→hover, real exit/reentry, eraser, cancellation, view-boundary hover exits and reconnect; verify ordered input frames and modifier state with simulated events, without host uinput. T317 independently releases the kernel key when action=4 explicitly ends proximity; it does not resolve this client/event semantic mismatch. |
| T308 | blocked | low | small | Resolve fork package-maintainer metadata: `README.md:8-9` and `docs/index.html:44,431` identify geraldo-netto as the fork maintainer and majmichu1 as the original author, while `packaging/deb/control:8` and `packaging/arch/PKGBUILD:1` still identify majmichu1 as the package maintainer. `packaging/build-packages.sh` copies that Debian field into fork releases without replacement. Unblock with the intended fork package-maintainer name and public contact address, then align Debian and Arch metadata while preserving upstream authorship and historical attribution. No email address should be inferred from a username or local Git identity. Metadata-only correction needs no artificial regression test. |
| T288 | blocked | medium | small | Blocked on the user-visible dependency policy when graphics-tablet mode is selected with the pen disabled. Resolve the GUI's selectable graphics-tablet mode with the pen device disabled: `gui/src/main.rs:setting_mode,setting_input_devices` permits and saves pen_only=true/input_pen=false while displaying that nothing will be streamed, but `host/src/main.rs:initial_pen_only` replaces that combination with display mode. Reproduce by selecting graphics-tablet mode, disabling Pen tablet, saving, then restarting; the saved GUI selection and effective daemon mode differ. Unblock by choosing a coherent dependency policy (reject/disable the incompatible selection, or explicitly enable the required pen) and exposing the effective result before restart. Required permanent T288 GUI/config/CLI fixtures: both toggle orders, an existing incompatible file, save/reload, and unchanged valid display-only and pen-only combinations; no real daemon or input-device startup. |
| T281 | open | high | medium | Resolve attachment identity versus geometry readiness: `host/src/main.rs:TabletMonitor::update_primary` publishes detach and attach through a boolean watch channel, while `spawn_display_gate` clears geometry_ready only when it observes false. Tokio watch can coalesce those transitions; a current-thread probe using the production gate sends false then true before yielding and retains active=true, geometry_ready=true from the previous tablet. This conflicts with T223's fresh-geometry-before-attachment requirement in `host/src/capture.rs`. Resolve by representing attachment identity/generation or invalidating readiness at the producer, defining physical-tablet replacement versus same-tablet transport migration, and associating negotiated metadata with the current authenticated session. Required permanent T281 regression: delayed gate consumer, immediate replacement with different native/physical dimensions, and USB/Wi-Fi migration; no helper attach with stale geometry, and no invalidation of newly accepted current-session metadata. Use fake helpers only. |
| T278 | open | medium | small | Resolve wireless ADB identity versus USB preference: `host/src/main.rs:transport_of` assumes every network serial contains a colon, but [AOSP's transport definition](https://android.googlesource.com/platform/packages/modules/adb/+/refs/heads/main/transport.h) also uses TLS mDNS names such as adb-43081FDAS000ST-XKzA7F._adb-tls-connect._tcp. A standalone probe using the production transport_of/select_device_transports functions classifies that serial as Usb and keeps it when the same tablet's physical USB transport is newly attached. This defeats the documented USB preference and also lets wifi_device_with accept an already wireless device as its USB prerequisite. Resolve by defining reliable transport classification from ADB metadata or supported serial forms and applying it consistently to selection, deduplication, Wi-Fi setup and labels. Required permanent T278 fixtures: USB, host:port, IPv6 and mDNS wireless serials, same-tablet USB takeover from an active wireless connection, unknown identities, and USB-only Wi-Fi setup eligibility; use fake ADB only. |
| T276 | blocked | low | small | Blocked on the protocol decision: reply dimensions describe the virtual display or the scaled encoded stream, including the pre-negotiation state. Define consistent dimensions in control replies: `host/src/input.rs:InputConfig::response` reads live FPS/codec from EncoderSettings but always emits virtual_width/virtual_height captured at startup in `host/src/main.rs`; negotiated geometry changes the actual display afterward. A probe using the production response function with startup 2960x1848 and live 1280x800@30 returns width=2960, height=1848, fps=30. `docs/architecture.md:Input/control WebSocket` now warns that these dimensions are stale; the reply fields still lack a consistent live-geometry contract. Unblock by specifying whether width/height describe the current virtual display or scaled encoded stream, implementing that meaning for initial/repeated replies, and documenting fallback behavior before geometry is ready. Required permanent T276 coverage: negotiated resolution changes, reconnect, non-unit stream_scale, and matching serialized host/client fixtures. The current Android decoder uses its native-size hint rather than these fields, so this finding concerns protocol consistency and other consumers. |
| T267 | open | medium | medium | Resolve authentication-disabled video compatibility: `common/src/model.rs:FileConfig.require_token` exposes an optional token check, but `VideoReceiver.kt:connectAndReceive` always transmits a saved token and `host/src/stream.rs:handle_client` treats those bytes as unexpected input when token=None. A loopback-only fixture compiled from the production stream module with token=None and a 64-byte saved token received the cached header then EOF within one second. Thus switching authentication off after a normal authenticated session breaks current Android streaming; launching MainActivity without a token does not clear Prefs.hostToken. Resolve by defining and implementing authentication-mode negotiation or compatible optional-token handling without weakening enabled authentication. Required permanent T267 coverage: host/client transitions enabled→disabled→enabled with persisted tokens, legacy tokenless clients when disabled, rejection of wrong/missing tokens when enabled, and idle disconnect cleanup from T139. No real daemon, display or tablet was used for the reproduction. |
| T259 | blocked | medium | medium | Blocked on the bounded-versus-explicitly-uncapped VAAPI policy and validation on a supported driver. Resolve the VAAPI bitrate-ceiling contradiction: the configuration/UI offer a bitrate limit, but both `host/src/capture.rs:encoder_quality_args` and `host/src/encoder.rs:low_latency_options` force VAAPI CQP. [FFmpeg 6.1 rate-control code](https://github.com/FFmpeg/FFmpeg/blob/n6.1/libavcodec/vaapi_encode.c#L1618-L1624) marks CQP as using neither bitrate nor maxrate and bypasses bitrate control parameters for it. The configured ceiling therefore does not constrain this path; common/src/lib.rs and docs/development.md now document that limitation, but the UI/policy remains unresolved. Unblock by selecting a driver-supported bounded rate-control policy (with explicit handling of unsupported modes), or documenting and exposing an intentionally uncapped mode. Required permanent T259 coverage: CLI/in-process option parity and capability fallback fixtures, plus real VAAPI high-detail output measurement against the chosen cap. No GPU/device workload was run during this review. |
| T252 | open | medium | medium | Enforce the runtime-directory privacy contract: `common/src/linux/runtime.rs:runtime_dir` requests a private directory on creation but trusts an existing directory (including a symlink), never checks its owner/mode and ignores creation errors. SECURITY.md now explicitly describes this limitation; it does not enforce privacy. Isolated reproduction: pre-create `$XDG_RUNTIME_DIR/uscreen` with mode 0755 and run the existing `runtime_dir_is_private` test with that temporary XDG_RUNTIME_DIR; it fails with group/world permissions 055. No host runtime directory was changed. Resolve with an explicit validation/error contract for existing directories, rejecting unsafe ownership/symlinks and propagating creation failures while preserving documented fallbacks. Required permanent T252 child-process fixtures: existing 0755/0777 directories, safe 0700 reuse, symlink/wrong-owner paths, unwritable base and successful fallback; verify token/FIFO creation only occurs in an accepted private directory. |
| T250 | blocked | medium | small | Resolve the fork release-signing contract: `android/app/build.gradle.kts` accepts any supplied release keystore, but an official fork signing identity and migration from upstream have not been established. SECURITY.md and docs/development.md describe this as pending and no longer assert the upstream fingerprint as the fork identity. A developer-generated key alone does not establish an official update contract. Unblock with the maintainer's chosen fork certificate and update/migration policy, then align documentation and, if an official certificate is required, verify its public fingerprint before publishing. Documentation-only changes need no artificial test; a publication gate needs a permanent wrong-certificate regression. |
| T222 | blocked | high | large | Diagnose and prevent the Xorg crash on EVDI attachment under Cinnamon. On 2026-09-17 at 01:52:40 CEST, Xorg faulted at address `0x20` through `ProcRRSetCrtcGamma`, immediately after `host/evdi/evdi_helper.c:1361` attached the virtual display; the later helper restart did not precede the fault. Blocked on identifying the failing Xorg state with a symbolized core or isolated reproducer and validating a mitigation. Required permanent coverage: isolated Xorg/EVDI attach, gamma update and detach without server/session loss; current fake-helper tests cannot exercise the system display server. See [incident evidence](docs/reviews/2026-09-17-cinnamon-restart.md). |
| T226 | open | high | medium | Make partial FIFO writes recover at a verified frame boundary. `host/evdi/evdi_helper.c:719-740,767-775` claims closing the FIFO resynchronizes it, while `host/src/encoder_io.rs:19-23` requires the reader to observe EOF; immediate reopening can prevent that. Isolated production-code reproduction: stall a capacity+64-byte frame until timeout, reopen before the reader drains, then send the next frame; the reader receives 1,048,576 old bytes plus 64 new bytes without EOF. Resolve with coordinated reader retirement/restart or explicit framing. Required permanent T226 regression: real FIFO, partial write, stalled reader and immediate reopen; neither encoder path accepts a mixed frame. |
| T229 | open | medium | medium | Resolve bundled-library versus installer dependency requirements. `docs/installation.md:Linux artifacts and prerequisites` distinguishes bundled libevdi from the source/system-library route in docs/development.md, but `scripts/install.sh:62-65` includes libevdi in both ffmpeg/adb install transactions. Mocking unavailable libevdi reproduces failure of both transactions, leaving the required tools uninstalled even for a bundle carrying libevdi. Resolve by separating runtime tools, source-build headers/library and kernel setup; make `check_deps` recognize bundled and `/usr/local` libraries. Required permanent T229 fixtures: Debian without libevdi packages, source checkout and prebuilt bundle. |
| T230 | open | medium | medium | Resolve distro-agnostic installation versus Fedora-only repository setup for RHEL/CentOS (`scripts/install.sh:18-49,104-109`). Reproduction: call `install_distro_deps rhel` with `%fedora` undefined; it requests `free/fedora/rpmfusion-free-release-%fedora.noarch.rpm`. Resolve by selecting dependencies/repository guidance for the actual distro and version, with a clear manual path for unsupported package managers. Required permanent T230 command fixtures: Fedora, immutable Fedora, RHEL/CentOS derivatives and unknown IDs; never apply Fedora repository URLs to other families. |
| T231 | open | medium | large | Resolve the requested distro-agnostic behavior versus systemd-only autostart. `scripts/install.sh:198-206,288` swallows failed service installation/enabling and promises startup at login; `gui/src/main.rs:13-30` only invokes systemctl, and `Makefile:43-47` reports service success after ignored errors. On a host without a systemd user manager, direct launch can work but autostart cannot. Resolve with detected service-manager capabilities and a supported desktop-autostart fallback, accurate setup results and matching module/uinput permission guidance. Required permanent T231 fixtures: working systemd, absent systemctl and unavailable user bus; preserve direct start/stop. |
| T233 | open | medium | medium | Resolve inconsistent XDG installation paths (`scripts/install.sh:8,187,198-200`; `Makefile:38-44`; `common/src/storage.rs:config_path`). Config and script-installed icons honor XDG overrides, but launchers and user units go under fixed HOME paths. Reproduce with custom XDG_DATA_HOME/XDG_CONFIG_HOME: the launcher/unit is absent from the configured location, although its icon/config is there. Resolve by sharing the base-directory policy across both installers and documenting any intentional fixed locations. Required permanent T233 regression: isolated custom XDG directories, paths with spaces, and unset-variable defaults. |
| T242 | open | low | small | Align fake-tablet token discovery with the host runtime-directory contract (`scripts/fake-tablet.py:14-16,114-116`; `common/src/linux/runtime.rs:14-36`). With XDG_RUNTIME_DIR pointing to a nonexistent directory, the script uses it verbatim while the daemon falls back; without `/run/user/<uid>`, only the daemon uses HOME/.cache. The advertised fake-tablet workflow then fails opening the token. Resolve by using matching fallback rules or accepting an explicit runtime/token path. Required permanent T242 fixtures: valid/invalid XDG_RUNTIME_DIR and absent user runtime directory, asserting both sides select the same token. |
| T395 | open | high | medium | Investigate Android cold-start foreground-service timeout. On the RugKing Pad 2 Pro running Android 16, the freshly installed debug APK from 29b8068 crashed at 2026-09-17 12:25:50 CEST with ForegroundServiceDidNotStartInTimeException for StreamingService (PID 9152), after MainActivity and host token delivery launched together. The next process (9276) connected successfully and reported isForeground=true. MainActivity.onStart requests startForegroundService, onStop stops it, and StreamingService promotes only in onStartCommand; the exact failing lifecycle sequence is not yet established. Reproduce with controlled cold start, token delivery and rotation/background transitions; preserve timestamped lifecycle evidence. Add a permanent failing lifecycle regression before fixing the demonstrated cause, covering prompt foreground promotion, cancellation/re-entry and lock release. Do not treat a successful retry as resolution. |
