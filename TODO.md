# TODO

## Open items

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| T492 | open | medium | medium | Implement coordinated, profile-aware idle capture cadence after the [live sparse checks](docs/benchmarks/2026-09-19-idle-cadence.md). One versus five local H.264 updates/s improved net charging by about 40.1 mA in T419, but live one-update/s encoding produces roughly two-second keyframe gaps; H.264 High also rises from about 224 ms to 1027 ms raw-write-to-callback-ACK delay, while Constrained Baseline stays near 23–26 ms. Keep the production 200 ms interval until idle opportunities and CLI wall-time IDR deadlines are coordinated, with conservative admission/fallback for unknown or changed encoder/decoder/stream selections. Add permanent failing timing/queue regressions before runtime changes: fresh damage just before deadlines, immediate wakeup, startup/timestamp rounding, final packet flush, long pauses, late joins, slow clients, reconnect, optional in-process IDR requests, Android watchdogs and selection health. Preserve motion responsiveness and user-controlled FPS; keep any cadence control on the host. Then measure complete capture/encode/USB/presentation and sustained battery behavior in an available device window without deliberately reattaching EVDI through T222. The completed isolated replay bypasses helper capture and production stream admission; its local battery and ACK results are not full-pipeline gains. |
| T418 | open | medium | large | Integrate an experimental shared-memory raw-frame transport using T389’s memfd-ring measurements and [ownership design](docs/benchmarks/2026-09-18-raw-input.md#backend-design-and-limits). Convert directly into bounded shared slots and wrap read-only slot leases in stock libavcodec AVBufferRef/AVFrame ownership so encoder-retained references delay reuse; sending mapped pixels through another FIFO would retain the main copy boundary. Implement explicit descriptor handoff, validated version/format/dimensions/strides/size/sequence/generation, shrink/grow seals, acquire/release state transitions, cancellation and peer-crash/resize recovery. Preserve the stock-FFmpeg CLI/FIFO fallback and optional VAAPI rejection. Require permanent retained-frame, malformed-descriptor, stale-generation, full-ring, partial-startup, crash and mode-change tests before enabling, followed by actual conversion/encode/display latency on the existing tablet at 60 FPS; isolated host concurrency measurements may supplement this without requiring a multi-tablet or large-machine campaign. The synthetic prototype uses memset and sampled-byte verification, not capture, full encoder reads, USB or physical display; its throughput gains are not end-to-end gains. This supplies the uncompressed local-transport comparison for T419, whose independent compression research can start in parallel. |
| T250 | open | medium | medium | Integrate the designated fork identity `io.github.geraldo_netto.uscreen` and its provisioned permanent release key; the public certificate, verified backup and remaining migration contract are in [release signing](docs/release-signing.md). Update android/app/build.gradle.kts, host package/component discovery and launch targets, release tooling and documentation consistently. Current builds still use `com.uscreen` and generic supplied keystores. Preserve upstream source attribution; the Kotlin namespace need not change solely because the application ID changes. Install the fork alongside existing com.uscreen apps; document settings migration without claiming an in-place update or automatically removing the old app. Add permanent package-targeting and wrong-certificate publication-gate coverage before behavioral changes, and verify the designated public fingerprint on the actual release APK before publishing. Keep all private signing material outside Git and retain the provisioned key for future fork updates. |
| T445 | open | medium | medium | Limit enabled `auto_launch` to a fresh tablet attachment; require manual reopening after Android process loss, including a crash or background eviction. Remove periodic process-recovery Activity launches from `host/src/main.rs:recover_app` / `host/src/monitor.rs` while preserving initial attachment launch and explicit user launches. Route retries, connection revalidation and transport migration of the same attachment must not count as a fresh launch request. The current recovery policy relaunches any missing process and can steal focus while another app is foreground; T420 only removed token-retry Activity launches. Add permanent failing host/lifecycle regressions for initial attachment, process loss, background use, retries and explicit reopening before changing behavior, then align auto-launch settings/help with the new contract. Implement before T444 so credential rotation uses the chosen launch policy. |
| T444 | open | medium | medium | After T445 establishes attachment-only auto-launch, rotate attachment-specific credentials for both video and input on each new logical attachment/slot replacement. Retire stale routes before reusing a slot and reject late connections carrying retired credentials; explicitly document that tokenless mode cannot provide equivalent attachment isolation. T413 repaired a transient package-query handoff, but `monitor.rs:change_primary` still leaves old reverse mappings and per-run credentials after legitimate replacement, allowing a reconnect through an old route to obtain the replacement epoch. Add permanent failing regressions with two fake devices and real isolated control sockets before changing admission; no second physical tablet is required. Cover both channels, credential delivery/retry, route retirement, reconnect and USB/Wi-Fi migration while preserving accepted-socket generation checks. Credential delivery must respect T445 and must not reopen an Activity after process loss or steal focus from another app. |
| T321 | open | low | medium | Reject fixed-porch display timing combinations below a 10 MHz DTD pixel clock consistently across settings/config sanitation, tablet negotiation and both EDID generators; document this as a conservative supported-timing boundary, not proof of a kernel/display failure. `common/src/model.rs:FileConfig::sanitize` currently accepts 640x480@10, `host/src/capture.rs:helper_command` uses that refresh for the display, and both generators produce a 4.14 MHz DTD that [edid-decode rejects](https://android.googlesource.com/platform/external/edid-decode/+/refs/heads/android15-qpr1-s5-release/parse-base-block.cpp). Reject unsupported requests with a clear explanation and supported higher-refresh alternative, including a documented compatible fallback for invalid saved/negotiated modes. Do not add separate display/stream FPS controls for this item. Add permanent failing coverage before behavior changes: smallest geometry at minimum FPS, cases around the 10 MHz boundary, generator parity and consistent rejection/fallback paths. Any compositor-acceptance claim still requires an isolated compositor test; do not deliberately reattach the active desktop. |
| T308 | open | medium | large | Implement the [AppImage packaging plan](docs/appimage-plan.md), replacing the Debian .deb release asset for simpler distribution; use the approved GitHub identity Geraldo Netto / https://github.com/geraldo-netto instead of inventing a maintainer email. Build from the portable Linux bundle, package the GUI/daemon/helper and required userspace dependencies (including stock FFmpeg and ADB where practical), and retain replaceable libevdi plus required notices. Verify actual dependency closure rather than claiming everything is self-contained: EVDI kernel/DKMS setup, GPU drivers and uinput/USB permissions remain host integration steps. Provide stable daemon/service and helper paths despite the temporary read-only AppImage mount, handle FUSE-unavailable extraction, and document migration/updates from installed .deb packages without automatically uninstalling them. Update packaging/build-packages.sh, scripts/publish-release.sh, checksums, release inventory, CI, distribution tests and download/install documentation together. Keep existing RPM/Arch/tar formats unless separately changed; correct retained Arch maintainer attribution to the GitHub identity while preserving upstream authorship. Add automated launch/dependency, path-with-spaces, service lifetime and release-inventory coverage; perform isolated packaging smoke checks without reattaching the active desktop. The previous Debian email blocker is superseded by this selected packaging change. |
| T480 | open | low | medium | Implement optional persistence of measured profile selections, reusing T479 measurement provenance and respecting the current automatic-selection and explicit-preference contracts. Scope saved entries to the tablet identity/capabilities and relevant host/software/stream/transport conditions; keep persistent identity distinct from transient attachment/selection epochs and coordinate with T444. Invalidate stale or incompatible entries, honor manual choices, retain safe fallback and require current-peer compatibility plus a fresh render check before reuse; a cached historical winner is not proof of the best current latency. Keep this a small convenience feature for the existing setup rather than a prerequisite for automatic selection. Add focused automated persistence, invalidation, interrupted-write and fallback coverage; no physical or simulated multi-tablet campaign or large-machine testing is required. Make no unmeasured cross-tablet performance claims, and let users choose what suits their hardware. |
| T495 | open | medium | medium | Decouple the GUI from Linux platform services after T493, in parallel with T494. GNU and MSVC checks both stop at 11 GUI import errors in `gui/src/main.rs`, `settings.rs` and `status_poll.rs`, although the eframe dependencies pass checking. Route lifecycle, status, runtime paths, autostart and executable discovery through the platform interface; make EVDI/uinput/systemd setup and Linux pipe/conversion controls conditional on actual capabilities. Add permanent service-contract and unsupported-action coverage before changing behavior; check both Windows targets and retain Linux GUI tests. A linked GUI must still be launched and exercised on Windows before claiming support. See [review](docs/reviews/2026-09-19-windows-cross-compilation.md). |
| T496 | open | low | medium | Add Windows-native shared-library test fixtures and a Windows CI gate, extending the portable boundary already checked on WebAssembly. The [cross-compilation review](docs/reviews/2026-09-19-windows-cross-compilation.md) links the common test executable but does not run it. `common/src/commands.rs` unconditionally uses `sh`/`sleep` fixtures and `/proc` absence assertions; `storage.rs:t140_xdg_config_isolation` requires HOME for fallback cases. Replace portable subprocess fixtures with a test executable, keep Linux-specific assertions in the Linux suite, and add real Windows handle/lifetime and configuration-location checks alongside T493. Run the portable policy tests first and the full default-feature suite once fixtures are portable; cover GNU cross-build and native MSVC build/test without publishing these probes as a supported app. Retain all existing regression contracts. No physical tablet is needed for these checks. |
| T497 | open | medium | large | Establish and enforce at least 80% coverage for every maintained production function/method, with invalid-input and out-of-bounds fuzzing as requested for this batch. Use per-function line coverage pending the maintainer's optional branch-coverage clarification; exclude generated code, dependencies, caches/build outputs and test fixtures from production coverage. Measure Rust, C, Kotlin and maintained scripts with their native coverage tools, report uncovered functions explicitly, and add meaningful tests without deleting or weakening existing regressions. Keep platform/device-dependent gaps explicit and skip only on a named unavailable prerequisite, retaining the corresponding TODO item. Add repeatable bounded fuzz targets/corpora for parsers, numeric limits, framing, configuration and lifecycle boundaries, preserve crash cases as permanent regressions, and add the gate to the automated suite. |

## Other items

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| T493 | blocked | medium | large | Complete native Windows validation of the shared platform-service foundation before enabling Windows lifecycle/capture. Configuration paths, executable discovery, owned-handle identity and kill-on-close child jobs are implemented; permanent regressions reproduced relative config paths and escaped descendants before their fixes and now pass in isolated Wine. Native private runtime-directory ACL and single-instance tests remain mandatory: Wine 9 loses the protected-DACL flag and rejects the new directory, so it cannot validate the positive ACL/lease cases. Keep those tests intact in the normal Windows suite and keep capabilities disabled. Unblock with a native Windows runner, validate owner-only ACLs, reparse rejection, single-instance ownership, token privacy and process retirement, fix any demonstrated failure, and satisfy the requested per-function coverage/fuzz gate. Linux behavior and tests must remain intact. See [service validation](docs/reviews/2026-09-19-windows-services.md); T494/T495 may use the separately validated path/discovery services but must report unavailable backends accurately. |
| T222 | blocked | high | large | Analyze the existing Xorg crash core with matching debug symbols before considering an isolated reproducer; do not deliberately reproduce the desktop crash. The 2026-09-17 01:52:40 CEST fault at `0x20` through `ProcRRSetCrtcGamma` followed EVDI attachment and preceded the helper restart. Blocked on readable access to the PID 2392 core and matching Xorg debug information (build ID `2dc631fda11e081777c5d43ba3ad1d6ce8a5a6fd`): `coredumpctl` still reports inaccessible, noninteractive privileged access requires a password, and the prior debuginfod lookup returned 404. Use the evidence to identify the failing state and validate a mitigation. Required permanent fix coverage: isolated Xorg/EVDI attach, gamma update and detach without server/session loss; fake-helper tests cannot exercise the system display server. See [incident evidence](docs/reviews/2026-09-17-cinnamon-restart.md). |
| T388 | blocked | medium | medium | Complete the USB battery comparison before the separate network/pen/power-source experiments in T487. The [low-latency USB attempt](docs/benchmarks/2026-09-18-usb-power-comparison.md) completed one five-minute normal/static phase, then rejected saver/static after Linux keyboard-focus loss; remaining pairs and the sustained streaming-off control did not run. Preferences were restored. T488 fixed the Android foreground-query collector bug with a permanent regression; the remaining prerequisite is an undisturbed Linux host session or an explanation of unexpected focus changes, not merely an idle tablet. Keep installed hashes, codec, geometry, FPS, brightness and refresh fixed; record newer undeployed checkout changes separately. The accepted counter stayed flat over 270 seconds, but coarse 9.99 mAh steps and a missing saver pair establish no power saving. Retry the balanced matrix and control in a 60–90 minute window, extending only if useful and the devices remain available; preserve raw observations, clock boundaries and preferences. Retain USB waiting/background/reconnect observations and conservative unknown-route behavior, without reattaching EVDI through unresolved T222. See the [power-policy report](docs/benchmarks/2026-09-18-power-policy.md) for permanent coverage. |
| T419 | blocked | medium | large | Live lossless RGB integration requires either a repeatable tablet battery advantage over matched reduced-cadence hardware H.264, or a maintainer decision to pursue RGB fidelity/CPU independently of battery. The [presentation and sustained-power follow-up](docs/benchmarks/2026-09-19-presentation-power.md) preserves the completed research and conditional implementation order: RGB pen app CPU fell about 33%, but its battery advantage did not repeat; static one-update/s H.264 charged more strongly than RGB in both rounds. Retain the unresolved bounded wire-format/base/sequence/generation contract, integrity and decompression validation, atomic updates, reconnect/full refresh, renderer retirement and video fallback requirements. Require permanent round-trip, corruption, stale-base, overload and lifecycle regressions before production enablement, then compare actual capture/compression/USB/render latency and sustained CPU, memory, thermal and battery behavior across UI, natural video, transitions, resize and background/resume. Keep callback, compositor and optical boundaries distinct; queued-but-unlatched frames explain the reproduced presentation omissions, not every earlier untraced miss. Preserve RGB/chroma fidelity, account for full refresh and photo/scroll bandwidth, retain bounded reads over whole-fixture mmap, and compare host-only compression separately with T389/T418 shared memory. No FFmpeg patch or multi-tablet/large-machine campaign is required. |
| T414 | deferred | medium | large | Defer further audio/video synchronization investigation unless the lag recurs: the maintainer reports that synchronization now seems correct after the latest optimizations. This is user-observed improvement, not a measured offset/drift result or proof of a particular cause. On recurrence, confirm the YouTube playback app/device, Bluetooth source/pairing and constant-offset versus growing-drift behavior, then measure the current video path on updated builds with a timestamped flash/click clip. Preserve host/APK versions, stream settings and initial/sustained offset, pause/seek, reconnect, overload and recovery observations. The protocol is video-only; codec PTS/ACK IDs are sequence identities, not a shared audio clock. T386 found callback render-time equal to sequence * 1000 rather than comparable System.nanoTime; validate monotonic presentation timing while preserving sequence/ACK identity before interpreting these timestamps as latency. Trace capture/FIFO age, encode, transport, decode and Surface presentation against the actual audio clock; manual audio delay or new audio transport requires separate scope. Any demonstrated-cause fix needs a permanent failing clock/queue regression, and claimed synchronization gains need physical offset/drift measurements. |
| T487 | deferred | medium | medium | Extend T388 power validation after the USB comparison, as separately prioritized experiments: physical network transport, pen interaction and any available higher-power data source. Prepare an isolated route/input/power setup and a suitable uninterrupted device window first; avoid the unresolved T222 desktop-attachment failure and do not require another physical tablet. Preserve conservative unknown-route behavior, preferences, raw observations and measurement boundaries; verify waiting/background/reconnect and actual lock ownership. Compare within each controlled configuration instead of mixing transport, input or supply changes into the USB profile comparison. Report measured charging/discharging separately from advertised USB current capability. Deferred to keep the current battery work focused on USB. |
| T417 | deferred | low | medium | Explicitly requested for later UI work; profile controls versus read-only diagnostics still needs a decision. Follow up on T386 by exposing decoder capabilities and the selected decoder profile in the Android app. Show the active codec identity, hardware-acceleration status, advertised standard low-latency support and supported operating rate for the current codec, dimensions and stream FPS. Distinguish advertised support, requested hints and confirmed active behavior; show unknown when Android cannot verify an effective setting, and identify watchdog fallback. Explain that advertised 2x rate headroom is not measured throughput or a latency guarantee, and that missing standard low-latency support does not by itself imply slow decoding. Use T386's repeated motion/sparse-stream measurements to explain any recommended profile. Before later UI implementation, confirm whether this should include profile-selection controls or read-only diagnostics; preserve current defaults until an explicit, measured policy is chosen. Include permanent tests for supported, unsupported and unknown capabilities, API 27 fallback, codec/stream changes and fallback reporting; if controls are added, also cover persistence and safe session transitions. Requested as a future follow-up, outside the current implementation batch. |
