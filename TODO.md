# TODO

## Open items

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| T486 | open | low | small | Align `host/src/input/wire.rs` render-event comments with the measured contract: `Rendered` currently says the tablet has put a frame on screen, while `DecoderSession::notifyRendered`, `latency.rs` and `docs/video-codecs.md` measure render-callback execution/ACK receipt without optical presentation proof. Describe `decode_us` as complete-frame arrival to callback execution, retain the distinct clock boundaries and do not imply independent percentiles can be subtracted for transport latency. Documentation-only correction; no artificial regression test. |

## Other items

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| T222 | blocked | high | large | Diagnose and prevent the Xorg crash on EVDI attachment under Cinnamon. On 2026-09-17 at 01:52:40 CEST, Xorg faulted at address `0x20` through `ProcRRSetCrtcGamma`, immediately after `host/evdi/evdi_helper.c:1361` attached the virtual display; the later helper restart did not precede the fault. Blocked on identifying the failing Xorg state with a symbolized core or isolated reproducer and validating a mitigation. Required permanent coverage: isolated Xorg/EVDI attach, gamma update and detach without server/session loss; current fake-helper tests cannot exercise the system display server. See [incident evidence](docs/reviews/2026-09-17-cinnamon-restart.md). |
| T395 | blocked | high | medium | Establish a reproducible failing startup sequence or timestamped lifecycle/main-thread trace for the original Android 16 foreground-service timeout. The affected tablet is connected, but [30 device trials](docs/reviews/2026-09-18-foreground-start.md)—12 historical-source isolated starts, 12 current-source isolated starts and 6 installed-app live-host starts—did not reproduce it. Existing early promotion, rejection, cancellation/re-entry and lock-release regressions pass; successful starts do not establish the original cause. Preserve token-delivery order, process identity, service promotion and ApplicationExitInfo on recurrence; add a permanent failing regression before a demonstrated-cause fix. Historical source in a separate package does not recreate the original fresh-install/authenticated-stream state. |
| T388 | blocked | medium | medium | Complete sustained power validation in an uninterrupted unlocked Linux session with the tablet available for the full balanced normal/profile static/motion matrix and streaming-off control. The [guarded attempt](docs/benchmarks/2026-09-18-power-validation.md) completed three five-minute phases, then correctly rejected normal-motion after keyboard-focus loss; repeated pairs and the sustained control did not run. Original preferences were restored. Physical lock ownership matches policy, but coarse 9.99 mAh charge steps and incomplete pairs establish no battery-saving gain. Retain waiting/pen/background/reconnect observations, available network transport and any available higher-power data-source comparison. Only USB ADB was available; physical network/pen validation needs a prepared isolated route/setup that avoids the unresolved T222 desktop-attachment failure. Preserve preferences, conservative unknown-route behavior, raw observations and clock/measurement boundaries. The [implementation report](docs/benchmarks/2026-09-18-power-policy.md) retains permanent regressions and prior lifecycle checks; T395 remains independently blocked on a reproducible cause. |
| T250 | blocked | medium | small | Resolve the fork release-signing contract: `android/app/build.gradle.kts` accepts any supplied release keystore, but an official fork signing identity and migration from upstream have not been established. SECURITY.md and docs/development.md describe this as pending and no longer assert the upstream fingerprint as the fork identity. A developer-generated key alone does not establish an official update contract. Unblock with the maintainer's chosen fork certificate and update/migration policy, then align documentation and, if an official certificate is required, verify its public fingerprint before publishing. Documentation-only changes need no artificial test; a publication gate needs a permanent wrong-certificate regression. |
| T414 | blocked | medium | large | Requires confirmation of the playback app/device, Bluetooth pairing/source and constant-offset versus growing-drift behavior, followed by the affected tablet for physical measurement. Investigate and improve audio/video synchronization: the user reports YouTube video appearing slightly behind audio on an older UScreen build. The user describes the audio setup as “bluetooth headset with a tablet”; verify the Bluetooth source/paired device and whether YouTube runs on Linux through UScreen or directly on Android. Record exact host/APK versions, stream settings and whether the offset is constant or grows over time; distinguish the reported older build from the subsequently deployed candidate. Reproduce with a timestamped flash/click clip and measure initial offset, sustained drift, pause/seek, reconnect, overload and recovery. Trace browser/compositor capture, raw FIFO age, encoder buffering, transport, Android decode and Surface presentation against the actual audio clock; larger pipes can hide stale video. `host/src/stream.rs` and `android/.../DecoderSession.kt` use sequence IDs as codec presentation timestamps for ACK identity, not a shared media clock, and the current protocol has no audio stream. T386 physical traces found the callback render-time argument equal to `sequence * 1000` rather than a comparable `System.nanoTime()` value; the boolean output-release API propagates input PTS to Surface timestamps. Preserve these raw traces and compare explicit monotonic output-release timestamps while retaining sequence-based ACK identity before interpreting render-time differences as latency. Separate latency reduction from any proposed audio-delay or synchronized-audio feature; retain sequence/ACK identity if real media timestamps are added. Coordinate T382/T386/T389/T399/T405. Before fixing a confirmed cause, add a permanent failing deterministic clock/queue regression, then prove it passes; require physical A/V offset/drift measurements for claimed playback improvements. |
| T422 | blocked | medium | medium | Find a compatible stock encoder/configuration or validated driver/firmware correction for AMD VAAPI HEVC → Unisoc native error 14. [26 controlled device trials](docs/benchmarks/2026-09-18-hevc-interop.md) establish 15 failed VAAPI cases versus 11 successful x265 controls; alternate CSD delivery, disabled decoder hints, ten stock VAAPI variants and removal of optional AUD/SEI do not fix it. All 20 archived fixtures yield 240 software frames, but four VAAPI variants emit decode errors and are excluded as clean interoperability controls; the original failing VAAPI fixture is software-clean. T461 now rejects decoder errors even when FFmpeg returns zero. T458/T460 repaired the replay prerequisites; valid trials preserve source/APK hashes, parameter sets and native logs. Blocked on a working stock configuration or a trace isolating the native failure before a cause-specific change; do not patch FFmpeg or rewrite undocumented bitstream fields. Add a permanent failing regression for the demonstrated cause and verify the physical tablet before closing. Preserve H.264 fallback and distinguish advertised HEVC capability from render-verified success. |
| T429 | blocked | medium | medium | Complete diagnosis of the low-latency selection returning to searching before streaming resumes. Blocked on confirmation of the exact original option, timestamped host/Android transition evidence and an isolated display setup avoiding T222. The [isolated investigation](docs/reviews/2026-09-18-low-latency-transition.md) verifies profile persistence, one post-save restart callback, error reporting, stock profile flags and switch-back; it does not reproduce the physical delay. The Linux encoder selector restarts the full daemon, so a reconnect is expected, but repeated recovery and the resumed profile remain unverified. Compare normal/low-latency/normal with identical resolution/FPS and other settings; record build hashes, GPU/driver/FFmpeg, command/headers, capture ownership, reconnects, decoder setup and first render ACK. Preserve the working default. Add a permanent failing regression for any demonstrated cause before fixing, then verify playback on the affected device. |
| T444 | blocked | medium | medium | Choose the attachment-admission contract, then retire stale routes before reusing a slot. Robust isolation needs attachment-specific credentials on both input and video; current per-run credentials and tokenless legacy clients cannot identify which tablet a late TCP connection came from. Unblock by choosing credential rotation with explicitly unisolated no-auth mode, or preserving shared-token compatibility with route/listener quarantine. T413's transient package-query repair prevents its erroneous handoff, but `monitor.rs:change_primary` still leaves old reverse mappings and a shared token after legitimate replacement; a newly accepted reconnect through an old route obtains the replacement epoch. Reproduce with two fake devices and real isolated control sockets, then enforce route retirement or attachment-specific authentication before admitting the replacement. Preserve accepted-socket generation checks, USB/Wi-Fi migration and explicit/manual launch behavior. |
| T445 | blocked | medium | medium | Choose the `auto_launch` policy after Android kills UScreen while another app is foreground. T420 removes token-retry Activity launches, but `host/src/main.rs:recover_app` deliberately launches a missing process when `auto_launch` is enabled; process absence cannot distinguish a streaming crash from background eviction. The request to allow use of another app conflicts with unconditional process-recovery launches in that case. Unblock with the desired distinction between launch on attachment, recovery of an active stream and background eviction, then add host/lifecycle regressions before changing the established auto-launch behavior. |
| T321 | blocked | low | medium | Blocked on the supported low-clock timing contract and isolated compositor acceptance evidence. Resolve accepted low-resolution/low-refresh settings versus EDID validator compatibility. `common/src/model.rs:FileConfig::sanitize` accepts 640x480@10, `host/src/capture.rs:helper_command` uses that refresh for the display, and both EDID generators accept it with a 4.14 MHz DTD clock. Offline edid-decode --check rejects that generated descriptor with Pixelclock < 10 MHz, assuming invalid data; the [primary parser](https://android.googlesource.com/platform/external/edid-decode/+/refs/heads/android15-qpr1-s5-release/parse-base-block.cpp) contains that threshold. This proves a validator incompatibility, not a reproduced kernel/display failure. Unblock by selecting and documenting the accepted timing/consumer contract: generate suitable timings while retaining the requested mode, or restrict incompatible combinations consistently across config, negotiation and both generators. Required permanent T321 coverage: smallest supported geometry at minimum FPS, cases around the selected pixel-clock boundary, generator parity and chosen rejection/fallback behavior; any display acceptance claim needs an isolated compositor test. No host display was attached. |
| T308 | blocked | low | small | Resolve fork package-maintainer metadata: `README.md:8-9` and `docs/index.html:44,431` identify geraldo-netto as the fork maintainer and majmichu1 as the original author, while `packaging/deb/control:8` and `packaging/arch/PKGBUILD:1` still identify majmichu1 as the package maintainer. `packaging/build-packages.sh` copies that Debian field into fork releases without replacement. Unblock with the intended fork package-maintainer name and public contact address, then align Debian and Arch metadata while preserving upstream authorship and historical attribution. No email address should be inferred from a username or local Git identity. Metadata-only correction needs no artificial regression test. |
| T418 | deferred | medium | large | Production integration was left outside the requested implementation batch; retain this as the separate follow-up experiment. Follow up on T389's isolated memfd-ring measurements with a production raw-frame transport experiment. Convert directly into bounded shared slots and wrap read-only slot leases in stock libavcodec AVBufferRef/AVFrame ownership so encoder-retained references delay reuse; sending mapped pixels through another FIFO would retain the main copy boundary. Implement explicit descriptor handoff, validated version/format/dimensions/strides/size/sequence/generation, shrink/grow seals, acquire/release state transitions, cancellation and peer-crash/resize recovery. Preserve the stock-FFmpeg CLI/FIFO fallback and optional VAAPI rejection. Require permanent retained-frame, malformed-descriptor, stale-generation, full-ring, partial-startup, crash and mode-change tests before enabling, followed by actual conversion/encode/display latency at 60 FPS and 1/2/4 streams. The synthetic ring uses memset and sampled-byte verification, not capture, full encoder reads, USB or physical display; its throughput gains are not end-to-end gains. Keep this follow-up outside the current implementation batch. |
| T419 | deferred | medium | large | Explicitly requested as later compression research, separate from the implemented video-codec work. Research alternative frame compression for a future desktop-streaming mode, separately from T400's video-codec comparison. Compare full frames, changed rectangles/tiles and temporal deltas using fast lossless LZ4 or low-level Zstandard, with QOI/PNG or intra-image coding as controls, against hardware-decoded H.264/HEVC. Evaluate a hybrid only if static text/pen content benefits and switching to motion has bounded latency without quality flashes or stale pixels. Preserve original RGB/chroma detail when measuring lossless text fidelity; compressing NV12 cannot recover already discarded chroma. Separate host-only raw-frame IPC compression from wire-to-Android compression, using T389/T418 direct/shared-memory transport as the local baseline. Prioritize capture-to-display p50/p95/p99, motion recovery, bandwidth, Android decode/composition/upload cost, sustained battery/thermal behavior and 1/2/4-stream scaling; ample host CPU/RAM does not establish a latency or Android power gain. Measure any second compression of already encoded video as a control rather than assuming savings. Specify capability negotiation, tile/base-frame identity, atomic presentation, bounded decompressed sizes, loss/reconnect/full-refresh recovery and fallback; require permanent round-trip, corruption, stale-generation and lifecycle tests before enabling. Start from [LZ4](https://github.com/lz4/lz4) and [Zstandard](https://github.com/facebook/zstd) primary documentation; corpus benchmarks do not predict desktop-frame results. Coordinate T399/T400/T403/T414. Requested as later research, outside the current implementation batch. |
| T417 | deferred | low | medium | Explicitly requested for later UI work; profile controls versus read-only diagnostics still needs a decision. Follow up on T386 by exposing decoder capabilities and the selected decoder profile in the Android app. Show the active codec identity, hardware-acceleration status, advertised standard low-latency support and supported operating rate for the current codec, dimensions and stream FPS. Distinguish advertised support, requested hints and confirmed active behavior; show unknown when Android cannot verify an effective setting, and identify watchdog fallback. Explain that advertised 2x rate headroom is not measured throughput or a latency guarantee, and that missing standard low-latency support does not by itself imply slow decoding. Use T386's repeated motion/sparse-stream measurements to explain any recommended profile. Before later UI implementation, confirm whether this should include profile-selection controls or read-only diagnostics; preserve current defaults until an explicit, measured policy is chosen. Include permanent tests for supported, unsupported and unknown capabilities, API 27 fallback, codec/stream changes and fallback reporting; if controls are added, also cover persistence and safe session transitions. Requested as a future follow-up, outside the current implementation batch. |
| T480 | deferred | low | medium | Optional future persistence of measured profiles for different tablets. The maintainer has one tablet and considers cross-tablet profile storage a nice-to-have; do not implement it or add a physical/simulated multi-tablet test campaign now. Revisit only when prioritized. If resumed, retain T479 measurement provenance, bind cache entries to tablet capabilities and relevant host/software/stream/transport conditions, invalidate stale entries, honor manual choices and keep a safe fallback. This deferral does not block automatic profile selection for the connected tablet (T479) and must not revive the declined T382 scaling campaign. |
| T382 | wont_fix | medium | medium | Full multi-tablet performance/scaling campaign declined for now. Maintainer testing with multiple physical tablets or a larger/NUMA machine is outside the current scope. Provide configurable options with sensible defaults and documented tradeoffs; users decide what is worthwhile for their hardware. Retain existing single-tablet/synthetic evidence and automated correctness/lifecycle coverage without claiming unmeasured physical scaling. T474 separately tracks the explicit Linux conversion-capacity setting. |
