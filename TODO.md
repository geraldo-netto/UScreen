# TODO

| id | status | severity | effort | description |
|---|---|---|---|---|
| T098 | open | medium | small | Do not certify HEVC support solely from host configuration (`host/src/doctor.rs::check_tablet`): `(false, false, true)` still prints HEVC OK, even when the tablet report contains no HEVC decoder. Repro: select hevc_nvenc with empty/failed codec output. Regression: mocked hardware/software/absent/unknown codec reports produce evidence-based results and no unsupported Main10 recommendation. |
| T099 | open | medium | medium | Diagnose the daemon's actual tablet/session (`host/src/doctor.rs::check_tablet`): doctor picks the first USB entry, while daemon can retain another device or prefer the one with com.uscreen; forwarding checks always use primary ports. Repro: charging phone precedes active tablet, or diagnose a second slot. Regression: multiple devices/transports and custom slot ports target the actual active session. |
| T128 | blocked | low | small | Resolve source-build/release instructions: docs/development.md omits the libevdi link dependency required by Makefile and says bump both Cargo manifests although publish-release.sh checks three. Unblock with complete source prerequisites and version-file list. Documentation-only; validate instructions against a clean source-build environment. |
| T129 | blocked | medium | small | Include referenced license notices in distributed artifacts (`scripts/build-release.sh`, `Makefile::dist-local`, package recipes): bundled README links LICENSE and THIRD_PARTY_LICENSES.md, but tarball copies neither; deb/rpm also omit these notices despite bundling libevdi. Unblock by defining and shipping the license/notice file set consistently. Regression: inspect tar/deb/rpm contents for the declared notices and resolve README's local links. |
