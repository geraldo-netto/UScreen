# TODO

| id | status | severity | effort | description |
|---|---|---|---|---|
| T361 | blocked | low | small | Align docs/assets/README.md and site metadata with actual assets: the asset README says no image is referenced, while docs/index.html already embeds an SVG and references a generated GitHub social image; Pages deployment is unverified. Unblock by documenting these existing assets, prospective photographs and deployment preconditions, without claiming a generated preview is a real screenshot. |
| T360 | blocked | low | small | Clarify developer/release instructions against Makefile, build scripts and CI: make install reloads but does not enable/start the user service, source builds need a runtime libevdi, alternative Cargo output paths remain broken (T336), some CLI flags are missing, and packaging success is not hardware validation. Unblock with complete documented prerequisites, accurate source/release workflows, and explicit existing limitations; no real installation or publishing during review. |
| T359 | blocked | low | small | Establish a private reporting channel: SECURITY.md promises GitHub private reporting or email from the maintainer profile and a response within days; CODE_OF_CONDUCT.md directs private reports through a profile without a concrete channel. Fork private-vulnerability-reporting/contact availability and response commitment are not established. Unblock when the maintainer supplies a verified private contact or enables private vulnerability reporting and chooses any response commitment; do not invent an address or publish sensitive reports. |
| T358 | blocked | low | small | Correct README.md roadmap and related summaries: inherited AOA/GNOME/PipeWire ordering is stated as a committed fork schedule, and iPad distribution claims conflate iOS with iPadOS and overgeneralize regional availability. Unblock by labeling unapproved work as proposals, linking the saved Windows plan and removing unsupported scheduling/distribution assumptions. |
| T357 | blocked | low | small | Align CONTRIBUTING.md and GitHub issue/PR templates with AGENTS.md: require TODO recording, blocked contradictions, one commit per resolved finding, permanent failing-then-passing behavior regressions and cyclomatic complexity at most 9; current templates imply narrower tests and guarantee every report is accepted. Unblock with consistent guidance and safe log collection that does not start a second daemon. |
| T356 | blocked | low | small | Update README.md, docs/llms.txt, docs/index.html, docs/development.md and CHANGELOG.md for current fork settings and feature scope: 50 percent app-only brightness/60 Hz preference are absent from most summaries, encoder choice is not automatic or universally available, rate-control limits vary by encoder (T259), and multi-tablet/geometry promises exceed validated coverage (T330/T332). Unblock by documenting actual defaults, control scope and limitations without inventing runtime fixes. |
| T355 | blocked | low | small | Align docs/architecture.md with stream.rs, input.rs and Android receivers: authentication is configurable, protocol lengths and deadlines are incomplete, example greeting uses non-JSON notation, MediaCodec does not guarantee hardware decoding, and input devices are configurable. Whole-frame FIFO and greeting claims also need explicit T226/T247 limitations. Unblock with exact wire framing and qualified implementation descriptions; retain behavioral bugs. |
| T354 | blocked | low | small | Revalidate comparison claims in README.md, docs/faq.md, docs/llms.txt and docs/index.html: old matrices categorically deny direct USB to spacedesk and stylus capabilities to Moonlight, and label software x264 as hardware. Unblock by using current primary sources for a smaller precise comparison, distinguishing prerequisites from supported capabilities and removing unsupported absence claims. |
| T353 | blocked | low | small | Correct benchmark attribution and measurement claims in docs/benchmarks.md, README.md, docs/llms.txt and docs/index.html: upstream results are described as fork guarantees; the stated 1.0–1.1 date window also includes 1.2 follow-ups; wireless/decoder/capture causal explanations and sub-millisecond encoding claims exceed the recorded measurements. Unblock by retaining original numeric results with provenance, version boundaries and uncertainty, and removing unsupported general performance guarantees. |
| T352 | blocked | low | small | Align installation, troubleshooting and compatibility instructions with packaging and host allocation: evdi-dkms is a Debian Recommends, portable artifacts are Linux x86-64 with runtime prerequisites, module/image availability is version-dependent, helper failures have multiple causes, and the daemon can pin a busy card (T330). Docs recommend live modprobe -r despite capture.rs warning about display-server loss (T269), and promise automatic behavior despite T222/T224/T234/T247. Unblock by correcting procedures and making known limitations visible without claiming the behavioral bugs resolved. |
| T351 | blocked | low | small | Correct SECURITY.md, README.md and docs/faq.md security/setup claims against current source: adb tcpip opens a tablet network listener and wifi --off only forgets/disconnects it; token checks are configurable; existing runtime directories are not validated (T252); root execution is not forbidden; source/package installation changes more than the four listed files. Unblock with accurate trust boundaries, path-aware uninstall instructions and qualified signing/update descriptions. Retain unresolved T250/T252/T267 and installer T269; documentation corrections do not remediate those behaviors. |
| T350 | blocked | low | small | Align fork publication metadata across README.md, CITATION.cff, CHANGELOG.md, docs/index.html, docs/llms.txt and release tooling: docs advertise released fork 1.2.3 downloads, whereas GitHub API on 2026-09-17 returns no fork releases, Discussions disabled and Pages 404. Unblock by distinguishing unreleased fork source from upstream release history, replacing unavailable support/download links, and preserving tested release-metadata preparation for a future release. Required permanent coverage if tooling changes: unreleased metadata converts consistently to a release candidate, check mode stays read-only, and existing validation-before-write tests remain passing. |
| T348 | blocked | medium | medium | Resolve runtime-path handling across `common/src/runtime.rs:runtime_dir,fifo_path_for` and `host/src/capture.rs:fifo_path_for,ensure_fifo,encoder_arguments,retire_orphan_capture`. The shared runtime module accepts native PathBuf bytes from XDG_RUNTIME_DIR and creates that directory, but capture converts its FIFO path with to_string_lossy. An isolated production-function probe using a temporary runtime directory with byte 0xFF selects different shared/capture paths; mkfifo then fails with ENOENT. Unblock by choosing and enforcing one path contract before claiming runtime resources: preserve native paths through helper/encoder/cleanup interfaces, or reject unsupported paths explicitly. Coordinate lossless process-argument matching with blocked T245 rather than constructing another lossy pkill expression. Required permanent T348 coverage: non-UTF-8 and ordinary paths, token/FIFO agreement, CLI and in-process encoder arguments, cleanup and isolated failure reporting. No real daemon, device or host runtime state was touched. |
| T344 | blocked | low | small | Resolve pointer-preference handling while the pen is disabled. `gui/src/main.rs:setting_input_devices` says a disabled checkbox must not retain a value the daemon would act on, and clears input_pointer during every render. However, `host/src/input.rs:InjectDevices::create` already gates pointer creation on both pen and pointer, and `FileConfig::sanitize` preserves the dormant preference. An isolated egui probe rendering the production settings method without events changes pen=false/pointer=true to pointer=false and marks the configuration dirty; pen=true preserves it. Unblock by choosing one consistent policy: preserve the dormant pointer preference, or explicitly clear it during a deliberate setting change/shared sanitization, then align the comment and UI. Required permanent T344 coverage: opening the tab without input, disabling/re-enabling the pen, saving/reloading and discarding edits; no host input devices are needed. |
| T339 | blocked | medium | medium | Resolve decoder fallback behavior for repeated one-frame stalls. `VideoReceiver.kt` describes a decoder that renders one frame then stops, and promises to disable low-latency hints after repeated stalls; however, `startOutputThread` resets `outputStalls` on every output frame, while `checkOutputProgress` requires two accumulated stalls. An isolated API 27/34 Robolectric probe runs the production output worker with a codec emitting one frame per restart, then triggers the watchdog after four queued frames and two seconds without output: all three restarts report stalls=1 and hints=true. Unblock by defining what sustained output counts as recovery and retaining the stall history until that condition, so the documented one-frame failure can reach the fallback. Required permanent T339 coverage: repeated zero-frame and one-frame stalls, sustained recovery resetting history, selected fallback format hints, stop/start and codec changes, with controlled clock/codec fixtures and no hardware stream. |
| T338 | blocked | low | small | Resolve the unused Android overlay timer in `MainActivity.kt:UScreenMain`: the comment promises to hide the overlay shortly after connection and a coroutine changes `showOverlay` after three seconds, but no UI reads that state. `ConnectionLayers` instead hides the connection screen directly from `isConnected` (subject to its fade animation). Unblock by confirming the intended overlay timing: retain immediate dismissal and remove the dead state/timer/comment, or wire a deliberate delay into the intended overlay. For a behavior change, add permanent Compose clock-controlled coverage for connection, the selected delay and disconnect during that delay; removing the unused code without changing behavior needs no artificial test. |
| T336 | blocked | medium | medium | Resolve source-build output selection across Cargo, Make and the installer. `Makefile:build` lets Cargo honor its selected target directory, but `scripts/install.sh:install_binaries` and Make run/status/stop/list/dist-local paths always use `target/release`; `scripts/ci/build-artifacts.sh` already selects CARGO_TARGET_DIR explicitly. [Cargo supports alternate output directories through environment/config/flags](https://doc.rust-lang.org/cargo/reference/build-cache.html). An isolated installer fixture with new binaries under CARGO_TARGET_DIR and stale binaries under the default target path returns success and installs the stale daemon/GUI. With no default outputs it instead fails after a successful custom-directory build. Unblock by defining one artifact-location contract for source workflows: resolve and consume the effective Cargo target output, or explicitly force a consistent project output directory for both build and consumers. Preserve prebuilt-tarball selection. Required permanent T336 regressions: absolute and relative target directories, spaces/metacharacters, Cargo configuration override, stale/missing default outputs and unchanged tarball installation; execute build/install/dist dispatch with disposable files/stub tools only. |
| T334 | blocked | medium | medium | Resolve keyboard suppression without recoverable saved state. `host/src/osk.rs:disable` says not to touch the desktop without a way back, but any existing `osk-restore` path bypasses reading/validating the saved mode before `set_mode(0)`. `restore_from` then retains empty/invalid data and cannot restore it. An isolated probe of those production functions with temporary state and a fake KWin interface starts at mode 2: valid saved `2` restores to 2; empty or invalid saved text suppresses to 0 and remains 0 after restoration. A crash between creation and writing, or a failed write, can leave an empty file because persistence uses `std::fs::write` in place. Unblock by defining recovery for unreadable/invalid existing state and requiring a validated, durably saved restore value before changing the live keyboard mode; preserve valid backups from interrupted earlier runs. Required permanent T334 coverage: empty/invalid/unreadable paths, interrupted/failed state writes, valid existing-state reuse, failed D-Bus restoration and subsequent retry. Preserve T111/T148; use temporary paths and a fake or isolated bus, never the active desktop. |
| T332 | blocked | medium | medium | Resolve independently accepted resolution/FPS settings versus the generated EDID pixel-clock limit. `common/src/lib.rs:FileConfig::sanitize`, `input.rs:negotiated_geometry,apply_tablet_config` and both FPS selectors accept 3840x2160@90, but `capture.rs:helper_command` passes that mode to `edid.rs:make_edid_sized`, which rejects its clock above 655.35 MHz before helper startup. An offline probe of the production config/EDID code accepts and generates 3840x2160@60, retains 3840x2160@90 unchanged during sanitization, then fails generation; 4095x4095@60 fails likewise. A live FPS change can therefore replace a working mode with repeated setup failures. Unblock by choosing a shared joint-mode validation/negotiation policy: reject incompatible changes before retiring the current stream, or explicitly negotiate and display a supported resolution/refresh fallback. Apply it to CLI, persisted settings, GUI and tablet updates; do not remove the generator's overflow guard. Required permanent T332 coverage: clock-boundary combinations, valid 4K60→invalid 4K90 update with current-session preservation, automatic/manual geometry and accurate effective settings. Distinct from T321's low-clock consumer warning; coordinate timing choices with T325. No display was attached. |
| T330 | blocked | medium | medium | Resolve multi-tablet card assignment versus EVDI ownership: `host/src/main.rs:run_daemon,assign_slot_card,spawn_extra_session` pins slots to the lowest/all-card enumeration from `vdisplay::evdi_cards`, including cards occupied by other applications. The startup comment says helper discovery would choose the same card for every tablet, but `evdi_helper.c:open_available_device_in` already leases distinct free cards and refuses a pinned busy card. Existing isolated T108 Rust/C fixtures confirm positional assignment plus rejection of connected card 0 while card 2 is free; enabling multi-tablet mode can therefore strand the primary on an external application's card despite spare capacity. Unblock by defining stable per-session allocation from available cards, preserving other applications' outputs and preventing two slots from sharing a card across restarts/device additions/removals. Required permanent T330 coverage: busy lowest card with sufficient free cards, simultaneous slot startup, reconnect/restart, card-set changes and exhausted capacity, with production daemon allocation and fake helper leases in one fixture. Preserve T108's no-stealing and pinned-card guarantees; no real DRM operation was performed. |
| T328 | blocked | medium | medium | Resolve command-timeout scope in `common/src/commands.rs` versus delegated operations such as `gui/src/main.rs:run_system_setup` (`pkexec sh -c ...`). Sync/async timeout paths kill only the direct child, so reporting timeout does not stop descendants from continuing the operation. An isolated probe of production SyncCommandExt with a disposable shell, a 100 ms deadline and a worker that writes after 300 ms reports timeout while the worker remains alive and subsequently writes; the probe reaps its own descendant and touches only a temporary directory. Unblock by defining and implementing cancellation of delegated work, including privileged descendants that the unprivileged caller cannot signal, or explicitly narrowing the operation/deadline contract and exposing continued work accurately. Required permanent T328 coverage: sync timeout, async timeout/cancellation, descendant retirement and no writes after completed cancellation, successful-command behavior and isolated privileged delegation if retained. Preserve T094's direct-child reaping assertions; no host setup or privileged process was exercised. |
| T321 | blocked | low | medium | Resolve accepted low-resolution/low-refresh settings versus EDID validator compatibility. `common/src/lib.rs:FileConfig::sanitize` accepts 640x480@10, `host/src/capture.rs:helper_command` uses that refresh for the display, and both EDID generators accept it with a 4.14 MHz DTD clock. Offline edid-decode --check rejects that generated descriptor with Pixelclock < 10 MHz, assuming invalid data; the [primary parser](https://android.googlesource.com/platform/external/edid-decode/+/refs/heads/android15-qpr1-s5-release/parse-base-block.cpp) contains that threshold. This proves a validator incompatibility, not a reproduced kernel/display failure. Unblock by selecting and documenting the accepted timing/consumer contract: generate suitable timings while retaining the requested mode, or restrict incompatible combinations consistently across config, negotiation and both generators. Required permanent T321 coverage: smallest supported geometry at minimum FPS, cases around the selected pixel-clock boundary, generator parity and chosen rejection/fallback behavior; any display acceptance claim needs an isolated compositor test. No host display was attached. |
| T318 | blocked | medium | medium | Resolve Android hover lifecycle versus Linux pen proximity in `TouchCapture.kt:handleHoverEvent,setSurfaceView,sendPenProximityExit` and `host/src/input.rs:UInputDevice::inject_pen`. Android ACTION_HOVER_EXIT is forwarded as action=4 (leave proximity), but [AOSP dispatchHoverExit](https://android.googlesource.com/platform/frameworks/native/+/refs/heads/main/services/inputflinger/reader/mapper/TouchInputMapper.cpp) emits it when a hovering stylus touches down too, using the previous button state. With the side button held, the app sends hover, button-down, proximity-out, tip-down without reasserting the held button. [libinput tablet_flush](https://gitlab.freedesktop.org/libinput/libinput/-/blob/1.28.0/src/evdev-tablet.c) clears button state on proximity-out, so that hover-to-contact transition loses the button modifier. The host also emits proximity-out on ordinary tip-up. Unblock by defining consistent tip/hover/physical-proximity semantics and transporting current stylus-button state through transitions without latching buttons after a real exit. Required permanent T318 Android/host fixtures: held-button hover→down→up→hover, real exit/reentry, eraser, cancellation, view-boundary hover exits and reconnect; verify ordered input frames and modifier state with simulated events, without host uinput. T317 independently releases the kernel key when action=4 explicitly ends proximity; it does not resolve this client/event semantic mismatch. |
| T308 | blocked | low | small | Resolve fork package-maintainer metadata: `README.md:8-9` and `docs/index.html:44,431` identify geraldo-netto as the fork maintainer and majmichu1 as the original author, while `packaging/deb/control:8` and `packaging/arch/PKGBUILD:1` still identify majmichu1 as the package maintainer. `packaging/build-packages.sh` copies that Debian field into fork releases without replacement. Unblock with the intended fork package-maintainer name and public contact address, then align Debian and Arch metadata while preserving upstream authorship and historical attribution. No email address should be inferred from a username or local Git identity. Metadata-only correction needs no artificial regression test. |
| T299 | blocked | medium | small | Resolve graphics-tablet output selection on modern KDE: `host/src/input.rs:primary_non_evdi_output` prefers a `primary` boolean but its comment describes the first enabled physical output. [Plasma 6.4's serializer](https://github.com/KDE/libkscreen/blob/Plasma/6.4/src/configserializer.cpp#L63-L83) emits `priority` instead; Config::setPrimaryOutput assigns priority=1. An isolated probe of the production function with a fake kscreen-doctor reports eDP-1 (priority=2, listed first) instead of HDMI-A-1 (priority=1); no display command was run. UScreen therefore ignores the selected primary and maps the pen to the secondary. Unblock by specifying the intended host-output selection (desktop primary/priority versus first enabled, including when EVDI is primary), aligning comments and supporting the actual KScreen schema. Required permanent T299 fixtures: priority=1 after a secondary, legacy primary=true, disabled/EVDI outputs and missing/invalid priorities; exercise selection and mapping arguments with fake commands only. |
| T288 | blocked | medium | small | Resolve the GUI's selectable graphics-tablet mode with the pen device disabled: `gui/src/main.rs:setting_mode,setting_input_devices` permits and saves pen_only=true/input_pen=false while displaying that nothing will be streamed, but `host/src/main.rs:initial_pen_only` replaces that combination with display mode. Reproduce by selecting graphics-tablet mode, disabling Pen tablet, saving, then restarting; the saved GUI selection and effective daemon mode differ. Unblock by choosing a coherent dependency policy (reject/disable the incompatible selection, or explicitly enable the required pen) and exposing the effective result before restart. Required permanent T288 GUI/config/CLI fixtures: both toggle orders, an existing incompatible file, save/reload, and unchanged valid display-only and pen-only combinations; no real daemon or input-device startup. |
| T287 | blocked | medium | medium | Resolve degree-valued pen tilt versus uinput metadata: `TouchCapture.kt:decomposeTilt`, `docs/architecture.md:Protocol` and `host/src/input.rs:apply_pen` use degrees, but `UInputDevice::new_pen` registers ABS_TILT_X/Y with range -90..90 and resolution=0. [libinput's adjust_tilt](https://gitlab.freedesktop.org/libinput/libinput/-/blob/main/src/evdev-tablet.c) interprets unspecified resolution through its -64..64 degree fallback. An isolated C probe of that upstream conversion with UScreen's metadata maps 45 degrees to 32 and 90 to 64 (negative values likewise). Unblock by choosing consistent angular units, axis limits and nonzero resolution or a correctly scaled fallback, preserving the Android degree protocol and verifying supported X11/Wayland consumers. Required permanent T287 coverage: declared axis metadata and emitted values reconstruct positive/negative, zero and near-flat angles through libinput conversion within a defined tolerance; cover pen/eraser and use an isolated fixture without host uinput devices. |
| T284 | blocked | medium | large | Resolve advertised in-process VAAPI support versus encoder initialization: `docs/architecture.md:Encode` includes VAAPI for both paths and `host/src/encoder.rs:low_latency_options` accepts it, but Encoder::new sets software NV12 and never supplies hardware frames or the configured vaapi_device. A standalone probe of the production Encoder implementation against FFmpeg 6.1 rejects both h264_vaapi and hevc_vaapi at open with unsupported pixel format nv12, before any GPU access. [FFmpeg's VAAPI initialization](https://github.com/FFmpeg/FFmpeg/blob/n6.1/libavcodec/vaapi_encode.c#L2563-L2566) requires a hardware-frames context; selecting VAAPI on this build otherwise causes repeated pipeline failures. Unblock by implementing device selection, hardware-frame allocation/upload and lifecycle handling, or explicitly restricting the optional path with early validation and accurate documentation. Required permanent T284 coverage: selected render-node propagation, supported frame format/context, deterministic initialization errors without display attachment, and successful encode/decode on an isolated VAAPI fixture if support is retained. Distinct from T259's rate-control choice. |
| T283 | blocked | low | small | Align Wi-Fi latency descriptions: `host/src/main.rs:announce_transport` documents a roughly doubled median on a quiet network, whereas `setup_wifi` says the median matches USB and `docs/benchmarks.md:USB vs Wi-Fi` distinguishes 32.0 ms without the radio lock from 22.8 ms with it (USB 22.0 ms). The source comment does not identify a different version, measurement or lock state. Unblock by tying transport guidance/comments to the documented measurement and radio-lock conditions, retaining the tail-latency caveat and avoiding a general guarantee for other hardware. Documentation/comment-only correction needs no artificial regression. |
| T281 | blocked | high | medium | Resolve attachment identity versus geometry readiness: `host/src/main.rs:TabletMonitor::update_primary` publishes detach and attach through a boolean watch channel, while `spawn_display_gate` clears geometry_ready only when it observes false. Tokio watch can coalesce those transitions; a current-thread probe using the production gate sends false then true before yielding and retains active=true, geometry_ready=true from the previous tablet. This conflicts with T223's fresh-geometry-before-attachment requirement in `host/src/capture.rs`. Unblock by representing attachment identity/generation or invalidating readiness at the producer, defining physical-tablet replacement versus same-tablet transport migration, and associating negotiated metadata with the current authenticated session. Required permanent T281 regression: delayed gate consumer, immediate replacement with different native/physical dimensions, and USB/Wi-Fi migration; no helper attach with stale geometry, and no invalidation of newly accepted current-session metadata. Use fake helpers only. |
| T278 | blocked | medium | small | Resolve wireless ADB identity versus USB preference: `host/src/main.rs:transport_of` assumes every network serial contains a colon, but [AOSP's transport definition](https://android.googlesource.com/platform/packages/modules/adb/+/refs/heads/main/transport.h) also uses TLS mDNS names such as adb-43081FDAS000ST-XKzA7F._adb-tls-connect._tcp. A standalone probe using the production transport_of/select_device_transports functions classifies that serial as Usb and keeps it when the same tablet's physical USB transport is newly attached. This defeats the documented USB preference and also lets wifi_device_with accept an already wireless device as its USB prerequisite. Unblock by defining reliable transport classification from ADB metadata or supported serial forms and applying it consistently to selection, deduplication, Wi-Fi setup and labels. Required permanent T278 fixtures: USB, host:port, IPv6 and mDNS wireless serials, same-tablet USB takeover from an active wireless connection, unknown identities, and USB-only Wi-Fi setup eligibility; use fake ADB only. |
| T276 | blocked | low | small | Define consistent dimensions in control replies: `host/src/input.rs:InputConfig::response` reads live FPS/codec from EncoderSettings but always emits virtual_width/virtual_height captured at startup in `host/src/main.rs`; negotiated geometry changes the actual display afterward. A probe using the production response function with startup 2960x1848 and live 1280x800@30 returns width=2960, height=1848, fps=30. `docs/architecture.md:Protocol` presents these together as the display greeting and mode updates without explaining stale dimensions. Unblock by specifying whether width/height describe the current virtual display or scaled encoded stream, implementing that meaning for initial/repeated replies, and documenting fallback behavior before geometry is ready. Required permanent T276 coverage: negotiated resolution changes, reconnect, non-unit stream_scale, and matching serialized host/client fixtures. The current Android decoder uses its native-size hint rather than these fields, so this finding concerns protocol consistency and other consumers. |
| T275 | blocked | medium | small | Resolve manual resolution versus native-size validation: `gui/src/main.rs:setting_resolution` enables configured dimensions when Auto is off and `docs/development.md:135` documents manual capture size, but `host/src/input.rs:negotiated_geometry` rejects the reported native size before checking auto_resolution. A standalone probe using the production negotiation function with configured 1920x1080, auto_resolution=false and valid 300x190 mm reports accepts native 3840x2160 but returns None for 4096x2160 and 5120x3200. Because `host/src/main.rs` starts geometry_ready=false and `CaptureManager::stream_frames` waits for it, a valid manual mode never attaches. Unblock by defining native-metadata validation independently of the selected EDID mode and allowing valid manual geometry to become ready, while retaining safe EDID limits and the T223 pre-attach negotiation requirement. Required permanent T275 tests: manual mode with oversized/undersized native reports, physical-size fallback, valid automatic mode, and explicit behavior for unsupported automatic resolutions; exercise the capture gate with fake helpers only. |
| T269 | blocked | high | medium | Resolve live EVDI setup policy: `scripts/install.sh:activate_evdi`, Debian postinst, RPM %post and Arch post_install/post_upgrade unload/reload an already loaded evdi module without first checking whether sufficient devices exist. This conflicts with the non-destructive add-only setup in `gui/src/main.rs:system_setup_script` and `Makefile:setup-system`; `host/src/capture.rs:evdi_setup_problem_in` explicitly notes unloading can take Xwayland down, while its existing regression requires reload guidance. A shell-only activate_evdi fixture with mocked lsmod/sudo/cat and count=2 still requests modprobe -r evdi. No kernel/module/display operation was executed. Unblock by choosing one setup policy that preserves live DRM devices, provisioning missing capacity through sysfs or deferring to reboot, and aligning all installers, diagnostic guidance and existing coverage. Required permanent T269 command fixtures: module absent, loaded with zero/sufficient/insufficient devices, upgrade, permission failure and active sessions; no automatic unload of a live module. Distinct from T222's unresolved Xorg attach crash. |
| T268 | blocked | medium | small | Align GUI command deadlines with daemon cleanup: `gui/src/main.rs:run_daemon_command` uses `common/src/commands.rs:COMMAND_TIMEOUT` (5 seconds), but `host/src/main.rs:stop_pids` explicitly allows 10 seconds for graceful shutdown. A standalone fixture using the production GUI routing/execution functions and a disposable six-second stop command returns command timed out at 5.002 seconds. Direct Apply & restart consequently abandons the restart while valid daemon cleanup may still be underway; service-manager actions may require longer too. Unblock by defining shared lifecycle deadlines with enough margin for cleanup and selecting them for GUI stop/restart actions, preserving bounded execution and progress reporting. Required permanent T268 regression: delayed successful stop within the CLI budget proceeds to restart; a true over-budget command fails and is reaped; use disposable commands and no real service manager or daemon. |
| T267 | blocked | medium | medium | Resolve authentication-disabled video compatibility: `common/src/lib.rs:FileConfig.require_token` exposes an optional token check, but `VideoReceiver.kt:connectAndReceive` always transmits a saved token and `host/src/stream.rs:handle_client` treats those bytes as unexpected input when token=None. A loopback-only fixture compiled from the production stream module with token=None and a 64-byte saved token received the cached header then EOF within one second. Thus switching authentication off after a normal authenticated session breaks current Android streaming; launching MainActivity without a token does not clear Prefs.hostToken. Unblock by defining and implementing authentication-mode negotiation or compatible optional-token handling without weakening enabled authentication. Required permanent T267 coverage: host/client transitions enabled→disabled→enabled with persisted tokens, legacy tokenless clients when disabled, rejection of wrong/missing tokens when enabled, and idle disconnect cleanup from T139. No real daemon, display or tablet was used for the reproduction. |
| T264 | blocked | low | small | Resolve position normalization across runtime, GUI and doctor: `common/src/lib.rs:Position::parse` accepts case/whitespace and top/bottom aliases, but `FileConfig::sanitize` preserves those raw strings. Runtime capture parses `position=" LEFT "` as Left, while `gui/src/main.rs:setting_position` displays Right and `host/src/doctor.rs:report_configured_display` reports Right because both match only canonical lowercase values. Unblock by canonicalizing every accepted position during sanitization or sharing the parsed representation for every consumer, preserving alias support. Required permanent T264 regression: left/above/below values with case/whitespace and top/bottom aliases survive load/save and produce matching runtime, GUI and diagnostic choices. |
| T263 | blocked | high | medium | Resolve release source-integrity validation versus mutable build inputs: `scripts/publish-release.sh` checks a clean checkout before building and exact local/remote tag identity again afterward, but never checks for source edits made during the build. Using the existing fully offline ReleaseTest harness, a build stub changes tracked `host/src/main.rs` after the first check; HEAD/tag remain unchanged, the checkout is dirty, and the fake API still records published=true (exit 0). No real API or remote repository was contacted. Unblock by building from an immutable tagged snapshot or verifying all inputs remain identical before publication, with a defined policy for ignored build/signing inputs. Required permanent T263 offline regression: modify tracked source during the build, keep HEAD/tag unchanged, and assert no publication/API write occurs; retain all T100/T117/T118 checks. |
| T262 | blocked | medium | small | Resolve release version-validation claims versus regex matching: `scripts/publish-release.sh` says every repeated version must agree with Makefile, but interpolates VERSION directly into grep patterns. With VERSION=1.2.3, the real Android gate accepts `versionName = "1x2x3"` and the Arch gate accepts `pkgver=1_2_3` because dots are regex wildcards; Android permits arbitrary versionName strings. Unblock with literal/parsed equality for every version/date field before building or making API requests. Required permanent T262 offline publisher fixtures: valid exact versions and separator substitutions in Cargo, Android, PKGBUILD and changelog; mismatches must fail before the build marker or mock API is reached. |
| T260 | blocked | medium | medium | Align GUI daemon ownership with the recovered CLI lifecycle: `gui/src/main.rs:poll_status,service_managed` trusts only the PID file and `common::daemon_is_running` (comm/liveness), while `host/src/main.rs:other_daemons,is_daemon_process,show_status` also validates UID/arguments and discovers missing or stale PID files. With a live direct daemon and no PID file, CLI status reports it but the GUI shows stopped and may select an installed inactive systemd unit for subsequent actions. A PID file naming a concurrent uscreen doctor process can instead produce a false running GUI status. Unblock by sharing validated daemon identity/discovery across GUI and CLI, including direct-versus-service routing. Required permanent T260 GUI fixtures: missing/stale/foreign-command PID files, real disposable default-command daemons, and installed inactive service; verify displayed state and action target without invoking the real service manager. |
| T259 | blocked | medium | medium | Resolve the VAAPI bitrate-ceiling contradiction: `common/src/lib.rs:FileConfig.quality` and `docs/development.md:152` describe bitrate as a burst ceiling for every encoder, but both `host/src/capture.rs:encoder_quality_args` and `host/src/encoder.rs:low_latency_options` force VAAPI CQP. [FFmpeg 6.1 rate-control code](https://github.com/FFmpeg/FFmpeg/blob/n6.1/libavcodec/vaapi_encode.c#L1618-L1624) marks CQP as using neither bitrate nor maxrate and bypasses bitrate control parameters for it. The configured ceiling therefore does not constrain this path. Unblock by selecting a driver-supported bounded rate-control policy (with explicit handling of unsupported modes), or documenting and exposing an intentionally uncapped mode. Required permanent T259 coverage: CLI/in-process option parity and capability fallback fixtures, plus real VAAPI high-detail output measurement against the chosen cap. No GPU/device workload was run during this review. |
| T257 | blocked | medium | small | Resolve the token-backup coverage contradiction: `RegressionTest.kt:t036_sessionTokenIsExcludedFromBackup` treats `allowBackup=false` as sufficient, but `Prefs.kt` stores the live host token in ordinary shared preferences and the manifest has no data-extraction exclusions. Android's [Android 12 behavior changes](https://developer.android.com/about/versions/12/behavior-changes-12#backup-restore) and [backup guidance](https://developer.android.com/privacy-and-security/risks/backup-best-practices) state that some OEM device-to-device migrations ignore allowBackup; absent device-transfer exclusions, app data is transferred. Unblock by excluding the credential-bearing preferences from cloud/legacy backup and device transfer, or moving the token to no-backup storage with migration handling. Required permanent T257 coverage must inspect the packaged rules/storage used on API 27 and 31+, retain T036, and demonstrate that the token path is excluded from every applicable transport. No real device migration or credential access was performed during review. |
| T252 | blocked | medium | medium | Enforce the runtime-directory privacy contract: `SECURITY.md:17-18` and `common/src/runtime.rs` promise a per-user mode-0700 directory, but `runtime_dir()` trusts an existing directory (including a symlink), never checks its owner/mode and ignores creation errors. Isolated reproduction: pre-create `$XDG_RUNTIME_DIR/uscreen` with mode 0755 and run the existing `runtime_dir_is_private` test with that temporary XDG_RUNTIME_DIR; it fails with group/world permissions 055. No host runtime directory was changed. Unblock with an explicit validation/error contract for existing directories, rejecting unsafe ownership/symlinks and propagating creation failures while preserving documented fallbacks. Required permanent T252 child-process fixtures: existing 0755/0777 directories, safe 0700 reuse, symlink/wrong-owner paths, unwritable base and successful fallback; verify token/FIFO creation only occurs in an accepted private directory. |
| T251 | blocked | medium | medium | Align doctor process ownership with start/status/stop: `host/src/doctor.rs:check_processes,report_daemon` classifies all `pgrep -x uscreen` results except itself as daemons and says stop cannot reach untracked processes, while `host/src/main.rs:other_daemons,is_daemon_process,stop_daemon` validates same-user daemon arguments and recovers them without PID files. A disposable executable named uscreen launched with the doctor argument is selected by doctor's pgrep query; T237 correctly excludes it from daemon discovery. Doctor can therefore report a concurrent diagnostic command as an orphan and recommend broad pkill commands; missing/stale PID files also misclassify valid recovered daemons and helpers. Unblock by sharing validated process discovery and updating ownership/remediation reporting, including the related FIFO identity work in T245. Required permanent T251 fixtures: concurrent doctor/status commands, another UID, default/explicit daemons, and missing/stale PID files; diagnostics must not signal any processes. |
| T250 | blocked | medium | small | Resolve the fork release-signing contract: `SECURITY.md:69-73` guarantees every project APK uses the fixed certificate `ee813fed...fa8a759` and treats failure to update in place as proof of an unofficial build, while `android/app/build.gradle.kts:8-9` and `docs/development.md:169-180` direct maintainers to create their own signing key. The build accepts any supplied release keystore; the fork's intended signing identity and migration from upstream have not been established. Unblock with the maintainer's chosen fork certificate and update/migration policy, then align documentation and, if an official certificate is required, verify its public fingerprint before publishing. Documentation-only changes need no artificial test; a publication gate needs a permanent wrong-certificate regression. |
| T247 | blocked | high | small | Resolve the pen-mode authentication greeting mismatch introduced in T241: `host/src/input.rs:InputResponse` serializes `status: "connected"` (also documented in `docs/architecture.md`), but `TouchCapture.kt:onMessage` only sets `authenticatedControl` for `type: "connected"`. The production greeting therefore leaves pen-only mode permanently displaying its reconnect overlay. `SettingsLayoutTest.kt:t241` passes because its synthetic greetings use the unsupported `type` field. Unblock by aligning the client and fixtures with the serialized host contract. Required permanent T247 coverage: one shared real greeting fixture checked against Rust serialization and consumed by Android, authenticated pen canvas visibility, disconnect/reconnect and stale callbacks; preserve all T241 lifecycle assertions. |
| T245 | blocked | high | medium | Resolve the orphan-cleanup targeting contradiction: `host/src/capture.rs:retire_orphan_capture` promises to avoid unrelated ffmpeg/helper processes, but interpolates the literal FIFO path into unescaped `pkill -f` regular expressions and gives ffmpeg no argument boundary. Non-signalling POSIX ERE reproduction: a FIFO at `/tmp/uscreen-review-[1]/capture.fifo` fails to match its own helper/ffmpeg command and matches `/tmp/uscreen-review-1/captureXfifo` instead. Unblock with same-user process identity checks and exact parsed FIFO arguments before signalling, including a bounded wait for retirement. Required permanent T245 fixtures: literal regex metacharacters/spaces in runtime paths, prefix-related FIFO names, unrelated processes and the intended orphan; signal only disposable fixture children. |
| T222 | blocked | high | large | Diagnose and prevent the Xorg crash on EVDI attachment under Cinnamon. On 2026-09-17 at 01:52:40 CEST, Xorg faulted at address `0x20` through `ProcRRSetCrtcGamma`, immediately after `host/evdi/evdi_helper.c:1361` attached the virtual display; the later helper restart did not precede the fault. Blocked on identifying the failing Xorg state with a symbolized core or isolated reproducer and validating a mitigation. Required permanent coverage: isolated Xorg/EVDI attach, gamma update and detach without server/session loss; current fake-helper tests cannot exercise the system display server. See [incident evidence](docs/reviews/2026-09-17-cinnamon-restart.md). |
| T224 | blocked | medium | small | Resolve the desktop-backend contradiction: `docs/compatibility.md:21-26` reserves automatic placement for KDE Wayland and leaves X11 placement to desktop settings, but `host/src/capture.rs:357-391,1100-1124` unconditionally invokes KScreen placement on Cinnamon/X11. Observed result: an already-rendering EVDI output incurs approximately 3s of retries and a false missing-output warning on each setup. Unblock by gating automatic placement to the documented backend while preserving X11 streaming. Required permanent T224 regression: Cinnamon/X11 performs no KScreen placement calls or retry delay; KDE Wayland still enables and positions its output. |
| T226 | blocked | high | medium | Make partial FIFO writes recover at a verified frame boundary. `host/evdi/evdi_helper.c:719-740,767-775` claims closing the FIFO resynchronizes it, while `host/src/encoder_io.rs:19-23` requires the reader to observe EOF; immediate reopening can prevent that. Isolated production-code reproduction: stall a capacity+64-byte frame until timeout, reopen before the reader drains, then send the next frame; the reader receives 1,048,576 old bytes plus 64 new bytes without EOF. Unblock with coordinated reader retirement/restart or explicit framing. Required permanent T226 regression: real FIFO, partial write, stalled reader and immediate reopen; neither encoder path accepts a mixed frame. |
| T229 | blocked | medium | medium | Resolve bundled-library versus installer dependency requirements. `docs/installation.md:89-90` says release users need no distro libevdi package and `docs/development.md:20-28` provides Debian 12's source-library route, but `scripts/install.sh:62-65` includes libevdi in both ffmpeg/adb install transactions. Mocking unavailable libevdi reproduces failure of both transactions, leaving the required tools uninstalled even for a bundle carrying libevdi. Unblock by separating runtime tools, source-build headers/library and kernel setup; make `check_deps` recognize bundled and `/usr/local` libraries. Required permanent T229 fixtures: Debian without libevdi packages, source checkout and prebuilt bundle. |
| T230 | blocked | medium | medium | Resolve distro-agnostic installation versus Fedora-only repository setup for RHEL/CentOS (`scripts/install.sh:18-49,104-109`). Reproduction: call `install_distro_deps rhel` with `%fedora` undefined; it requests `free/fedora/rpmfusion-free-release-%fedora.noarch.rpm`. Unblock by selecting dependencies/repository guidance for the actual distro and version, with a clear manual path for unsupported package managers. Required permanent T230 command fixtures: Fedora, immutable Fedora, RHEL/CentOS derivatives and unknown IDs; never apply Fedora repository URLs to other families. |
| T231 | blocked | medium | large | Resolve the requested distro-agnostic behavior versus systemd-only autostart. `scripts/install.sh:198-206,288` swallows failed service installation/enabling and promises startup at login; `gui/src/main.rs:13-30` only invokes systemctl, and `Makefile:43-47` reports service success after ignored errors. On a host without a systemd user manager, direct launch can work but autostart cannot. Unblock with detected service-manager capabilities and a supported desktop-autostart fallback, accurate setup results and matching module/uinput permission guidance. Required permanent T231 fixtures: working systemd, absent systemctl and unavailable user bus; preserve direct start/stop. |
| T232 | blocked | medium | small | Unify executable discovery: `gui/src/main.rs:85-90` requires external `which`, whereas `host/src/doctor.rs:85-90` accepts any existing PATH entry, including non-executable files/directories. A production GUI-function probe with executable adb but no which reports adb missing; doctor would report it present. Unblock with shared PATH lookup that checks executable regular files and needs no distro-specific helper command. Required permanent T232 regression: absent which, executable tool, non-executable shadow entry, directory and later valid PATH entry. |
| T233 | blocked | medium | medium | Resolve inconsistent XDG installation paths (`scripts/install.sh:8,187,198-200`; `Makefile:38-44`; `common/src/lib.rs:234-242`). Config and script-installed icons honor XDG overrides, but launchers and user units go under fixed HOME paths. Reproduce with custom XDG_DATA_HOME/XDG_CONFIG_HOME: the launcher/unit is absent from the configured location, although its icon/config is there. Unblock by sharing the base-directory policy across both installers and documenting any intentional fixed locations. Required permanent T233 regression: isolated custom XDG directories, paths with spaces, and unset-variable defaults. |
| T234 | blocked | medium | small | Resolve supported non-KDE Wayland sessions versus fatal KWin diagnostics. `docs/compatibility.md:27-30` permits GNOME/Sway with manual mapping, but `host/src/doctor.rs:703-735` marks missing KWin as FAIL and recommends installing busctl/qdbus on every non-X11 session, even with all input devices disabled. Unblock by detecting the desktop and requested capabilities: reserve KWin failures for KDE features, and report manual mapping guidance elsewhere. Required permanent T234 fixtures: GNOME/Sway without KWin, display-only mode, Cinnamon/X11 and KDE with a failed bus. |
| T238 | blocked | medium | medium | Resolve automatic rotation's natural-portrait assumption versus support for arbitrary tablets (`android/app/src/main/java/com/uscreen/MainActivity.kt:226-243`; `docs/compatibility.md:45-46`). Android reports sensor angles relative to the device's [natural orientation](https://developer.android.com/reference/android/view/OrientationEventListener#onOrientationChanged(int)); the listener only handles 90/270 degrees. On a naturally landscape tablet, passing through portrait pins a landscape direction, then returning at 0/180 cannot update that pin. Unblock by deriving the natural display orientation and mapping both landscape directions appropriately. Required permanent T238 Robolectric coverage: portrait/landscape natural panels, both flips, hysteresis and unknown/flat readings. |
| T240 | blocked | low | small | Resolve encoder-selection inconsistency: `common/src/lib.rs:63-67` and `docs/development.md:147-149` support HEVC VAAPI, but `gui/src/main.rs:734-768` offers only H.264 for AMD/Intel; HEVC requires manual config/CLI editing. Unblock by exposing the supported HEVC VAAPI choice with correct labeling and compatible 10-bit controls, preferably from shared encoder metadata. Required permanent T240 GUI regression: select HEVC VAAPI, save/reload it and preserve its depth/device settings. |
| T242 | blocked | low | small | Align fake-tablet token discovery with the host runtime-directory contract (`scripts/fake-tablet.py:14-16,114-116`; `common/src/runtime.rs:14-36`). With XDG_RUNTIME_DIR pointing to a nonexistent directory, the script uses it verbatim while the daemon falls back; without `/run/user/<uid>`, only the daemon uses HOME/.cache. The advertised fake-tablet workflow then fails opening the token. Unblock by using matching fallback rules or accepting an explicit runtime/token path. Required permanent T242 fixtures: valid/invalid XDG_RUNTIME_DIR and absent user runtime directory, asserting both sides select the same token. |
