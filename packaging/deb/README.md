# Legacy Debian fixtures

AppImage replaced the Debian release asset in T308. These metadata and postinst
files remain for permanent historical integration regressions; the package
builder and publisher do not produce or upload `.deb` files. Use the AppImage,
RPM, Arch or portable tar workflows documented in `docs/installation.md`.
