# Rust
/target/
/target-portability/
/target-deb12/
/target-appimage-tools/
/target-appimage-sources/
/target-appimage-ffmpeg/

# Cargo.lock is committed intentionally (this is a binary project)

# Android
android/.gradle/
android/.kotlin/
android/build/
android/app/build/
android/local.properties
android/.idea/
*.apk
*.aab

# Release signing — NEVER commit these
keystore.properties
keystore-password.txt
*.keystore
*.jks
*.[pP]12
*.[pP][fF][xX]
*.[kK][eE][yY]
*.[pP][eE][mM]
id_rsa
id_dsa
id_ecdsa
id_ed25519
# This certificate contains only the fork's public verification key.
!/docs/release-certificate.pem

# Release output (keep archived benchmark evidence under docs/ visible)
/dist/
/*.AppImage
/AppDir/
/*.AppDir/

# EDID binaries (regenerate with: python3 scripts/gen-edid.py)
edid/*.bin
s9ultra.bin

# EVDI compiled helper
host/evdi/evdi_helper
host/evdi/*.o
host/evdi/*.a

# OS
.DS_Store
*.swp
*.swo
*~

# Temporary files
/tmp/
*.fifo
hs_err_pid*.log
replay_pid*.log

# Local tooling and notes — not part of the project
.claude/
.cursor/
.aider*
.windsurf/
.vscode/
.idea/
CLAUDE.md
AGENTS.md
.env
.env.*
*.local

# Python tooling environments and caches
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
__pycache__/
*.py[cod]
