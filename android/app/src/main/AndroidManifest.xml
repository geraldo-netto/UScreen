<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- The service maintains a connection to an external display host, so it
         uses connectedDevice. On Android 14+, this type requires its dedicated
         FGS permission plus a qualifying permission or USB permission grant.
         CHANGE_NETWORK_STATE satisfies the declared-permission prerequisite.
         mediaPlayback has no MediaSession runtime prerequisite in the FGS type
         rules; it describes a different use case. See:
         https://developer.android.com/develop/background-work/services/fgs/service-types -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- For the Wi-Fi lock that keeps the radio out of power save while
         streaming. Read-only: it does not let the app change any setting. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="UScreen Display"
        android:supportsRtl="true"
        android:theme="@style/Theme.UScreen"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="sensorLandscape"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- DUMP is granted to adb shell, not ordinary installed apps. Token
             delivery goes through this gate; the launcher ignores extras. -->
        <activity
            android:name=".TokenActivity"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.NoDisplay" />

        <receiver
            android:name=".CodecReportReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP" />

        <service
            android:name=".StreamingService"
            android:foregroundServiceType="connectedDevice"
            android:exported="false" />
    </application>
</manifest>
